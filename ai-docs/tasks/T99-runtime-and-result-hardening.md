# T99 · 运行生命周期与大结果处理优化

- 状态：待开工。
- 优先级：P2；来源为整体评审的后续优化项，不阻塞 T98 已有缺陷修复。
- 依赖：T98；性能测量沿用 T01/T84，真机验收沿用 T94/T96。

## 分批范围与验收

- [ ] 将作业准备/启动/轮询/回传状态机抽为可注入 runner 的服务，Tauri 只桥接进程与 Channel；用假 runner 覆盖取消准备、启动失败、远端状态未知与资源占用策略。
- [ ] CSV 导出、张量/矢量统计下沉 core，避免前端创建与场规模成比例的中间对象；修改前后使用相同百万单元夹具对比耗时/峰值内存。
- [ ] 场缓存与槽位明确绑定工程、运行、时间步及文件修订，评估 Arc 共享以减少 Vec 克隆；覆盖乱序返回、并发读取和文件更新。
- [ ] 将 EnvironmentCheck / CaseOutcome DTO 收口至 core models，同步 TS 镜像与契约测试。
- [ ] 逐条核对 T92/T93 的纯算法、编排、产品入口及验收状态，避免用已实现算法代替端到端完成。
- [ ] 对跨 store/IPC/进程链继续做故障注入；不以行覆盖率替代进程存活、槽位身份和输出归属断言。

每批遵循许可/性能/包体依赖准入规则；无新增依赖时不虚构对比。未知远端状态的恢复与调度策略必须明确且可测，不能只改提示文案。
