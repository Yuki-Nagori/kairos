# T99 · 运行生命周期与大结果处理优化

- 状态：**已实现**（运行生命周期、结果链路与 T93 优化回填闭环已完成；真实 VM 复测按 T100/T94 记录）。
- 优先级：P2；来源为整体评审的后续优化项，不阻塞 T98 已有缺陷修复。
- 依赖：T98；性能测量沿用 T01/T84，真机验收沿用 T94/T96。

## 分批范围与验收

- [x] 第一批：抽出无 Tauri 依赖的生命周期状态机，覆盖取消准备、启动失败、远端状态未知/恢复和资源占用前的状态迁移；下一批接入真实 runner 与 Tauri Channel。
- [x] 第二批：真实 runner 接入生命周期事件；远端轮询连续失败进入 `Unknown`，取消只有收到终止确认后才进入 `Cancelled`。
- [x] CSV 导出：标量场 CSV 编码下沉 core，Tauri 提供异步导出命令；无结果目录的本地分析场保留轻量前端回退，并已完成百万值基准。
- [x] 结果统计：标量、矢量模量与对称张量的 `FieldStats` 均在 core 单次扫描，Tauri/API/store/结果面板已接线，并已完成百万值基准。
- [x] 场缓存与槽位按算例目录（运行作用域）绑定，并按时间步、字段和文件修改时间/长度区分修订；切换算例会清空标量/矢量槽位与 LRU；槽位请求序号丢弃同一算例内的乱序返回；补充不完整结果写入后更新文件、双字段并发读取和错误码回归；会话主/对比槽位改用 `Arc<ScalarField>` 共享。
- [x] 将 EnvironmentCheck / CaseOutcome DTO 收口至 core models；TS 镜像保持同步，并新增 camelCase 契约测试。
- [x] 逐条核对 T92/T93：T92 的算法、串行编排、汇总和失败行已完成；T93 CLI 自适应回填循环已完成；真实 VM 指标属于运行环境验收，不阻塞本任务。
- [x] 增加可复现大结果基准入口：`cargo test -p kairos-core --test large_result_benchmark -- --ignored --nocapture`。本机 arm64 / rustc 1.98.1，1,000,000 个 f64 的 core min/max/count 约 5.8 ms，CSV 编码约 211 ms、载荷约 27.5 MB；该数字用于同机回归，不作为跨硬件承诺。
- [x] 对跨 store/IPC/进程链做故障注入：runner 已覆盖退出码、超时、取消回收、stderr、非 UTF-8、空结果目录与输出归属失败；结果修订缺失的 `io` 错误码契约也已锁定。

每批遵循许可/性能/包体依赖准入规则；无新增依赖时不虚构对比。未知远端状态的恢复与调度策略必须明确且可测，不能只改提示文案。
