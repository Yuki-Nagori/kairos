# T83 · 多型腔与流道系统参与填充

- 阶段：B2（求解器 · 浇注系统）
- 依赖：T07（流道 / 浇口建模）、T61（浇口 portal → inlet patch）
- 优先级：**P1**（C4 完成度的下一块：现在浇口只是一个入口面，流道不参与流动）
- 状态：**部分实现**（2026-09-15：范围 4 完成；范围 1/2/3 的上游依赖已核实并写明，见 §进度）

## 目标

一模多腔与流道系统进入求解：熔体经流道分配后再进各腔，腔间填充差可见。

## 现状

- `RunnerElement`（流道 / 浇口）已在模具网络面板建模并做连通性校验；
- case 生成只把**浇口**当成 `inlet` patch（`GatePortal`：Gate 单元型腔端 +
  直径/2，等效面积 πr² 保底），流道本体不参与网格与流动；
- 多浇口在数据上支持（`gates` 可多个），但没有多腔分配策略。

## 范围

1. **流道几何进网格**：梁 → 体网格（或等效阻力模型），与制品网格合并 / 分区；
2. **多腔分配**：每个腔一个浇口的流量分配与边界条件（顺序填充 / 平衡填充的判据）；
3. **case 与面板对应**：模具网络面板的流道参数（直径 / 起止）如何落进 case；
4. 校验：流道-制品连通、浇口面积与流道截面的匹配（有效性提示，接 T72）。

## 进度与依赖（2026-09-15 核实）

| 范围                             | 状态           | 说明                                                                                                                                                                                                                          |
| -------------------------------- | -------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 4 校验：流道-制品连通 / 截面匹配 | **已完成**     | 连通性由既有 `check_mold_network`（端点焊接成图、孤立单元报错、浇口视为网络端点）覆盖；浇口与流道的**截面匹配**本轮补进同一函数（浇口直径 > 所在流道直径 → 截面先收缩再放大，填充会先在这里失速）                             |
| 1 流道几何进网格                 | **等上游口径** | 流道单元已在双域网格里以**梁单元**表达（`dualdomain` 消费 `RunnerKind::Runner`：表面三角形齐厚度 + 一维梁）；「进网格 / 参与求解」取决于上游能否消费一维流道（等效阻力模型 vs 体网格分区），**与 T82 的降维口径是同一个问题** |
| 2 多腔分配                       | **等上游口径** | 多腔流量分配与「顺序 / 平衡填充」判据由求解器侧的多入口 BC 决定，Kairos 侧无法独立定义                                                                                                                                        |
| 3 面板参数落 case                | **部分**       | 浇口参数已落 case（`gate_portals` → inlet patch 选择，含入口面积与等效直径回显）；流道（非浇口）参数落 case 同样等上游口径——先写进 case 也没有消费者                                                                          |

**需要向上游确认的两个问题**（与 T82 合并提问，避免重复打断上游）：

1. moldingFoam 能否消费一维流道表示（梁 / 等效阻力模型），还是要求流道也进体网格？
2. 多腔（多 inlet patch）的流量分配是求解器按几何自动分配，还是需要 case 侧给出分配曲线 / 权重？

在上游答复之前，T83 只做数据位与校验类工作（已完成），不做 case 契约的推测性扩展。

## 非目标

- 热流道 / 阀浇口的时序控制（后续按需）；
- 流道内的详细传热（先按绝热或简化）。

## 验收标准

- 一模两腔样件（T07 的演示数据）跑通填充，两腔填充时间差有明确读数；
- 与单腔 case 对比，入口压力 / 填充时间偏差在可解释范围；
- `bun run verify` 全绿。
