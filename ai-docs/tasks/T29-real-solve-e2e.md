# T29 · 真实求解端到端验证（OpenFOAM 环境）

- 阶段：进行中（链路已跑通：bundle v0.2.1 上 case 生成 → checkMesh → 并行求解 → 结果回传 → CLI 读取全程 exit 0；Kairos 侧 6 项集成缺陷已修。剩余：前端视口/图表验证（桌面控制权限未授权）与真实 STL 一轮（本机无几何素材）。见 e2e-solve-report.md）
- 依赖：T09、T10、T11（已完成的求解集成代码）、T34（求解入口收口）
- 优先级：**P1**

## 目标

在真实 OpenFOAM 14（.org，foamRun 模块化框架）环境中完整跑通「导入 → 网格 → case 生成 → 求解 → 结果读取」链路。目前求解集成代码从未对接过真实求解器（M4 评审遗留），这是全项目最大的未验证假设。后续 OpenFOAM-14 fork（注塑求解模块，见 T34 对接清单）就绪后，在本任务框架下复验一轮。

## 范围

- 本机安装 OpenFOAM 14（.org）（按依赖面板应用内下载 + 编译流程走一遍，顺带验证引导文案与自动编译）；
- 样例立方体 + 真实 STL 各跑一轮填充分析（controlDict 入口为 `foamRun` + `SOLVER_MODULE`）；
- 核对：case 字典字段被 foamRun 接受（注意 Kairos 扩展字典 packingDict/coolingDict 当前会被 vanilla 模块忽略）、进度行被正确解析、结果场可被 services::results 读取、前端视口/图表展示合理；
- 记录并修复暴露的集成缺陷（case 模板字段错漏、超时、路径含空格/中文等）。

## 非目标

- 性能调优（真实资产性能计时属 T01 基准回填）；
- 多核扩展性调参；
- 注塑物理字典在 vanilla 求解器上的生效性（依赖 fork 模块，见 T34）。

## 交付物

- 修复补丁（如有）；
- `ai-docs/reviews/e2e-solve-report.md`：两轮求解的配置、日志摘要、结果截图与发现的问题清单。

## 验收标准

- 样例立方体填充分析在真实求解器上完成且结果可读；
- 发现的集成缺陷全部修复或明确记录为已知问题；
- 依赖面板下载 / 自动编译流程被实测验证可用。
