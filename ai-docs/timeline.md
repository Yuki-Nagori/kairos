# 开发时间线

按 git 提交历史记录 Kairos 的开发过程：一行一 commit，条目与时间为提交真实信息
（2026-09-12 整体重建自 git log，此前批次补记导致的日期断裂与时间漂移就此消除）。

## 2026-09-07

- **22:19** chore: 工程基础配置（Node 工具链、编辑器规范、许可证）
- **22:19** feat: Cargo 工作区与 kairos-core 领域层
- **22:19** feat: Tauri 适配层与桌面配置
- **22:19** feat: 前端工作台壳子
- **22:20** docs: AGENTS 路由与 ai-docs（架构约定、时间线）
- **22:20** docs: README 项目介绍与使用说明
- **22:22** docs: 时间线规则补充提交对应关系，移除非提交决策备注
- **22:42** docs: 注塑成型路线图与任务拆解 T01–T18（openInjMoldSim 选型）
- **22:45** docs: GPU 辅助计算路线（wgpu 跨厂商），新增 T19/T20 并同步预算与红线
- **22:47** docs: 里程碑评审与整体优化固化为循环任务 T21
- **23:10** feat(T01): 性能预算与基准框架
- **23:12** feat(T02): CI/CD 流水线
- **23:13** review(T21): M0 里程碑评审——八项清单通过，1 项立即修
- **23:39** feat(T03): 仿真项目模型与持久化
- **23:52** feat(T04): 材料数据库

## 2026-09-08

- **00:25** feat(T05): 几何导入（STL）
- **00:56** feat(T06): 3D 体积网格生成
- **01:12** feat(T07): 浇口流道与冷却水路建模
- **01:24** review(T21): M2 里程碑评审——八项清单通过，README 领域功能同步
- **01:40** feat(T09): OpenFOAM 运行时与 openInjMoldSim 集成
- **01:56** feat(T10): 求解作业调度器
- **02:15** feat(T11): 填充分析端到端闭环
- **02:21** feat(T12): 保压与冷却分析
- **02:21** review(T21): M3 里程碑评审——八项清单通过，求解链路关闭
- **02:36** feat(T13): 结果数据模型与流式读取
- **02:54** feat(T14): 3D 视口渲染引擎（WebGL2）
- **03:00** feat(T15): XY 曲线与探针
- **03:03** feat(T16): 设计系统与工作台布局
- **03:08** feat(T17): 仿真报告导出
- **03:23** feat(T19,T20): GPU 计算基础设施与加速算子
- **03:28** review(T21): M4 里程碑评审——八项清单通过，3 项立即修
- **03:29** feat(T18): 打包分发与发布流水线
- **03:31** review(T21): M5 收官评审——全项目完成度审计通过
- **04:39** chore: 已完成任务标注 -done，入库下一阶段任务 T22–T25
- **04:42** fix(ci): 修复 1.98 stable 新 clippy 并钉版工具链
- **04:51** feat(T22): Gmsh 网格引擎评估与解析原型
- **05:00** feat(T23): 时间步动画通道
- **06:12** feat: 应用内运行时依赖管理器（许可分级 + 就绪探测 + 引导安装）
- **06:41** feat: 依赖面板应用内下载（流式进度 + 受管目录）
- **06:46** docs: 移除产品文档中的第三方商标直接引用
- **06:49** fix(ci): 工具链安装与 rust-toolchain.toml 钉版对齐，显式安装 rustfmt/clippy 组件
- **06:52** docs: 补入 T26（UI 重构）与 T27（组件下载）任务文件
- **06:59** feat(T16): 设计系统与工作台布局
- **07:11** feat: 主题切换按钮（深色/浅色 CSS 变量）+ app-header 整合
- **07:12** feat(T26): Moldflow 风格 CAE 工作台 UI 重构 + 主题切换
- **07:20** docs: 时间线改用 git 历史真实时间戳
- **07:21** fix: theme.ts 移除未使用导出
- **07:35** feat: 三端统一使用系统窗口按钮，macOS 交通灯避让 + 头部拖拽区
- **07:35** docs: 补入时间线最近三条记录
- **07:40** fix: 主题样式改由 theme.ts 统一注入，兑现「新增主题零改码」
- **07:40** docs: 补入主题注入修复时间线
- **07:48** fix: 头部类被覆盖致折行 + 浅色主题 zinc 映射反转
- **07:48** docs: 补入视觉对比修复时间线
- **07:50** fix: 恢复三端标准系统标题栏（macOS 不再隐藏）
- **07:50** docs: 补入标题栏恢复时间线
- **07:53** feat: 头部左上角加入品牌图标（直接复用 public/icon.svg）
- **07:53** docs: 补入品牌图标时间线
- **07:54** feat: Tauri 窗口默认以全屏启动（退出全屏回落到最大化）
- **07:54** docs: 补入默认全屏时间线
- **07:56** feat: 沙漏品牌图标统一三端 bundle 图标；全屏策略按平台区分
- **07:56** docs: 补入图标统一与平台全屏策略时间线
- **07:58** fix: 清理界面 emoji 与装饰性符号
- **07:58** docs: 补入 emoji 清理时间线
- **08:00** fix: macOS 启动窗口偏左——弃用配置式 center/maximized，改为启动时最大化
- **08:00** docs: 补入窗口位置修复时间线
- **08:04** feat: 原生应用菜单（文件/编辑/视图/分析/结果/工具/帮助）
- **08:04** docs: 补入原生菜单时间线
- **08:08** docs(T28): 仓库结构评审入库——state 拆分 / 着色器外置 / 组件分组
- **08:08** docs: 补入结构评审时间线
- **08:11** refactor: 注释与代码质量走查——菜单构建去样板、下拉框样式归口 ui.ts
- **08:11** docs: 补入代码质量走查时间线
- **08:17** refactor: 输入框构建归口 ui.ts，修正求解日志失实注释
- **08:17** docs: 补入输入框归口与注释修正时间线
- **08:22** fix: 网格流水线线程模型合规 + 错误契约收口（review F4/F5/F6）
- **08:22** docs: 补入线程模型合规时间线
- **08:26** fix(ci): release 流水线钉版工具链 + 交叉产物收集路径（review F7/F8）
- **08:26** docs: 补入 release 流水线修复时间线
- **08:36** ci: macOS 仅保留 arm64，补齐 Ubuntu 构建与 Linux 门禁
- **08:36** docs: 补入 Ubuntu 平台支持时间线
- **08:41** ci: Release 手动触发 + 输入标签自动同步三处清单版本号
- **08:41** docs: 补入手动发布时间线
- **08:45** refactor: 版本号收敛为根 Cargo.toml 单一事实源
- **08:45** docs: 补入版本单源化时间线
- **08:57** feat: OpenFOAM 与 openInjMoldSim 支持应用内直接下载
- **08:57** docs: 补入直链下载时间线
- **08:59** docs: 修正直链下载放宽后残留的旧口径注释
- **08:59** docs: 补入注释修正时间线
- **09:05** feat: 收口 review 遗留 F1/F10/F11/F12/F13/F14
- **09:06** docs: 补入遗留收口时间线
- **09:13** fix: 修复 CI 裸 toolchain 步骤；锁中毒恢复（F2）+ WebGL 上下文恢复（F9）
- **09:14** docs: 补入 F2/F9 修复时间线
- **09:15** fix(windows): 补 CommandExt 导入，creation_flags 编译失败
- **09:15** docs: 补入 Windows 编译修复时间线
- **10:45** refactor(T28): state 按域拆分 / WGSL 外置 / 面板分组
- **10:51** research(T24): 一维 Stefan 问题 PINN spike——前沿误差 12.2%，限定演示用途
- **10:55** research(T25): moldflow-api 分层概念对照笔记（仅概念借鉴，不引入依赖）
- **10:56** docs: T24/T25/T28 标注完成，任务索引与时间线同步
- **11:04** feat(ui): 视觉全面升级——海军蓝 CAE 色板 + 控件精修
- **11:04** docs: 补入 UI 升级时间线
- **11:07** docs: 入库下一阶段任务 T29–T33
- **11:38** feat(download): 修复 Gmsh 死链 + 下载体验优化（失败重试 / 跨会话清单 / 不占全局 busy）
- **11:38** docs: 补入下载优化时间线
- **11:54** fix(ui): 布局对标 ui.html——整页锁定、卡片零裁切、视口主导
- **11:54** docs: 补入布局重构时间线
- **12:48** feat(download): 下载文件智能命名 + 压缩包自动解压
- **12:48** docs: 补入自动解压时间线
- **12:59** feat(test): 建立覆盖率门槛——Rust core ≥90% / 前端 lib ≥90%
- **14:31** feat(test): 覆盖率门槛拉满——TS lib 四维 100% · Rust core 行覆盖 98.88%
- **14:31** docs: 补入覆盖率门槛拉满时间线
- **14:46** refactor(test): 全部测试集结到根目录 tests/ 并分类
- **14:46** docs: 补入测试集结时间线
- **14:47** docs: AGENTS 路由表登记测试地图
- **14:50** refactor(test): tests/frontend 更名 tests/web，与 src-web 镜像对应
- **14:50** docs: 补入测试目录更名时间线
- **14:56** refactor(ci): 简化为三平台单作业流水线，消除重复编译
- **14:56** docs: 补入 CI 简化时间线
- **15:04** refactor(download): 文件名推导提取为纯函数并修正 .tar.gz 截断 bug
- **15:05** docs: 补入文件名推导修正时间线
- **15:11** docs: gap_tests 头注释去重，results expect 消息去重
- **15:18** fix(test): gap_tests 临时目录幂等创建，消除本地重复运行的 flaky 隐患
- **15:18** docs: 补入 F15 收口时间线
- **15:46** feat(deps): 依赖就绪检测纳入应用内受管副本；修复 zip 解压命令
- **15:46** docs: 补入依赖链修复时间线
- **15:52** test: 删除永真式断言用例，契约测试导入合并
- **15:53** docs: 补入用例清理时间线
- **16:04** fix(stats): MinMax 类型改模块内私有（knip 无外部消费者）
- **16:05** docs: 补入 F16 收口时间线
- **16:13** refactor(ui): materials-panel 小节标题收编 ui.sectionLabel
- **16:13** docs: 补入面板标题收编时间线
- **16:21** docs(deps): 依赖 hint 按各自真实安装流程改写
- **16:21** docs: 补入依赖 hint 改写时间线
- **17:06** fix: F15 收口——gap_tests 临时目录幂等创建；依赖 hint 按真实流程改写
- **17:06** docs: 补入 F15 收口时间线
- **17:36** feat(deps): 一键下载后自动编译——日志实时滚动进依赖面板
- **17:36** docs: 补入自动编译时间线
- **21:06** feat(solver): 移除 openInjMoldSim，求解入口切到 OpenFOAM 14 foamRun（T34）
- **21:07** refactor(deps): 组件生命周期统一为 componentStages 阶段状态

## 2026-09-09

- **13:58** feat(vm): 虚拟机适配层——Multipass/WSL2 一键安装、内嵌 Shell 与退出联动关机（T35）
- **14:06** fix(vm): 实例探测语义修正 + 启动失败回落创建 + 日志清洗 ANSI（真机反馈）
- **14:19** fix(vm): 探测异步化 + 超时保护；新增 Linux native provider（真机反馈）
- **14:20** chore: 用户提供的 shell 图标素材移入 public/（沿用 icon.svg 的静态资源约定）
- **14:24** feat(vm): Shell 模拟终端化 + 面板浮于右下角 + --memory 修正（真机反馈）
- **14:30** fix(vm): 虚拟机命令对齐 moldingFoam README
- **14:38** feat(vm): 虚拟机默认规格按宿主机动态推导
- **14:48** feat(vm): 国内时区自动走国内镜像源下载 24.04 云镜像
- **14:52** feat(ui): Shell 环境入口上状态栏，切换右下角终端面板显隐
- **15:07** fix(ci): platform_command 按平台拆分变体，修非 macOS 的 unused_mut
- **15:11** refactor(vm): 代码与注释优化
- **15:25** fix(ci): 补装 cargo-llvm-cov；Windows zip 解压显式 -xf 模式位

## 2026-09-10

- **00:59** feat(deps): 求解环境改下载 moldingFoam release bundle，编译流程退役
- **01:02** refactor(deps): 求解环境条目正名 moldingfoam——基于 OpenFOAM-14 但不是 OpenFOAM-14
- **01:10** feat(deps): 已安装组件的在线检查更新
- **01:19** docs(tasks): T36 立项——moldingFoam 契约 case 对接 + bundle 进 VM 执行
- **01:23** feat(ci): 覆盖率门禁扩展到三平台全跑
- **01:25** docs(tasks): T34/T35 完成转正（-done 后缀 + 索引同步）
- **01:33** feat(case): case 生成对齐 moldingFoam case-contract v1.1（T36 Part 1）
- **01:36** fix(vm): 镜像下载补建父目录；终端清洗覆盖退格重绘 spinner（真机反馈）
- **03:10** fix(vm): 状态解析大小写不敏感 + Shell 前自动拉起实例（真机反馈）
- **03:19** fix(vm): Shell 子进程包 PTY，修复输入无法到达虚拟机（真机反馈）
- **03:21** docs: Icons8 图标署名（README 致谢 + icons.ts 来源头注）
- **03:23** fix(ui): 图标 createElementNS 构建，修复 Tauri 窗口不显示
- **03:25** feat(ui): 虚拟机功能接入原生工具菜单
- **03:29** refactor(ui): shell 图标改图片资源方式加载
- **03:31** refactor(ui): 菜单路由抽独立文件 + 关于对话框致谢
- **03:33** refactor(ui): main.ts 瘦身——快捷键与虚拟机浮层各自成模块
- **03:41** feat(vm): T36 Part 2——bundle 部署进虚拟机 + 作业 VM 内执行（闭环）
- **03:42** docs(tasks): T36 完成转正（-done 后缀 + 索引同步）
- **03:43** docs(tasks): T36 完成转正——索引链接同步 -done 文件名
- **03:48** feat(derive): 派生结果算子 CPU 参考实现（T31 第一批）
- **03:56** docs(tasks): T31 完成转正 + 触发 T21 评审（M6 记录：T31 批次）
- **04:03** feat(cli): T32 无头 CLI——kairos-cli crate（project/mesh/solve/results/pipeline）
- **04:15** feat(mesh): T30 Gmsh 引擎正式集成
- **04:19** feat(release): T33 updater 插件接入 + CSP 收窄
- **04:32** docs(reviews): T29 真机 E2E 报告——环境链路全通，阻塞于 bundle SIGILL（转用户）+ M6 评审触发（T32/T30/T33/T29 批次）
- **09:07** feat(vm): bundle 部署自动安装 VM 内 OpenMPI + 国内 apt 镜像切换
- **14:11** docs: 补完整架构与完成度清单 architecture-status.md（分支勾选 + moldingFoam 特殊标注）
- **14:17** refactor(core): vector_magnitude CPU 参考实现归位 kairos-core
- **14:25** refactor(ui): card 工厂参数化，收敛「状态行 + 刷新按钮」面板样板
- **14:31** feat(ui): KaTeX 公式渲染组件 + 去卡片阴影
- **15:56** docs(ui): ui.html v2 设计稿——参考 Moldflow Insight 优化
- **15:57** docs(ui): 设计稿去除第三方商标字样，改为中性描述（业界商业注塑 CAE 布局）
- **16:06** feat(ui): 分析阶段选项卡落地（v2 设计稿核心结构）
- **16:09** docs: 修正注释漂移——jobs VM 分支 tar 复制、derive GPU 版本已落地
- **16:12** refactor(ui): 分析阶段选项卡上移顶栏，与品牌同行（整理优化）
- **16:24** fix(ui): 色标图例数值统一 replaceChildren 填充
- **16:26** fix(ui): 移除 main.ts 残留的独立阶段选项卡条（顶栏已内嵌一份）
- **16:40** fix(gpu): run_scalar_pipeline 多余闭括号导致解析错误
- **16:41** docs(reviews): M6 评审收尾——八项清单全过
- **16:44** refactor(ui): stage-tabs 嵌入顶栏后去掉独立边框/背景
- **19:16** feat(ui): Vue 3 基础设施（T37 Phase 1）
- **19:25** feat(ui): Vue 3 迁移启动——App.vue + Card.vue（T37 起步）
- **19:28** docs: index.html 补 description / theme-color meta + Vue 挂载点注释
- **19:31** refactor: 全仓注释规范化——去任务编号/真机叙事/对标引用
- **19:32** refactor(ui): 去除残余注释中的对标引用与设计稿标注
- **19:59** feat(ui): Vue 3 核心组件 SFC（T37 Phase 2）
- **20:04** feat(state): 状态层换 Vue reactive（T37 Phase 3）
- **20:44** feat(ui): 面板全量迁移 Vue SFC，应用切换 Vue 挂载（T37 Phase 4）
- **20:47** chore: 清理旧状态容器与基准（T37 Phase 5）
- **20:54** bench(web): 状态层基准按 Vue reactive 重建
- **20:57** style(ui): 任意值类改写 canonical spacing 刻度
- **21:02** lint(ui): eslint 覆盖 Vue SFC，ui 基础件统一 Ui 前缀
- **21:06** refactor(ui): LatexBlock 去除 v-html，改用 katex.render 构建 DOM
- **21:15** refactor(web): 目录分层对齐标准 Vue 工程结构（T38 Phase A）
- **21:32** refactor(state): 状态层迁移 Pinia defineStore（T38 Phase B）
- **21:50** refactor(views): 面板逻辑全量抽 composable，.vue 只写 UI（T38 Phase C）
- **21:53** docs(web): 新增前端规范 web-conventions.md
- **22:09** style(web): 注释规范化清扫
- **22:14** build: 依赖与产物体积优化
- **22:19** docs: README 与架构文档对齐 T38 新前端结构
- **22:27** refactor(ui): ShellIcon 改自绘原创图标并迁入 ui/icons/（T38 后续）
- **22:31** fix(ui): ShellIcon 提示符与下边框间距优化
- **22:37** docs: 架构图表改用 mermaid 提升可读性
- **22:42** docs+ui: 图标设计规范与 ShellIcon 比例修正
- **22:44** docs: AGENTS.md 约定 md 图表优先使用 mermaid
- **22:45** docs: 图标设计语言英文用词修正 Hyper-modern → Ultramodern

## 2026-09-11

- **00:07** test: 覆盖率口径扩至逻辑层全量，前端测试 96 → 336（四维 100%）
- **00:07** docs: 覆盖率门禁口径描述同步（utils + stores + composables + use*.ts）
- **00:13** fix(ui): 材料面板「用于当前研究」与删除动作的两处 UX 缺陷
- **00:36** fix(ui): 材料面板详情区在窄栏下不可见
- **00:40** fix(tauri): 主窗口固定不可拖拽缩放
- **01:00** test(core): 补齐覆盖率缺口并修复 write_stl_binary 记录宽度 bug
- **01:49** feat(ui): v2 顶栏三行骨架——菜单栏与阶段选项卡独立成行
- **01:56** feat(ui): 阶段功能工具条 StageRibbon
- **02:05** feat(ui): 左列对齐 v2——层管理面板 + 工程树徽标 + 方案任务三态清单
- **02:09** feat(ui): 视口 v2 对齐——悬浮视图工具条 + 标题 + 坐标读数
- **02:14** feat(ui): 布局收尾 v2——状态栏三段式 + 三列可折叠 + 面板归位
- **07:48** refactor(ui+tauri): 菜单迁回系统栏，窗口内不再重复渲染
- **07:55** fix(ui): 命令面板快捷键按平台区分
- **08:07** feat(tauri): 原生菜单系统预定义项中文化
- **08:35** feat(tauri+ui): 标题栏按平台分化——Win/Linux 自绘 + macOS Overlay
- **08:43** refactor(ui): 外壳适配层收敛平台差异，标题栏不再写平台分支
- **08:50** refactor(ui): 快捷键收敛到 utils/shortcuts 注册表，使用方只拿定义调用
- **08:57** refactor(ui): 组合根类型化与动作 id 编译期约束
- **09:04** fix(ui+tauri): 标题栏三端 review 收尾
- **09:26** test+refactor: 视口叠加层下沉 render/overlays + 菜单动作契约测试
- **11:51** feat(tauri+ui): 窗口装饰改由 tauri-plugin-decoration 运行时管理
- **11:54** chore(tauri): 删除 tauri.macos.conf.json 平台覆盖文件
- **12:03** fix(tauri+ui): macOS 不走插件激活，Overlay 标题栏由配置原生生效
- **12:07** fix(tauri): jobs.rs 平台条件编译的未使用告警
- **12:12** chore: 注释与引用清扫（comment-style 全仓执行收尾）
- **12:22** refactor(core): 移除工程文件 schema 迁移链
- **12:27** docs: architecture-status 刷新——B1 前处理标注当前阶段完成
- **12:55** feat(core+ui): 牌号数据集扩充与纤维/填料参数组（B1-C3/C2 前处理收尾项）
- **13:49** feat(core+ui): 几何修复工具（孔洞填充 / 法向一致化 / 自交检测）
- **14:13** feat(core+ui): STEP 镶嵌网格导入 + 几何修复工具
- **14:32** fix(ui): 禁用 macOS 触控板双指滚动的整页弹性晃动
- **14:40** fix(ui): 标题栏 logo 改内联 LogoIcon，消除 Windows CI 资源加载错误
- **14:47** chore(data): 内置材料库升级为公开文献典型值参考库
- **14:54** chore(data): 内置参考牌号逐族参数量级校准
- **14:58** docs(readme): 内置材料数据来源如实标注
- **15:32** refactor(core+ui): 结果派生算子下沉 Rust，消除 TS 重复实现
- **21:28** docs(arch): 完成度状态化与后处理 GPU 必需口径贯通，新增 T39 渲染后端 POC 任务
- **23:23** test(gate): verify 门禁接入两端覆盖率，kairos-core 行覆盖冲上 100%

## 2026-09-12

- **00:46** feat(core+ui): B1-C1 IGES 镶嵌网格导入（T40）——实体 106 Copious Data forms 0/1/2/11/12 封闭环扇形三角化 + 实体 63 Compact Plane Subfigure + 实体 124 变换矩阵定位；Hollerith 整体吞除、开放折线/点列/B-rep 跳过计数、无可导入几何明确报错；import_iges 命令与 STL/STEP 共用会话登记，前端按扩展名分发（igs/iges）并入对话框过滤器
- **01:08** feat(core+ui): B1-C2 双域网格（T41）——表面三角形质心 ±法向射线厚度配对（均匀格 3D DDA 加速 + 暴力求交一致性测试）+ 流道/浇口梁单元端点捕捉耦合（BeamCoupling，自由节点按焊接容差去重）+ DualDomainReport 契约锁定 + generate_dual_domain_mesh 命令 + 几何面板「双域网格」动作与报告行（活跃方案杆系透传，修复时作废）
- **01:20** feat(core+ui): B1-C2 中面网格（T42）——顶点配对法中面抽取（相邻面内法向多射线取近、中面节点=顶点与命中点中点、单元继承表面连接、未配对顶点丢弃单元并计数）+ 杆系梁端点捕捉中面节点与自由结点复用（复用 T41 均匀格射线加速，提为 pub(crate)）+ MidplaneReport 契约锁定 + generate_midplane_mesh 命令 + 几何面板「中面网格」动作与报告行
- **01:46** feat(core+ui): B1-C2 局部加密/边界层（T43）——体素引擎支持 MeshRefinement 分级加密：边界层（包围盒面几何比率聚集）与区域盒（相交轴区间逐级对半细分），非均匀坐标贯穿节点表/行扫描/占位填充；保形依据为奇偶对角分解与单元尺寸无关（否决四面体二分级联方案的面片对角冲突）；MeshRefinement 契约锁定 + generate_volume_mesh 可选参数 + 几何面板「边界层数」输入透传
- **01:49** docs(arch): B1 前处理收官校准——状态截至 2026-09-12，C1/C2 全项 [x]，里程碑对照 B1 ~75% → ~95%（余缺口：双域/中面的求解消费、精确 B-rep 导入）
- **01:57** refactor(core+ui): B1 新增模块整体 review——删除 IGES 只有测试消费的 ImportStats/parse_iges_with_stats 死 API（与 STEP 解析器口径对齐）；DualDomainBeam 更名 ShellBeam（双域/中面共用，原名误导）；提取 dualdomain::weld_surface 消除双域/中面重复的焊接+退化剔除闭包；中面自由节点索引算术拆直；MeshSession 文档补中面缓存与存储理由；面板测试改按 placeholder 定位输入框
- **02:14** feat(core+ui): B3-C7 派生算子产品管线补全（T44）——DeriveRequest 可辨识 DTO（normalize/threshold/linear/difference）+ derive_scalar_field 改造与 derive_difference（长度校验、命名、完整性取与）+ ResultSession 双槽（主场/对比场）+ load_result_field slot 参数 + derive_difference 命令 + 结果面板线性参数输入/槽位选择/差值按钮；契约锁定 kind 标签；T21 八项清单评审入 ai-docs/reviews/T44-review.md
- **02:23** feat(ui): B3-C7 视口空间拾取与探针场关联（T45）——render/picking 纯函数（轨道相机指针射线 + Möller–Trumbore 逐面求交，拾取 faceCells 单元索引与场值同域）+ renderer.getCamera 快照（FOV_Y 常量提取）+ 视口 pointerdown/up 位移阈值点击判定 → 探针自动入列 → XY 图表数值随场刷新；T21 八项清单评审入 ai-docs/reviews/T45-review.md
- **02:38** feat(ui): B3-C7 探针时间曲线与时间轴联动（T46）——results store 增 probeTimeSeries/loadProbeTimeSeries（遍历目录时间步采样各探针值、越界回退 0、结束恢复原时间步、移除探针同步过滤）+ XY 图表「空间分布/探针时间曲线」模式与「跳转到时间步」选择器（图表→视口经既有 loadedField watch 联动）；T21 八项清单评审入 ai-docs/reviews/T46-review.md
- **02:44** feat(ui): B3-C7 视口完整剖切（T47）——剖切条件泛化为 dot(p,normal)>offset 任意轴平面（法向 ±单位轴 + 偏移）+ setClipPlane/getMeshBounds + math.clipPlaneFromFraction 纯函数（分数映射/反向/夹取，单测覆盖）+ 视口控制条轴选择/位置滑块/反向开关；T21 八项清单评审入 ai-docs/reviews/T47-review.md
- **02:59** feat(ui): B3-C7 渲染后端决策落地（T39）——自研 WebGPU 后端 POC（render/webgpu：最小 d.ts + WGSL 云图/剖切/线段 + 渲染器，与 WebGL2 同一 RenderMesh/场值/剖切语义）+ render/backend 后端工厂（真实适配器探测优先 WebGPU、回退 WebGL2，面板经 ViewportBackend 接口无感）+ 确定性 1M 三角形资产生成器（单测锁确定性）与基准入口（上传/首帧/中位与 P05 FPS/剖切延迟）；VTK.js 对照 POC 取消（自研已满足前提）；三端真机验收回流 T48；评审入 ai-docs/reviews/T39-review.md
- **03:00** docs(arch): B3 后处理阶段校准——C7 剩余缺口收敛为体渲染/多视口/大结果数据链与三端真机数字（T48），里程碑对照 B3 ~20% → ~50%
- **03:10** feat(ui): B3-C7 多视口联动（T49）——useViewportPanel 重构为多实例槽位（单视口/四分格布局，共享网格缓存、控制条广播、拾取每实例可用）+ 双后端轨道相机 getOrbit/setOrbit 与交互事件（ViewState 扩展 yaw/pitch/distance）+ 任一实例交互联动其余实例相机 + 时间轴经全局 loadedField 按构造同步；viewport store 增 layout 状态（单测）；T21 八项清单评审入 ai-docs/reviews/T49-review.md
- **03:23** feat(core+ui): B3-C7 三维体渲染 POC（T50）——core 体数据重采样（四面体包围盒栅格化 + 重心坐标判定 → VolumeFieldGrid 结构化体素场，covered_ratio 诊断，5 单测）+ render/webgpu 光线步进管线（96 步前向合成、三线性采样、冷热迁移函数、轨道相机）+ 128³ 确定性球体密度基准入口；T21 八项清单评审入 ai-docs/reviews/T50-review.md
- **03:53** feat(core+ui): B3-C7 大结果数据链第一步（T51）——field_binary 编解码（magic+meta JSON+f64 LE 值区，魔数/截断/数量校验）+ FieldCache FIFO 有界缓存（容量 1..=64，同键覆盖去重）+ load_result_field 缓存命中跳过磁盘 + load_result_field_binary 命令（tauri ipc Response 原始字节通道）+ 前端 decodeFieldBinary 解析器（DataView 小端）与全套单测；T21 八项清单评审入 ai-docs/reviews/T51-review.md
- **03:54** docs(arch): B3 状态终校准——多视口联动/剖面切片 [x]（T49/T47）、三维体渲染 △ POC（T50）、大结果二进制通道+缓存淘汰（T51）；剩余：迁移函数 UI、懒加载/持久关联、三端真机数字（T48）；B3 ~70%
- **05:22** chore(build): Dev 构建优化——依赖 profile 降为行表调试信息 + O1（app crate 全量重编译 20.4s → 17.7s，链接输入变小；wgpu/naga 在 dev 下运行不再 O0 卡顿）；工作区 crate 保持 debug=2，llvm-cov 行覆盖不受影响。future-incompat 警告（wgpu-hal→metal→block 0.1.6）保留不屏蔽，wgpu 迁移 objc2 后自然消失
- **05:36** feat(ui): C8 分析报告内容增强（T52）——HTML 报告新增几何摘要（三角形/尺寸/网格健康+体积网格质量行）、探针数值表（越界占位）、探针时间序列逐采样表三节（可选字段无数据自动省略，全量转义）；util/panel 各 2 新测试；T21 八项清单评审入 ai-docs/reviews/T52-review.md
- **05:47** feat(ui): C8 模板化自定义报告（T53）——buildReportHtml 增 ReportOptions（自定义标题 / 备注转义渲染 / 六分区开关，缺省全开兼容既有调用）+ 报告面板模板区（标题输入、备注文本域、分区勾选）透传生成；util/panel 各 2 新测试（含标题与备注转义注入用例）；T21 八项清单评审入 ai-docs/reviews/T53-review.md
- **05:48** docs(arch): C8 报告生成收官——富文本/HTML 分析报告（T52+T53）与模板化自定义报告转 [x]；B3 ~75%
- **07:19** fix(mesh): Gmsh 目标尺寸透传(-clmax)+修复报告真正返回(RepairOutcome DTO);gmsh 子进程编排下沉 core 供 CLI/桌面共用
- **07:26** fix(jobs/downloads): multipass tar 复制移入作业线程不再冻结 UI;downloads 网络解析移入 spawn_blocking
- **07:34** feat(gpu): 派生命令接入 wgpu compute 生产路径;device 进程级缓存;管线尾段合并
- **07:41** refactor(contract): 8 个命令统一 Result 契约;渲染网格提取下沉 core
- **07:53** refactor(web): view→api 直连下沉 store;withBusy 统一动作包装;错误 code 契约生效
- **08:04** perf(render): WebGL2 渲染专项——fitToMesh 逐轴/uniform 缓存/矩阵复用/GL 资源释放/法线统一/动画背压
- **08:09** refactor(web): store 内 DOM 下载外移 utils/download;materials 自定义 id 防撞
- **08:18** docs(arch): 整体评审文档整理——architecture-status 立「完成度只在里程碑表维护」单一事实源条款并修正 B3 ~20% → ~75% 实锤矛盾（头部状态补 T44–T53+评审修复批次、C7 派生算子条目改 wgpu compute 主路径口径）；README 阶段句从「框架搭建」更新为能力补全阶段（补 VM 执行层/STEP/IGES/双域中面/WebGPU/GPU 派生/模板化报告）+ OpenFOAM 版本口径修正；tasks/README 补 T51/T52/T53 索引并去重 T45–T47 重复行；AGENTS.md 契约测试路径纠偏（tests/rust/contract/main.rs）；timeline 整体重建——从 git log 生成一行一 commit 完整台账（280 条，真实时间，消除日期断裂/时间漂移/重复行/56 条漏记）；整体评审发现与闭环记录入 ai-docs/reviews/R1-full-project-review.md
- **08:41** refactor(web): 消融测试前置加固——fitToMesh 的适配计算抽纯函数 fitCameraToBounds 入 render/math.ts（逐轴中点回归测试 + 退化包围盒 + 斜法向偏移 3 用例，消融可验证）；quickselect 从 useViewportPanel（覆盖率口径外的最大 composable）迁入 utils/stats.ts 并补 6 用例（对拍全量排序中位数 + 10⁶ 冒烟），口径外逻辑继续收窄；视口面板 min/max 复用既有 minMax
- **08:53** test(quality): 消融测试——修复批次逐点消融矩阵 12 项（临时还原为修复前行为跑定向测试再回滚）：11 项被测试套件当场捕获，1 项（动画背压，口径外）接受并记录；GPU/CPU 消融计时规模扫描（1e5~~3e7 值）暴露真 bug：>4.19M 值 dispatch 超过 Metal/Vulkan 每维 65535 workgroup 硬限直接 panic（100MB 级结果必触发），标量/矢量管线改切块 dispatch + 超限回归测试；量化 device 缓存贡献（冷 32ms vs 热 2.2ms）；简单派生算子全尺寸段 GPU 慢于 CPU（0.2~~0.4×，上传/回读支配）如实入 perf-budget 基线，≥5× 预算适用边界注明；新增常驻 #[ignore] 计时入口 gpu_derive_ablation；前置加固（fitCameraToBounds/quickselect 纯函数化）使 fitToMesh 原始 bug 算法可被回归测试当场捕获
- **12:38** refactor(web): 消融缺口修复——时间步动画节拍器抽为 utils/animation.ts（背压/循环回绕/停止与重启归零，假计时器 3 用例，闭 A12「背压无测试保护」缺口，useViewportPanel 与渲染器解耦）；新增 utils/storage.ts 统一 localStorage 网关（kairos:<域>:<名> 前缀 + JSON 值 + 损坏回退 + storageIndex 键控集合管理），useLayout/useTheme/UiCard/useProcessPanel 四处迁移，工艺预设去 localStorage.length 遍历扫描改索引 key；相关测试断言同步（key 形态与 JSON 值）
- **12:56** test(quality): 全项目消融第二阶段——23 个消融点覆盖 core 领域服务(gmsh/repair/jobs/双域/openfoam/FieldCache/derive/material/project/process/render_mesh/geometry)、命令层(GPU 切块/差值长度校验)与前端(field-binary/report 转义/shortcuts/pipeline/project/animation/storage/picking/mesh-asset):18 点当场捕获,5 缺口(容差焊接/分带方向/CSV 表头/updatedMs 盖章/资产布局)全部加固测试并复验捕获,捕获率 78%→100%;三个弱断言教训(contains 误满足/≥ 连不盖章都过/只验一致性不验正确性)与 Float32 精度边界入报告;verify 全绿(前端 516/core 257)
- **13:27** fix(web+tauri): 整体评审遗留问题清账——①P0:useViewportPanel 纳入覆盖率口径时发现 T49 重构丢失 slot.renderMesh 赋值,云图/剖切/空间拾取三功能自 421674a 静默失效,修复并以 18 个后端 mock 行为测试锁定(面板从唯一 coverage exclude 收编为 100% 四维);顺带修复 attachCanvas 与 setupSlot 并发触发渲染器重复创建的竞态(在途创建按实例去重)与 pointerup 未按下分支;②GPU 测试适配器探测门控(无 GPU 环境显式跳过并提示,有 GPU 全量验证),D0「GPU 必需但 CI 可验证」策略落地转 [x];③materials.assignMaterial 改走 touchActiveStudy(updatedMs 盖章单一来源),results store 3 处 busy 样板迁移 withBusy;④menu-actions IPC 载荷先验证类型再查注册表
- **13:33** docs(arch): 修正 D0「覆盖率门禁 + GPU CI 策略」项 △→[x]——上一 commit(18d0b9d)的脚本在第二条断言失败时把本条编辑一并回滚,仅里程碑表落盘,commit 信息却声称已改;现补上该项勾选(表述:GPU 测试适配器探测门控,无 GPU 显式跳过、有 GPU 全量验证)
- **13:37** docs(review): 消融抽查固化为 T21 常设项——清单第 5 项「测试覆盖缺口」增补:每轮评审必抽 3~5 个关键纯函数/修复点做消融(临时破坏→定向测试须变红→还原,还原用文件级暂存),优先抽新写的防御分支与回退逻辑;交付物与验收标准同步增补消融记录要求;R1 消融报告标注建议已采纳
- **14:04** feat(ui): 对齐 Moldflow 方案任务窗格范式——左列新增常驻「方案任务」窗格（utils/study-tasks.ts 纯函数评估 + StudyTasksPanel）：任务按 Moldflow 执行顺序排列（导入几何→网格划分→[修复网格·条件任务]→选择材料→成型工艺设置→分析→结果分析），六态状态图标（✓ 成功/! 警告·网格健康告警/✕ 失败/⧖ 排队/⟳ 执行中/空 未开始）+ 上游失败阻断后续（结果任务 blocked 带原因，对齐 Moldflow「上一任务失败则不运行后续」）；双击任务直达对应工作台阶段（对齐 Moldflow 双击打开编辑器）；窗格底部吸收流水线面板的分析序列选择与提交表单；工程树方案层可点击切换活跃研究（对齐 Moldflow 工程视图）；删除被替代的 PipelinePanel/utils/pipeline；新增 util 10 测试 + 面板 4 测试
- **14:22** fix(ci): 修复 windows-latest 覆盖率门禁——上一批 gmsh 假脚本测试为 cfg(unix)，Windows 下 tetrahedralize 成功/非零退出/无输出三条路径 10 行无覆盖致行覆盖 99.85%<100%；补 cfg(windows) 等价 .cmd 批处理假 gmsh（夹具路径嵌入批处理、取末参为输出路径），与 Unix 版逐路径镜像
- **14:41** refactor(web): 遵循 web-conventions 与 T21 消融抽查——①修正本次 UI 批次的两处规范偏差:工程树方案切换改走 project store 新增的 selectStudy action(消除 composable 直接改写他域 store 状态的反模式),pipeline store 的 submitPipeline busy 编排迁移 app.withBusy(上轮推广遗漏,闭包收窄以 const 捕获处理);②按 T21 第 5 项对新纯函数 utils/study-tasks.ts 执行消融抽查 5 点(状态优先级/失败阻断/前置排除分析/网格警告态/修复任务条件门):4 点当场捕获,1 点缺口(单一状态夹具区分不了混合队列优先级)当轮加固混合夹具测试并复验变红,5/5 可捕获;抽查全程文件级暂存还原,工作树逐轮核对干净
- **15:53** feat(ui): Moldflow 对齐第二批——①阶段选项卡任务状态角标:stageBadges 纯函数把方案任务中需立即注意的状态投射到对应 tab(几何←健康告警 !,求解←作业 ⟳/⧖/✕),完成/待办不产生角标;共享任务快照上提 composables/useStudyTasksSnapshot(选项卡与任务窗格共用);②右列新增常驻「方案摘要」卡(材料牌号家族供应商/工艺六参数/最新作业状态与物理时间,空态给下一步指引),对齐 ui.html 设计稿右列;③任务右键菜单(对齐 Moldflow 右键常用命令):打开编辑恒有,分析运行/排队可取消作业,结果已扫描可重扫目录;Esc/点击外部关闭,视口边缘夹取。新增 21 个测试,verify 全绿
- **16:46** feat(deps+vm): T54 求解环境"更新未部署"提醒——vm_deploy_bundle 部署成功后把 releaseTag 写入 VM 标记文件 ~/moldingfoam-env/.kairos-release-tag;新命令 vm_deployed_release_tag 读取标记(非 multipass/VM 未启动 → null);纯函数 isPendingDeploy 比对下载侧与 VM 侧标签;依赖面板与求解作业面板在"已下载新版本但 VM 未部署"时显示琥珀横幅(含版本从→到),部署成功后自动消失;types DownloadedEntry 补 releaseTag 镜像;新增 parse_deployed_tag/isPendingDeploy/deploy 测试与两面板横幅测试,verify 全绿
- **16:50** docs(tasks): Moldflow 使用流程对照评审回流建档 T55–T60——T55 浇口位置分析(P1,简化启发式/求解侧两方案,无需预设浇口)、T56 网格纵横比+双域匹配率(P2 立即做,匹配率数据已有)、T57 网格预估单元数(P2 立即做,体素精确对拍)、T58 视口拾取放浇口+节点吸附(P2)、T59 填充预览(P3 依赖 T55)、T60 导入日志流+PPT 报告(P4);均含目标/范围/验收标准/当前实现边界
- **16:57** docs(arch): 确立「Rust 优先」逻辑归属原则——ARCHITECTURE 铁律 3「计算住 core,前端以 UI 为主」+ §1.1 判断口诀(数值计算/数据量大/需与求解一致或复用 CLI 任一为真即住 core;禁止 TS 双实现;重计算不进前端,大数组走二进制通道);web-conventions 判断标准前置第 0 问 + 反模式增补(TS 重写 core 计算/前端跑重计算);AGENTS 速查同步;配套例证:派生算子下沉、render_mesh 下沉、T56 纵横比按 core 统计设计
- **17:04** docs(tasks): 任务索引重构对齐实际阶段——「已实现任务」表头 M0–M5 更新为 M0–M6 里程碑主线;「后续增量实现」更名「增量阶段已实现」并收编 T39(实现已落地,决策门关闭,真机数字归 T48);T37/T38/T39 任务文件补 -done 后缀并全库更新链接;T55–T60 从增量表移至待开工表(补依赖列);待开工注脚更新(数值侧已闭环,性能验收归 T48,新增缺口见 T55–T60)
- **18:28** fix(case): T29 真机实测修复 case 生成两处缺陷——①保压压力表重复横坐标：molding_dict 固定写 (0 1e5) 大气压首点后再追加保压曲线，曲线起点为 t=0 时表出现重复横坐标，foamRun 以 Function1s::Table::check out-of-order 拒绝启动；契约要求 packing.switchPressure 与保压曲线起点一致以消除压力阶跃，改为写出 switchPressure 并去掉大气压首点（空曲线兜底单点表）。②polyMesh 三数组错位：write_poly_mesh 只把 faces/owner 重排为内部面在前、neighbour 未同步重排，owner 与面逐项错位，checkMesh 报 1968/5000 负体积单元、求解第一步即 NaN；改为三列表统一重排（内部面→inlet→vent→walls），内部面按 (owner,neighbour) 升序满足 upper-triangular 约定，面顶点按最小下标循环，vent patch 类型对齐契约 case 的 patch。新增写出一致性回归测试（每单元 4 面且体积为正），verify 全绿
- **18:31** fix(jobs): T29 真机实测修复 VM 求解链路四处缺陷 + 收尾判定——①并行调用：foamRun 不自行调 mpirun，直接 `foamRun -parallel` 以 "attempt to run parallel on 1 processor" 退出，统一走 core 的 solve_command（`decomposePar -force && mpirun -np <cores> foamRun -parallel`），-np 与 decomposeParDict 的 numberOfSubdomains 一致。②结果重建：并行结果写在 processor*/ 下，case 级时间目录需 reconstructPar（用 `;` 串接，求解器异常退出时也尽力重建部分结果）。③VM 内路径：作业脚本 cd 的是宿主绝对路径（VM 内不存在，此前必然失败），copy_case_into_vm 改为返回 /home/ubuntu/<name> 并据此执行，重跑前先删 VM 内同名目录防旧结果混入。④结果回传：结果落在 VM 原生文件系统，新增 copy_results_from_vm（tar 管道回传；时间目录由 core 的 results::time_dir_names 按同一套判定筛查），回传失败按作业失败上报。另加收尾判定 solver_signal：上游 bundle 存在退出期堆破坏（argList 析构中 malloc_consolidate 报错，moldingFoam 自带 case-contract 同样复现，已移交求解器侧），跑完整段求解仍以非零码退出——「无 FOAM FATAL/exiting/aborting 标记 + 见过 End」判定为该崩溃而非求解失败，结果仍按完成处理并在日志说明。环境路径（ENV_ROOT/ENV_BASHRC + env_source_command/env_probe_command）收敛到 core services/vm.rs 单点。verify 全绿
- **18:34** refactor(naming): 求解链路命名统一到 moldingFoam 口径 + 文档同步——模块 services/openfoam.rs → moldingfoam.rs（SOLVER_MODULE 常量保留为求解入口单点）；Tauri 命令 probe_openfoam/generate_openfoam_case → probe_moldingfoam/generate_moldingfoam_case；DTO EnvironmentCheck.openfoam → moldingfoam（Rust 结构体、src-web/types 镜像、前端读取处与四个测试文件同步）；前端 api/store 函数名与用户文案改「求解环境」口径（探测提示：「未检测到求解环境（moldingFoam bundle，基于 OpenFOAM 14）。可在依赖面板下载官方预编译包。」）。刻意保留：致谢署名、结果面板「OpenFOAM case 目录」（case 格式确为 OpenFOAM 的）、GPL 合规决策文件名。文档：T34 重写为完成态（命名口径/交付四项/fork 对接清单/T29 回填的三则实测修正/遗留 bundle 退出期堆破坏），任务索引 T29 注记更新为「v0.2.0 已修 SIGILL，剩余阻塞是退出期堆破坏」，运行时分发决策文档补现状注记（v1 引导安装已被 bundle 通道取代），dependencies 模块注释同步（应用内编译流程早已退休）。verify 全绿
- **19:05** refactor(jobs): 退出期崩溃兜底收敛为 SolverOutcome——原先在作业层用两个布尔量（abort/completion 标记）拼 4 项条件判定，改为 core 的 SolverOutcome 累积器（一次 Aborted 即定案、Completed 只表示见过 End，因 decomposePar/reconstructPar 也会打印 End）+ completed_cleanly() 谓词，作业侧判定收敛为一条表达式；顺带把根因写进 T34 遗留：真实 bundle 上 A/B 定位到「同一模块被打包成两份独立 .so，运行期两份都加载」，改符号链接后完整链路 exit 0、无 Duplicate 警告与堆报告。verify 全绿
- **19:24** fix(jobs): 撤除退出期崩溃兜底(上游 v0.2.1 已修)——bundle v0.2.1 把 libmoldingFoamSolver.so 改为指向 libmoldingFoam.so 的符号链接，VM 内复跑零步与完整链路均 exit 0、无 malloc_consolidate、无 Duplicate entry，兜底失去存在理由且带真实风险（decomposePar 也打印 End，求解器中途段错误会被误判成功）。改回严格判定：退出码非零即失败，仅用输出里的 FOAM FATAL/exiting/aborting 标记区分失败原因（求解器报错退出 vs 进程被终止/崩溃）。core 侧 SolverSignal/SolverOutcome 收敛为单一 is_abort_line 谓词。verify 全绿
- **19:34** docs(review): T29 真机复跑报告成文（bundle v0.2.1）——环境链路全绿：依赖面板下载 v0.2.1 arm64 资产 → vm_deploy_bundle 部署（含 OpenMPI 与版本标记）→ 生成 case（5000 四面体）→ checkMesh "Mesh OK" → decomposePar + mpirun -np 4 foamRun -parallel 跑到 endTime 2 s（无 NaN、无堆破坏、无 Duplicate 警告、exit 0）→ reconstructPar 出 21 个时间目录 → tar 回传宿主 → kairos-cli results list/dump 四场可读；报告含 t=2 场统计（alpha 均值 0.751、T 288–503 K、p_rgh 15.6 kPa–0.43 MPa、|U| 均值 10.6 m/s）与分层填充剖面、6 项 Kairos 侧缺陷修复表、上游 SIGILL 与打包重复库两项（均已由 moldingFoam 修复）、遗留（前端截图待权限、真实 STL 待素材、样例 case 填充分数与 endTime 拉长后 t≈2.05 s 发散的现象已移交求解器侧）；T29 任务文档阶段说明同步。verify 全绿
- **19:52** feat(cli): pipeline run 增加 --target-size（默认 1.0 保持原行为）——真实零件按壁厚/算力调整体素尺寸；首轮真实 STL 验证（Thingiverse 控制器支架，46.7 万三角面、111×167×133 mm，3MF→STL 转换后导入）：5/4/3 mm 三档体积保真 99.8%/99.8%/99.3%（真实体积 880.8 cm³），checkMesh "Mesh OK"、零负体积单元，100 万面级导入 + 体素化 18–46 s；求解侧在真实件上于固定填充分数（约 1.4%）发散（Q 与 Q/4 下分别在 t=0.0134/0.0537 s，与 1/Q 严格成比例），发散前门控压力已 16 MPa——暴露入口分带把阶梯侧向面（占入口面 39%）当浇口、默认工艺（1 s 注射对 880 cm³）与多连通域（3 域，含 2 个 5 单元游离块）三项待办，详见 e2e 报告。verify 全绿
- **19:58** docs(review): e2e 报告补真实 STL 一轮——素材（Thingiverse 控制器支架 46.7 万面，CC BY-NC-SA，samples/ 已 gitignore）的导入与体素化三档数据（体积保真 99.8%、Mesh OK、零负体积单元）与求解发散现象（固定填充分数 ~1.4% 处触发、与 1/Q 成比例、崩前门控压力 16 MPa）；登记四项 Kairos 侧待办（入口分带含阶梯侧向面 39%、默认工艺不适配真实件尺寸、多连通域游离块、求解侧需更密写盘定位）。verify 全绿
- **20:22** docs(arch): B2 求解器状态对齐真机验证结果——C4「真机求解复跑」由阻塞（SIGILL）改为已完成（bundle v0.2.1：样例方盒全段跑通、checkMesh Mesh OK、结果可读，6 项 Kairos 侧缺陷已修、上游 2 项已修）；新增三项 Kairos 侧待办并给出实测依据：①浇口几何落地（case 的 inlet 仍是 z 分带启发式，未消费 T07 流道网络的 Gate 单元；真实阶梯边界下分带把 39% 侧向面当浇口，熔体从外壁注入）②工艺参数合理性校验（880 cm³ 真实件用默认 1 s 注射时填充 1% 门控压力已 5 MPa、3% 时 10² MPa 量级，压力 runaway → Courant 10⁷ → NaN，属参数不适配）③网格最小特征提示（5 mm 体素把底座压成单层单元，流动退化为薄板，润滑压降按 1/h² 放大）。verify 全绿
- **21:05** feat(T61): 浇口几何落地——case 的 inlet 由模具网络浇口决定（GatePortal：Gate 单元型腔端 + 直径/2，等效面积 πr² 不足时按最近面补足），分带回退加外法向过滤（阶梯侧向面归 walls）；适配层新增 runnerElements 入参、前端透传研究流道网络、CLI 新增 --gate x,y,z[,半径]；实测样例盒 inlet 240→200 面、带浇口 16 面，真实件 382→21 面，checkMesh 均 Mesh OK；真机回归发现样例盒 t≈0.5s 出现求解侧失稳（能量方程先 NaN），判别实验（旧 vent / maxAlphaCo 0.03 / endTime 6s / 改动前 case）确认该 case 本就在稳定性边界（同族配置在 0.46/0.49/0.64/2.05s 分别失稳），按约定记录移交 moldingFoam 侧。verify 全绿
- **22:05** feat(T62): 工艺参数合理性校验——core 新增 estimate_fill_load（Q = V/t、浇口表观剪切速率 4Q/(πR³)）与 fill_load_hints（流量超常规注塑机包络 500 cm³/s 或浇口剪切超 5×10⁴ 1/s 时给建议值，体积缺失/时间为 0/半径为 0 均不误报），经 check_process 可选入参（volume_mm3 / gate_radius_mm）接入工艺面板（面板校验时带活跃几何网格体积与首个 Gate 单元半径），CLI pipeline run 生成 case 后打印工况提示（JSON 含 loadHints）；实测 873 cm³ 真实件用 1 s 注射触发「873 cm³/s → 建议 ≥ 1.7 s」，样例方盒（1 cm³、1 s）静默；新增任务文档 T62 与索引条目。verify 全绿
- **22:20** docs(review): B2 求解器（Kairos 侧）评审第 1 轮（T61/T62）——八项清单逐项结论 + 消融抽查 6 点（gate_portals 端点/外法向过滤/面积补足/流量阈值/剪切公式/浇口优先于分带，全部被测试当场捕获）；立即修 2 项：集成用例补「入口等效面积 ≥ πr²」断言（消融暴露的缺口，已复验先红后绿）、T21 清单新增第 9 项「外部产物信号必须跟进到根因」（固化 M6 接受项 Duplicate entry 的教训——它实为 bundle 双份 .so 的表征）；接受 3 项（阈值量级口径、不估填充压力、case 生成性能不在 T01 覆盖）并写明重评条件。verify 全绿
- **22:35** feat(T63): 网格最小特征提示——新增 core services/thickness.rs：三角面沿内向法向做射线壁厚探测（复用双域管线 TriangleGrid + weld_surface；新增背面命中过滤排除擦边/共面伪命中，真实件 p05 由 0.08 → 0.38 mm；内向失败再外向兜底以兼容朝向不一致的网格），判据用「薄于 2×目标尺寸的表面占比 ≥ 5%」（真实装配件厚度双峰分布，占比法对离群点稳健）；MeshingReport 新增 thin_feature_hints（Rust↔TS 镜像 + 契约测试锁定），体素与 gmsh 两条网格命令填充、几何面板琥珀警告行渲染、CLI 输出网格提示与 thickness 统计（min/p05/median/samples）；实测 20×20×2 板在目标 1.5 mm 提示、1.0 mm 静默，真实件 3/5/8 mm 分别 42%/48%/51%，样例方盒（10 mm 立方体）无提示。新增任务文档 T63 与索引条目。verify 全绿
- **22:45** feat(T64): 翘曲结果场消费——位移场 D 纳入矢量场口径（services/results.rs::is_vector_field），按模量解析（is_magnitude）可直接在结果面板/视口以变形量云图查看；新增合成位移场单测；变形显示（视口按位移扭曲网格 + 倍数控制）另立 T65 并写明阻塞条件（M4 的 case 级位移场未落地、GUI 截图验收需桌面控制权限），architecture-status C6 相应拆分为「消费 ✓ / 变形可视化 待开工」。verify 全绿

## 2026-09-13

- **09:20** refactor(comments)+chore(gate): 注释规范全库执行与代码收敛——按 comment-style.md 清掉 49 行违例（任务编号 / 对标出处 / 过程叙事 / 日期戳 / 评审路径，含三处用户可见字符串：依赖面板 hint 的 T22、GPU 跳过提示的「真机验收归 T48」）；新增 scripts/comment-style.mts 门禁并接进 bun run verify（已做「注入违例 → exit 1 → 还原」验证）；代码收敛两项：write_poly_mesh 的边界分类抽成 classify_boundary_faces、thickness::hints_for 封装探测+提示；文档修正三处陈旧声明（T34 的兜底、T29 的真实 STL 状态、索引的 T29 注记）+ 代码里两处里程碑编号（M4/M2 口径）改为中性表述；报告入库 ai-docs/reviews/R2-code-comment-audit.md。verify 全绿
- **07:05** fix(T66): case 单位制与排气边界对齐契约——坐标写米（MM_TO_M；契约是「mm 顶点 + scale 0.001」，Kairos 直写 polyMesh 须自算）、注射流量改 SI（V_m³/注射时间）、排气口改 moldingVentVelocity / moldingVentPressure（CdA = 排气 patch 几何面积）+ moldingDict 增 ventSealAlpha 0.9 与 massBudget（间隔 200）、write_poly_mesh 返回 PatchAreas 供字典侧使用；依据上游 038 诊断：旧配置排气口为通用开放边界致密封逻辑不生效（入口流量 92% 逃逸、填充卡 0.75、V/P 切换永不触发）且 mm-as-m 让 10 mm 件在求解器里是 10 m；真机复跑（bundle v0.2.1）：checkMesh 包围盒 0→0.01 m 且 Mesh OK、求解 nan=0、V/P 切换 t≈1.16 s（填充 0.95）、t=2 s 填充 0.9731、整链 exit 0，与上游参考用例（0.9614 / 0.9786）一致。新增任务文档 T66 与索引条目。verify 全绿
- **08:10** feat(follow-up): 对齐上游 038 §6c/§7b 的两项 Kairos 侧改动 + 建档 T67——①工况校验接入浇口名义速度判据：输入口径由浇口半径改为流通面积（inlet_area_m2；CLI 用 case 里 inlet patch 实测面积），FillLoad 增 inlet_velocity_m_s = Q/A_in，>5 m/s 谨慎、>20 m/s 判不可行（与上游 fillVelocityWarn 一致）；②结果场类型改由 FoamFile 头的 class 行判定（FieldKind：标量/矢量模量/不支持），volSymmTensorField 等明确报「暂不支持」——不再按场名维护白名单，D/sigmaEq 这类新场自动适配；③新建 T67（冷却水路落地：模壁 1D 通道 BC + coolant 子字典，含模型补字段与制品域近似的待定设计）；④T65 补翘曲场契约（D/SI 米→显示 mm、sigma/sigmaEq、shrinkage 系），T62/T64 文档回填。verify 全绿
- **08:55** test(T66): v0.2.2 三组验收——样例盒（整面 inlet）exit 0/nan=0（V/P 切换 t≈1.16 s）；点浇口（--gate 5,5,0,1.5）与真实件（873 cm³、8 mm 浇口、4 s 注射）分别填充到 98.5%/95.9% 后，在**保压阶段**因压力阶跃（填充分数触发切换时闸口仅 5.4 MPa，保压曲线起点 60 MPa）把熔体推到跨声速而失稳，上游 postSolve 快速失败按设计报出（T=3000 K、p_rgh=nan、附处置建议）；对比旧配置真实件在填充 1.4% 即崩，T66 效果确认。CLI 增 --injection-time 供真实件按合理工艺复验；三个 case 与日志放 samples/cases/（本地）；e2e 报告与 T66 文档更新。verify 全绿
- **09:40** fix(ci): 修复 Linux/Windows CI 的 clippy 失败——jobs.rs 里 `results` 导入与 `vm_case` 参数只在 macOS 分支使用，非 macOS 平台触发 unused import/variable（-D warnings 即错误），两平台 CI 同点挂；改为按平台门控导入，并把路径选择抽成按 `os` 参数分派的纯函数 `script_case_dir(os, case_dir, vm_case)`（macOS 用 VM 暂存路径 / Windows 用 WSL 的 /mnt 形态 / 原生用宿主路径，含单引号转义），`to_wsl_path` 去掉 cfg 以便跨平台单测；新增两个单测覆盖三端取值与转义（在 Linux/Windows CI 上会一起编译）；T21 清单第 1 项补「cfg 分平台代码逐平台审计符号」的教训。verify 全绿
- **09:55** fix(ci): 修 Ubuntu 第二轮 clippy 失败（dead-code 传递性）——上一版把路径选择抽成 `script_case_dir` 后它只在 macOS/Windows 分支被调用，Linux 下与它引用的 `to_wsl_path` 一起被判 dead_code（dead-code 判定是传递的，只在 cfg 分支里出现的调用点救不了函数）；改为原生路径也调用 `script_case_dir(std::env::consts::OS, …)`（语义不变、顺带消掉重复的单引号转义），两函数在三端皆为活代码；T21 清单第 1 项补该教训（与上一轮的 cfg 符号审计合成一条）。verify 全绿（本地 macOS，Linux/Windows 待 CI 复核）
- **10:52** fix(T68): GUI 走查发现工作流断点——全应用没有创建方案的入口。`file.new` 建出的工程 `studies` 为空且不设活跃方案，工程面板方案层 `v-if="studies.length>0"` 之外没有任何新建控件（store 的 `addStudy` 只有单测在用），于是材料「用于当前研究」静默失败（状态栏「请先创建或选择一个研究。」）、提交求解作业恒禁用，模具网络面板还指引「在上方项目栏选择或创建一个研究」指向不存在的控件。修复：core `services::project::create` 建工程自带默认方案（`DEFAULT_STUDY_NAME`，GUI 与 CLI 一致）+ 前端 `syncActiveStudy` 装载后兜底落到首个方案（新建/打开两条路径）+ 工程面板加「＋ 新建方案」（默认名 `方案 N`）与空态提示 + 模具面板文案改为指向该入口；新增 core 单测、store 三态单测、组件四个用例（含未打开工程时方案层为空）。同时建档 T69（术语「方案 / 研究」混用收口）。verify 全绿
- **11:01** fix(T70): 材料公式刷屏可访问性树——按可访问性树驱动 GUI 走查时，材料库的 Cross-WLF / Tait 公式把 KaTeX 的 MathML 逐符号摊开（「η」「=」「0」各一个节点），两个公式约 255 个节点占满整窗 419 个节点的六成，参数表反被挤出列表；`LatexBlock` 增 `label` 属性并按 `role="img"` + `aria-label` 暴露（整段公式 = 一张有名字的图像），材料库两处传入公式名，组件测试补断言。真机复测：整窗节点 419 → 177，公式各占一个 `image` 节点。走查记录（含工艺面板等折叠线以下内容不可达的驱动限制）入 ai-docs/reviews/gui-walkthrough.md；新增任务 T70 与 D0 状态条目。verify 全绿
- **11:23** fix(T71): 面板折叠入口可访问性 + 长面板可折叠——GUI 走查定位到「折叠线以下面板不可达」的真正机制：可访问性桥对**每个容器按 DOM 顺序只暴露前 63 条**（`main` 报 showing 0-63 of 130 items），滚动容器内元素在 AX 上的 y 是内容坐标（滚下去后变负），因此**滚动无法**把第 64 条以后的面板带进树；模具网络一张卡片就占 ~32 条（15 个 `input type=number` 每个约 5 个 AX 节点），工艺设置正好被截在门外。改动：`UiCard` 可折叠时标题栏按 `role="button"` + `tabindex=0` + `aria-expanded` 暴露（回车/空格切换），模具网络与运行时依赖两张长卡片开 `collapsible`。真机复测（打包版 v0.1.0 debug bundle）：折叠模具网络后容器条目 130 → 91，**工艺设置面板（9 字段 + 校验并应用 + 预设行）与运行时依赖面板完整进入可访问性树**。走查报告升级为驱动侧四条件（打包身份 / 窗口在屏 / 前台激活 / 栅格坐标 = 整屏缩放到 1280 宽，比例 0.8707，动作前刷帧）的实测判据；新增任务 T71 与 D0 状态条目。verify 全绿
- **11:31** fix(ci): 修 Ubuntu 门禁的 gmsh 偶发失败（ETXTBSY）——`test:rust` 在 ubuntu-latest 上偶发 `gmsh 启动失败：Text file busy (os error 26)`（断言处报「gmsh 网格化失败：bad mesh」不匹配）。根因：用例把假 gmsh 脚本写进临时目录后立刻 exec，而 `Command::spawn` 在并行测试里由其它线程 fork 时会复制 fd 表，新进程短暂持有该脚本的写引用（CLOEXEC 要到 exec 才关），内核因此拒绝 exec（同进程里的 fork 来源只有 gmsh 自己的用例，已核对 `Command::new` 在 core 中唯一）。修法：加 `FAKE_GMSH_LOCK` 把「写脚本 → 起进程」串行（三个 Unix 假 gmsh 用例持锁），从构造上消除竞态。T21 清单第 1 项补该平台差异教训。本地 macOS 不复现（不执行 ETXTBSY 判定），待 Ubuntu CI 复核；verify 全绿（core 行覆盖仍 100%）
- **12:22** test(v0.2.3): moldingFoam v0.2.3 复测（B 通过 / C 仍失败）——先把 v0.2.3 arm64 bundle 拉下来（本地那份其实是 v0.2.2：两版资产**同名**、面板只显示文件名 → 误判「已下到最新」，建档 T73；下载本身被 GitHub 掐断两次，最终走镜像拿到并核了大小+xz -t）并干净部署进 multipass `kairos`（删旧环境树、解压、写 `.kairos-release-tag=v0.2.3`）。复跑原 case：**B 点浇口样例盒通过**——V/P 切换 t=1.1307 s（filled 0.9604、p_gate 2.97 MPa，与 v0.2.2 崩溃点同一处），pressureRamp 0.05 平滑升压后跑到 endTime 2 s、Phase-1 体积分数 0.9983、exit 0；**C 真实件仍在切换后 6 ms 失稳**（t=4.3463 s non-finite、Courant max 2e8、deltaT 5e-153），按通告补做密集写盘复跑（writeInterval 1e-3 + purgeWrite 4）并打包回传材料（log 全文、moldingDict/physicalProperties.melt/0 场、故障前最后三帧 4.344–4.346）到 samples/cases/handoff-v023/，报告 ai-docs/reviews/v023-retest-report.md。复测暴露 Kairos 侧 case 生成缺陷：真实件请求 8 mm 浇口，case 内 inlet patch 实为 7 个三角形 1200 mm²（等效 φ39 mm、面心横跨 30 mm）→ 名义入口速度 0.18 m/s，建档 T72；浇口速度阈值仍按旧口径（5/20 m/s）与上游 SI 重标定（20）不一致，建档 T74。应用侧下载状态已同步到 v0.2.3（文件+manifest tag）
- **12:25** fix(deps): 资产架构匹配加固以配合上游新命名——上游把 release 资产名改成 `moldingFoam-<version>-<arch>.tar.xz`（文件名带版本号，同名歧义消除，T73 据此结案）。Kairos 侧 `bundle_asset_matches_arch` 随之放宽与收紧：架构别名覆盖 `arm64/aarch64` 与 `x86_64/x86-64/amd64/linux64/x64`（新命名若用 `aarch64`/`x86_64` 旧规则会失配），并**先排除 mac/win 资产**（旧规则只写「arm64」，遇到同一 release 的 `macosArm64` 包会误选）；两条新测试覆盖新命名六种写法与四类非 Linux 资产。版本标签仍取自 GitHub API 的 `tag_name`（未依赖文件名）；旧归档在新版本登记后按「文件名不同」自动清理，新命名下升级路径更干净。verify 全绿
- **12:33** docs(tasks): 工作区布局建档 T75——工程当前不自包含：`Project` DTO（schema v4）只存结构、无几何/网格引用，导入几何与体积网格只在会话内存（重启即失，重开工程要重新导入+重划网格），case 又在 `app_data_dir()/cases/<studyId>`、与工程文件位置无关，机器级数据（bundle 700 MB、自定义材料、最近工程）与工程数据混在同一层。布局定稿：工程文件所在目录即工作区根（不加 DTO 字段），内放 project.kairos + geometry/ + mesh/<studyId>/ + cases/<studyId>/ + results/<studyId>/ + reports/，工程文件只存相对路径，可重建物缺失按「可重建」提示而非报错；机器级资源留应用数据目录；散装 .kairos 继续可打开并提供「转为工程目录」。选此刻建档的理由：`recent-projects.json` 与 `cases/` 均不存在，无存量可迁移。本轮只建档，路径行为未改
- **12:59** fix(T76): case 材料热物性口径 + 冻死守卫 + 保压斜坡（上游 v0.2.3 复测定论驱动的修复；C 由失稳转为跑通）——上游用回传三帧定论 C 是冻死短射类：case 里 `transport { mu 100; Pr 4; }` 让求解器反推 k = mu·Cp/Pr = 62500 W/(m·K)（真值 0.2 量级），熔体入模即冻；本仓核实生成器只采样比热表、**从未使用材料导热表**。修：①`Pr = MELT_TRANSPORT_MU·Cp/k`（k 取导热表在熔温插值；缺失/非正报校验错）；②moldingDict 顶层写 `freezeOffTemperature = b5`（无流温度代理，键位按上游源码 `moldingDict.lookupOrDefault` 确认）与 `freezeOffFraction 0.01`；③`packing { pressureRamp }` = 注射时间 5%、夹 [0.05, 0.5] s。复跑矩阵（v0.2.3 同环境同 case）：原始失稳 t=4.3463 s → 仅修 k 后填充变快（切换 4.3404→3.9184 s）且**守卫全程静默＝不再冻**，但切换后 2.5 ms 仍 non-finite → 再加 ramp 0.2 后**跑到 endTime 8 s**（填充 0.9957、保压段闸口 57.5→51.8 MPa 平滑、reconstructPar exit=0），即上游「更长 ramp 预计同崩」的预期被对照实验推翻；B 修正后仍通过（填充 0.9947）。另记：日志回显的 `gateFreezeTemperature = -4.5036e+15` 是另一个旧键，不代表守卫状态。复测报告与 T76 文档同步，verify 全绿（core 行覆盖 100%）
- **13:02** fix(deps): 资产匹配按上游口径简化为「只看架构」——发布侧确认产物名为 `moldingFoam-v0.2.4-{arm64,amd64}.tar.xz` 且只出 Linux 包（bundle 一律跑在虚拟机里），故去掉上一版加的 mac/win 资产排除（用户明确不需要区分平台），匹配收敛为 `arm64|aarch64` 与 `amd64|x86_64|x86-64|linux64`，兼容旧命名与其它写法；测试改为用现行确切产物名 + 旧命名 + 未知架构三组断言。verify 全绿（dependencies.rs 行覆盖 100%）
- **13:15** docs(tasks): 状态清单逐条补齐任务档案（12 个）——按「status 每一步是否都有 task」核对：status 引用的 30 个任务号全部存在（无悬空），但逐条比对后发现 16 处无档案，分三类补齐：①**已实现（[x]）但无档案** 5 项——STEP 镶嵌导入（T77；T05 当年把 STEP 列为非目标、T40 又把它当既有同级实现引用）、几何修复工具（T78；T05 非目标写着「只做检查不自动改几何」）、内置样例几何（T79）、材料「21 列 CSV 批量导入」（T80）、「纤维/填料参数组」（T81）——这五项的实现都有代码但无范围/验收记录；②**已知缺口无任务** 4 项——双域/中面网格的求解消费（T82）、多型腔与流道体参与填充（T83）、大结果数据链剩余分块/压缩/矢量场（T84）、Linux 原生执行通道（T85）；③**未实现且无任务** 7 项合并为 3 条规划任务——扩展工艺模块 GAIM/双色/微发泡（T86）、多物理场耦合 纤维取向/流固（T87）、DOE 与工艺寻优（T88），避免为 [ ] 项建空壳。同时把 status 对应条目补上任务号（13 处），使每条都能点到档案。补档文件均标注「补档：实现早于本文件，范围按现状回填」并写明当前实现边界（如纹理/填料不参与求解）。verify 全绿
- **15:07** fix(moldingfoam): 保压斜坡口径对齐上游自适应默认（切换时刻的 5%）——上游回执确认 v0.2.4 起 `packing.pressureRamp` 缺省自适应取「切换时刻的 5%，夹 [0.05, 0.5] s」，并更正了他们此前「更长 ramp 对 C 无解」的预期（那是修材料前的冻结态）；Kairos 生成期拿不到实际切换时刻，改按「注射时间 × V/P 切换体积分数」预估（873 cm³/4 s/0.96 → 0.192 s，求解器实测切换 3.92 s 给 0.196 s，同窗口）；测试改为带切换分数的三段边界（样例盒 1 s/0.96 → 夹 0.05、真实件 4 s/0.96 → 0.192、20 s/1.0 → 夹 0.5）。verify 全绿（core 行覆盖 100%）
- **15:52** test(moldingfoam): v0.2.4 保压 ramp 实验矩阵（E2/E3/E5/E4/E7）——先核对部署：VM 内 libmoldingFoam.so 的 sha256 与官方 v0.2.4 arm64 资产逐字节一致；并确认上游的「ramp 缺省自适应」不在已发布的 v0.2.4 里（实测回显仍 pressureRamp = 0.05；该线走分支对接、未并入 release）→ E1 与按自适应口径的 E4 需含该分支的 bundle。实测（同 bundle 同环境，每次只改一个变量）：E3 ramp 0.2 → 跑满 8 s、填充 0.99567；E2 ramp 0.05 → 切换后 non-finite（与 v0.2.3 同配置**逐位同刻** 3.920958583275949 s，可作 039 下界证据）；E5 样例盒（缺省 0.05）→ 跑满 2 s、填充 0.99466；E4 不写 ramp + 守卫关 → 与 E2 **逐行相同**（恰好证明守卫对正常工况零影响）；E7 ramp 0.5 → 跑满 8 s、填充 0.99568（窗口上界不紧）。三条硬证据入 ai-docs/reviews/v024-ramp-matrix-report.md：ramp 速率是触发量（E3 ≈0.28 GPa/s vs E2 ≈1.1 GPa/s 的 p_gate 序列）、守卫零扰动、失稳确定性；回传包 samples/cases/v024-ramp-matrix/（log + 逐时刻 p_gate/p_target + 两帧）。verify 全绿
- **16:05** fix(T74): 浇口速度阈值按 SI 重标定对齐上游——`GATE_VELOCITY_LIMIT_M_S = 20` 单档告警（与 `fillVelocityWarn` 缺省同口径），删除 5 m/s「谨慎」档（旧值在 mm-as-m 口径标定，SI 下真实浇口速度 ≤ 数 m/s 会误报）；提示文案改为「浇口面积/流量错配」口径并点出「网格没表达浇口时名义速度被拉低」（与 T72 有效面积回显呼应）；单测覆盖 8.8 m/s 不报警与 20 m/s 两侧边界。verify 全绿（core 行覆盖 100%）
- **16:14** feat(mesh): 网格纵横比与双域匹配率进质量报告（T56）——`MeshingReport` 新增 `aspectMax`/`aspectAvg`（最长棱 ÷ 最短高；最短高 = 3V/A_max，与 edge ratio 共用一次遍历），`DualDomainReport` 新增 `matchRatio`（paired/全部）；退化单元分母取下限返回大有限值（IPC 数值字段不接受无穷）；角点/正四面体解析值（√6、√6/2）与无量纲性单测锁定，契约测试与几何/报告面板同步展示。verify 全绿（core 行覆盖 100%）
- **16:38** feat(mesh): 网格预估单元数（T57）——几何面板目标尺寸防抖 300 ms 后显示「约 N 单元」；体素按包围盒上限精确推算（`build_axes` 从 generate 抽出，估算与生成共用同一套逐轴数学，填满包围盒时估算值 == 实际四面体数，三档目标尺寸对拍锁定），Gmsh 按体积粗估并标注依据；超 `MAX_TOTAL_CELLS` 直接标红告警、不必等生成报错；估算走独立 `estimate_volume_mesh` 命令，失败静默不打断输入。verify 全绿（core 行覆盖 100% / 前端四维 100%）
- **17:05** feat(mold): 视口拾取放置浇口与节点吸附（T58）——模具网络面板进入放置模式后单击模型表面，命中点吸附到最近网格节点（顶点索引越界 / 无节点数据时回退命中单元中心：该单元表面顶点均值，再退命中点），坐标按微米取整回填浇口**终点**（型腔端，与 case 的 GatePortal 取端一致，起点保持表单值）；支持连续放置（按钮式开关，不挤动既有输入次序）、未载入网格 / 无活跃方案时禁用并给原因；`PickResult` 补命中点坐标，顶点读取改严格版（索引越界不再被静默当成原点）。verify 全绿（core 行覆盖 100% / 前端四维 100%）
- **17:52** fix(solver): 浇口入口面口径收敛与有效面积回显（T72）——入口面选择从「半径内全吃 + 补到目标」改为「最近面累积、一达到 πr² 就停手」（真实件 1200 mm² 宽带入口的根因：半径内单个 493 mm² 大面被整面带进）；`CaseReport` 回显逐浇口请求 vs 实际（面积 / 面数 / 倍数 / 最小入口面）、inlet 有效面积与等效直径，面积比 > 2 判不可表达并给出「请加密网格或加大浇口」告警；CLI JSON 增 `inlet` 字段与文本行，Tauri `CaseOutcome` → 工艺 store（带 studyId 不复用跨研究）→ 工艺面板「请求 vs 实际」行；工艺面板的填充工况校验改用有效面积（T62 提示与求解器实际入口一致）；回归用 4 mm 立方体两档网格锁定「可表达 ≤ 2 倍 / 粗网格告警」。verify 全绿（core 行覆盖 100% / 前端四维 100%）
- **18:20** docs(web): 术语统一「方案 / 研究」收口（T69）——`Study` 在界面上只用「方案」一个中文词：src-web 全部用户可见文案（面板标题 / 按钮 / 提示 / 任务窗格 hint / 报告页眉）、core 四条用户可见错误文案与 CLI 校验输出行统一改词，注释一并收口避免下次按旧词复写；DTO 字段与标识符保持 `Study` / `study` 不变，错误 code 分类不受影响；43 文件 + 59 处测试断言同步。verify 全绿（core 行覆盖 100% / 前端四维 100%）
- **19:05** feat(analysis): 浇口位置分析序列（T55）——core 新增 `services/gate_location.rs` 纯函数启发式：候选限**表面单元**（浇口在制品表面），评分 = 流动长度均衡（1 − 最长流动距离/对角线）× 壁厚可达性（单元最短高 det/(2·A_max) 相对全场中位数折减、下限 0.5）；输出逐单元适合度场（直接作为当前场载入视口云图）与 Top-N 建议；方案任务窗格新增「浇口位置（无需浇口）」序列（旁路：不参与提交前置），模具面板列出 Top-N 并「设为浇口」一键落成浇口单元（终点 = 建议点、起点回退半径，直径非法按 6 mm）；实测 62.5 万四面体 / 399 候选 = 296 ms（release，预算 3 s）。消融：去掉「候选限表面」→ 2 项测试变红，还原复绿。verify 全绿（core 行覆盖 100% / 前端四维 100%）
- **19:38** feat(analysis): 填充预览（T59）——core 新增 `services/fill_preview.rs`：单元共享面邻接（排序面元组一次成表）+ 从浇口单元出发的多源 Dijkstra（边权 = 形心距离），输出「可充填覆盖（图连通）」与归一化到达序场（0 = 浇口、1 = 最远可达，直接作为当前场进视口云图）；断连区域给出单元计数/占比告警、浇口离件过远（> 5× 平均单元尺寸）提示核对坐标；模具面板新增「填充预览」按钮与结果行。实测 62.5 万四面体/单浇口 = 168 ms（预算 3 秒，见 perf-budget.md 新增实测段）。消融：多源种子改为仅首个浇口 → `multiple_gates_share_the_source_set` 变红，还原复绿。verify 全绿（core 行覆盖 100% / 前端四维 100%）
- **20:26** perf(results): 大结果数据链收口（T84）——二进制值区改 **f32 截断**（相对误差 ≤ 2^-24，f64 通道保留作无损对拍；1e7 值载荷 80→40 MB，编解码 31/12 ms 与 f64 基本持平，省的是传输与内存）；场缓存从 FIFO 升级为 **LRU + 值数预算**（默认 2e7 值 ≈ 160 MB，命中刷新最近使用、单场超预算保底不空转，新增命中/未命中与用量统计）；**矢量场三分量通道**：`read_vector_field` + `decode_vector`（标量/矢量载荷交叉解码明确报错、未知值格式报错、缺 format 的旧载荷按 f64 兼容）+ `load_vector_field_binary` 命令 + 结果面板「加载矢量场」（时间步跟随当前已加载场、显示首单元分量与 |v| 范围）。基准入 perf-budget（ascii 解析 426 ms 为加载大头 / 编码 31.4 ms / 解码 12.4 ms / 矢量 1e6 单元 9.1+4.0 ms）。verify 全绿（core 行覆盖 100% / 前端四维 100%）
- **18:42** feat(project): 工作区布局（T75）——工程变成自包含目录：新建工程落在 `<用户文档目录>/kairos/<工程名>/<用户命名>.kairos`（文档目录经 Tauri `document_dir()` 解析，兼容 Documents / 文档 / Dokumente 等语言差异，不硬编码）；导入几何归档进 `geometry/`（原样拷贝、重名加 id 前缀）并在工程文件里存**相对路径**（schema v5 新增 `geometries`，v4 文件按空集合迁移）；体积网格落盘 `mesh/<方案 id>/`（`mesh.bin` 二进制 + `mesh.json` 清单，新 `services/mesh_store.rs`），生成网格后自动写入；case 目录 → `<工作区>/cases/<方案 id>`（散装工程回退应用数据目录），工作区工程生成报告直接写 `<工作区>/reports/`（散装工程回退下载，写入失败也回退）；打开工程按引用恢复几何（沿用工程内 id）+ 逐方案读回网格；工程树新增工作区路径行（无工作区显示「散装工程」）；CLI `pipeline run --out-dir` 默认 `workspace/`、case 落 `cases/cli/`。core 新增 `services/workspace.rs` 纯路径规则（工作区判定 / 默认路径 / 相对路径安全 / 归档命名，跨平台含冒号拒绝）。verify 全绿（core 行覆盖 100% / 前端四维 100%）
- **19:26** docs(research): 扩展模块可行性规划收口（T86/T87/T88）——三份结论文档：**特殊工艺**（GAIM/双色/微发泡：GAIM 等上游三相与气芯场、双色暂不建档、微发泡 PVT/黏度经验修正近似可先行）、**多物理场**（纤维取向链路等上游取向张量、双向流固耦合判定不做——需模具域网格与接触模型，与「只建制品域」架构冲突）、**优化与自动化**（DOE 串行排队 + 工作区汇总表、寻优贝叶斯优先且失败点必须报出、并发调度不做）；每项写明「能/不能/依赖谁」与开工条件，回填 architecture-status E1/E2/E3，新建后续任务 T89–T93 并更新索引
- **20:12** feat(solver): 冷却水路落地到 case（T67）——模壁 `walls` 的 T 边界在有水路时改为 `moldingMoldTemperature` + `coolant` 子字典：`massFlowRate`（各水路求和）/`cp`/`inletTemperature`（取最冷一路）/`direction`（首段轴向单位化，零长回退 +x）/`htc`（默认 5000 W/m²/K）/`D`（直径写米），无水路保持恒定模温（快照测试锁定）；`CoolingChannel` DTO 补 `massFlowRateKgS` + `specificHeatJKgK`（水默认 0.05 kg/s / 4180，`CoolingChannel::default` 与面板同源），模具面板新增流量/比热输入，校验面板提示缺项（旧工程文件迁移后为 0）；工艺面板 → case 生成透传水路。简化记录：制品域无模具域，先「整壁共用一组通道参数」，逐通道 patch 与三维水路 CHT 待模具域网格立项；真机冷却复跑归 T29。消融：入口温度聚合改取首段 → BC 测试变红，还原复绿。verify 全绿（core 行覆盖 100% / 前端四维 100%）
- **21:05** feat(viewport): 翘曲变形可视化（T65）——core 新增 `services/deformation.rs`：`变形 = 原位 + 倍数 × 顶点平均位移`（顶点位移取所属各面单元位移均值，避免共享顶点被相邻单元重复拉动撕裂；位移按 m→mm ×1000）；`deform_render_mesh` 命令复用 T84 的矢量三分量通道（加载矢量场时同步写入结果会话槽位，不再回传分量），倍数 0 由后端短路返回原位网格（前端无备份/还原分支）；视口新增「变形显示」开关 + 倍数输入，变形后重放云图与剖切。消融：位移聚合改直接覆盖 → 共享顶点用例变红，还原复绿。真机翘曲 case 截图归 T29。verify 全绿（core 行覆盖 100% / 前端四维 100%）
- **21:40** docs(review): T21 循环任务第 2 轮（2026-09-13 收口批次）——覆盖 14 个提交（T55–T88）：八项清单逐项结论（core 无 tauri / 无组件直连 invoke / 契约测试 19 项锁新 DTO / 三条新性能预算全部实测回填 / 消融抽查 8 处全部「消融→变红→复绿」/ 零新增依赖 / CSP 与 capabilities 未动 / 无新增平台条件编译面），上轮遗留逐条复核（T51 三项全闭环、C5 与 C6 的 Kairos 侧闭环、C2/T48/T85 仍在），回流建档 T89–T93；报告见 ai-docs/reviews/M6-review-round2.md，下轮重点为 T29 真机复跑（本批次改了入口面口径、模壁冷却边界与矢量通道）
- **22:18** feat(solver): Linux 原生执行通道（T85）——bundle 解压在本机即就位：`downloads/moldingfoam/**` 下含 `openfoam14/etc/bashrc` 的目录为环境根（多版本取最近修改），原生求解脚本固定顺序 `source <env>; export PATH='<受管 bin>:$PATH'; cd '<case>' && decomposePar…`（core 侧单点构造 + 单引号转义，防路径注入）；新增 `native_env_status`（环境 / OpenMPI `libmpi.so.40` 探测 + 处置提示）与 `native_deploy_bundle`（结构校验 + 写 `.kairos-release-tag`，Linux 上 T24/T54 的「更新未部署」提醒同样生效），`vm_deployed_release_tag` 原生分支读本机标记；`submit_job` 前自动刷新环境快照（刚下载解压也能直接跑）；依赖 / 作业面板按 provider 改用「部署到本机」措辞。设计要点：原生判定按值（provider / vm_shell）而非 `cfg`，因此 macOS 上可跑完整单测；消融：script 的 source 段挪到 cd 之后 → 顺序用例变红，还原复绿。Ubuntu 真机复跑归 T29。verify 全绿（core 行覆盖 100% / 前端四维 100%）
- **23:05** feat(material): 微发泡近似（T90）——材料新增 `blowing` 参数组（发泡剂 / 质量分数 0~~30% / 密度下降 0~~60% / 黏度下降 0~90%，双端镜像 + 契约测试）；`apply_blowing_correction` 在 case 生成期做两项经验修正：有效密度下降 x% → Tait 比容系数 `b1m`/`b1s` × 1/(1−x)，表观黏度下降 y% → Cross-WLF `D1` × (1−y)（`n`/`τ*`/`A1` 形状参数不动；修正只作用于本 case，工程材料保持原值以便发泡/不发泡对照）；生成报告带「非预测级」告警（走 T72 的通路到 CLI JSON 与工艺面板），材料面板新增琥珀色「微发泡近似（非预测级）」参数组。消融：volume_scale 恒 1 → 字典落值用例变红，还原复绿。真机对照归 T29。verify 全绿（core 行覆盖 100% / 前端四维 100%）
- **23:52** feat(results): 对称张量场读取通道（T91 第一步）——`volSymmTensorField` 从「明确不支持」升级为独立类型：解析 6 分量（OpenFOAM 顺序 xx/xy/xz/yy/yz/zz）、模量 `sqrt(xx²+yy²+zz²+2(xy²+xz²+yz²))`（与 `mag(symmTensor)` 同口径）、主方向（3×3 Jacobi 对角化取 |λ| 最大特征向量，符号规范化保证多次读取方向一致）；标量入口（`read_field`）取模量直接把饱和应力云图送上视口，张量入口（`read_tensor_field` + `load_tensor_field` 命令）给分量/模量/主轴，结果面板新增「对称张量场（残余应力）」行（|σ| 范围 + 首单元主轴）；非对称 `volTensorField` 仍明确拒绝。消融：模量剪切项去掉系数 2 → 解析值用例变红（70.71→50），还原复绿。剩余：`filler` 入 case（关键字待上游冻结）与取向张量 a2 消费（等上游取向求解）。verify 全绿（core 行覆盖 100% / 前端四维 100%）
- **00:35** feat(geometry): 导入日志流（T60 第一项）——STL / STEP / IGES / 样例四条导入路径返回 `ImportOutcome { summary, log }`：日志含格式与文件名、三角形数、包围盒三轴与推断单位、表面积/有符号体积/解析耗时（`Instant` 计时）、健康检查结论（异常时四类计数 + 「建议在几何面板执行修复」）；core 纯函数 `import_log` 生成、命令层只计时；前端 geometry store 以环形缓冲（200 条）保存，日志区「网格日志」标签置顶展示，导入失败仍走原错误通道。PPTX 导出未做（需先定依赖策略：前端 pptxgenjs ≈1 MB 或 Rust 侧手写最小 OOXML），结论与两条路线记录在评审里。verify 全绿（core 行覆盖 100% / 前端四维 100%）
- **01:20** fix(security): 生产 CSP 零行为变化加固（T33 部分）——新增 `script-src 'self'`（显式化，原由 default-src 隐含）、`object-src 'none'`、`base-uri 'self'`、`frame-ancestors 'none'`、`form-action 'none'`：对应能力（`<object>`/`<embed>`/`<base>`/被 iframe 嵌入/`<form>` 提交）全仓 grep 为 0 命中，故不改变任何现有功能；`style-src 'unsafe-inline'` 仍保留（主题运行时注入 `<style>`，移除需 nonce 化或主题文件化 + 真机回归，归 GUI 走查）；updater 插件接入与三平台签名等密钥 / 证书就绪（接入清单已写进 release.md 与 T33 评审）。构建校验：`bun run build` + `cargo check` 通过
- **02:05** refactor(paths): 路径处理收口到 `std::path`（新增 `services::paths` 工具模块）——新增 core 模块 `paths`：`join` / `to_storage` / `from_storage` / `validate_relative` / `sanitize_file_name` / `file_stem` / `normalize_separators` / `wsl_path`，全部基于库函数（`join`/`file_name`/`file_stem`/`extension`/`components`/`as_os_str`/`with_extension`/`MAIN_SEPARATOR`），跨机器落盘的相对路径统一 `/`；`workspace.rs` 删掉手写清洗与主干切分、`commands/project.rs` 报告名改走 `file_stem` + `with_extension`、WSL 盘符映射下沉 core（取首组件 `as_os_str`，Unix/Windows 同一代码路径）、导入分派回 Rust（新增 `GeometryFormat` + `format_from_path` + `parse_file`，三条导入命令合并为 `import_geometry`，前端不再 `split('.')`）；AGENTS.md 增「路径一律走库」红线 + ARCHITECTURE §1.2 入口表。同时修 CI 两处红：`vm_logic` 导入去掉 cfg（Linux 原生分支要用）、`validate_relative` 增 `has_root()`（Windows 上 rooted 路径 `is_absolute()` 为 false）。消融：去掉 `has_root()` → `\etc\passwd` 用例在 Unix 变红，还原复绿。verify 全绿（core 行覆盖 100% / 前端四维 100%）
- **02:40** fix(paths): 修 Windows 复跑暴露的两处路径语义 —— ① 用户输入的**名字**不再当路径解析：新增 `paths::path_segment`（保留字符含分隔符/冒号一律换下划线，无目录语义）用于工程名/文件名/报告名，真实文件路径取末段改用 `paths::file_name_of`（`Path::file_name`）——此前 `file_stem("x:y")` 在 Windows 得 `y`、Unix 得 `x_y`（Windows 把 `x:` 当盘符）；② `validate_relative("C:/Users/…")` 的报错文案两平台不同（Windows 先被 `has_root()` 拦下），测试对该用例只断言报错、文案断言留给平台无关用例。同时清掉三处「一边平台永不执行」的写法：分隔符归一改为两个分隔符都替换（幂等，无平台分支）、`drive_letter` 空串 `?` 分支改单 match、`file_stem` 去掉永不执行的回退闭包。zip 全绿（core 行覆盖 100% / 前端四维 100%）
- **03:05** fix(paths): 修 Windows 第三条（文案断言的漏改）——`workspace.rs` 里 `validate_relative("C:/Users/x/part.stl")` 仍断言「不得含盘符」文案，而 Windows 上该输入先被 `has_root()` 拦下（文案为「必须是相对路径」）；改为只断言报错，文案断言统一放平台无关的 `geometry/a:b.stl` 用例。全仓扫描同类断言，确认只剩两处且都挂在平台无关用例上。verify 全绿（core 行覆盖 100% / 前端四维 100%）

## 2026-09-14

- **01:36** fix(solver-e2e): 求解链路四处集成缺陷（T29 真机复跑暴露）——①VM case 解压层级：宿主归档条目自带叶子名前缀，解压目标却选成 case 目录本身，VM 内多套一层同名目录、求解器找不到 `system/controlDict`；新增 core `vm_case_extract_command`（解到 case 根）并复用于作业层，单测锁命令文本与单引号转义。②作业成功判定：求解命令用 `;` 串接 `reconstructPar`，求解器中途报错退出码仍可为 0（已写出部分时间目录的失败作业会被判成功）；core 新增 `job_failure`——求解器错误标记（FOAM FATAL/exiting/aborting）优先于退出码，其次才看退出码与回传；单测覆盖四种组合。③场清单过滤：时间目录里的非场对象（`moldingStage` 等）被列进场清单，结果面板一点即报「暂不支持该场类型」；`scan_times` 改按 FoamFile 头 `class` 过滤（只读 512 B 前缀，避免为筛选读全套结果），新增两分支单测并同步修正 gap 测试夹具。④**GUI 提交的作业永不启动**：`submit_job` 先把作业提升为 Running，`promote_and_spawn` 随后看不到待提升作业、不再起线程，作业永远停在「运行中」——前两轮 e2e 走 CLI/脚本绕开了调度器；提升与起线程收敛到 `promote_and_spawn` 单点，并加回归测试（消融：恢复旧写法后作业 10 s 内不退出运行态即变红）。另把工程序列化往返样本补上材料 / 工艺 / 杆系 / 几何引用，锁住这些字段不再静默丢失。verify 全绿（core 行覆盖 100% / 前端四维 100%）
- **01:38** feat(web): 方案配置自动保存 + 工艺出厂默认预设 —— ①工程配置持久化：材料 / 工艺 / 杆系 / 几何引用此前只改内存，关掉再打开 `.kairos` 全部丢失（用户报「每次打开都重置了」）；`project` store 增 800 ms 防抖自动保存，`touchActiveStudy` 与 `upsertGeometryRef` 落盘最新工程，无工程 / 无路径（散装工程）不排队，失败进全局错误不打断编辑；显式保存 / 另存为不变；store 单测覆盖连续编辑只写一次、几何引用同样触发、无路径不排队、失败与早退分支，消融（去掉 `touchActiveStudy` 里的排程）即红。②工艺面板不再空表单：出厂默认值直接预填进表单（无工艺的方案打开即可「校验并应用到方案」），并新增常驻「选择预设」清单首位的**出厂默认**预设（不落 localStorage，可随时回填；同名保存被拒并提示）；③顺带修压力回填口径——表单字段是曲线峰值（应用时铺 100% → 80% 两点），回填却取末点，导致「应用 → 回填 → 再应用」每次衰减 20%，改取起点后幂等（单测锁「切走再切回仍是峰值」）。verify 全绿
- **01:40** docs(review): T29 e2e 报告重组 + 任务档案状态 —— 报告改为「结论摘要 → 环境与产物 → 链路①样例盒 → 链路②真实件 → 集成缺陷（分首轮 / v0.2.5 轮）→ 上游事项 → 未决与遗留 → 最小连通性复现步骤」，两轮结果并排对照（v0.2.1 vs v0.2.5：填充 0.751→0.990、门控压力 0.15→57.6 MPa、真实件由 t≈0.0134 s 发散变为跑满 endTime），缺陷表逐条写明修复与验证方式，并显式记录两条未决：视口/图表截图核验（本会话 Computer Use 图片栅格不可交付）与真实件 4 进程死锁（上游 issue #7）；T29 任务档案同步为「已跑通 + 剩余三项（最小连通性复跑 / 截图核验 / 上游并行修复）」。verify 全绿
- **01:44** docs(status): 架构状态与任务清单同步 T29 进展 —— `architecture-status.md` 新增「真实求解链路复跑（T29，bundle v0.2.5）」条目（样例盒 4 进程跑满 endTime、真实件 1–2 进程跑满、桌面端作业链路打通、4 进程并行归上游 issue #7、视口截图待补），B2 分支缺口从「等 bundle SIGILL 修复的真机复跑」改为「真实件多进程并行（上游）+ moldingFoam 物理迭代」，完成度 65% → 70%；`tasks/README.md` 的 T29 行同步为「三条链路已跑通，剩最小连通性复跑 + 视口截图核验」。verify 全绿
- **02:05** docs(review): 最小连通性复跑结论回写（GUI 链路）—— 样例盒 1 mm 体素（5000 四面体）全程在桌面应用里走完：导入样例 → 生成网格 → 材料「用于当前方案」（自动保存随即落盘）→ 工艺面板（表单已带出厂默认值）一键「校验并应用到方案」→ 提交；VM 内可见 case 复制、`processor0/1` 分解产物与 `foamRun` 启动（日志回传 `Selecting solver moldingFoam`），**但数十秒后求解进程消失、作业停「运行中」**；同一 case 用同一套命令手动重跑跑满 endTime（31 个时间目录、V/P 切换 1.6709 s、保压 59.9 MPa），把结果按回传口径放回宿主后，结果面板「扫描结果」列出时间步与每步可用场（非场对象 `moldingStage` 已被过滤）、视口「载入网格到视口 / 加载 T 场」色标读数 506.40 / 498.11 / 353.28 与 t = 3 s 的 T 场一致。报告新增 §6「最小连通性复跑」并把遗留重排为五条（GUI 作业进程生命周期、截图与 XY 探针、真实件并行、真实件工艺、历史填充窗口），T29 任务档案同步。verify 全绿
- **02:12** fix(web): 作业列表运行期自动轮询 —— GUI 最小连通性复跑暴露：作业早已被后端判定失败，界面仍显示「运行中」（作业列表是提交时拉取的快照，只有手动「刷新作业」才会更新），把「作业没跑完」误读成卡住。作业面板在存在排队 / 运行中作业时每 1.5 s 刷新一次列表，作业收尾（成功或失败）即停表，面板卸载经 `onScopeDispose` 清理定时器；新增用例覆盖「运行中轮询 → 结束停表 → 卸载清理」三段，消融（令活动判定恒为 false）即红。verify 全绿
- **02:16** fix(web): 作业失败原因可见 —— 作业行此前只有状态文案（「失败」），调度器写进 `job.message` 的失败原因（求解器报错退出 / 进程异常退出 / 结果回传失败等）界面上看不到，用户只知道失败、不知为何。作业行新增一行玫红文案渲染失败 / 取消原因；用例补断言（失败行必须出现原因）。verify 全绿
- **02:22** docs(review): GUI 作业进程消失的机制定位 —— VM 日志（`systemd-logind`：`session-*: Deactivated` → `Removed session`）显示 `multipass exec` 的每次调用在 VM 内是一个 ssh 会话，会话关闭会连带回收会话内进程；桌面端作业的存活期因此被绑在客户端进程上，客户端提前退出即把 `foamRun` 一起收走（同一 case 手动 `nohup … &` 跑满 endTime 正说明这一点，nohup 脱离了会话进程组）。报告把该条从「待查」升级为「已定位机制 + 修法」：远端命令以 `setsid` 脱离会话并把输出落到 case 内日志，客户端改 `tail` 日志流式回传、结束时取退出状态。verify 全绿
- **02:30** feat(web): 新建项目弹窗（项目名 + 文件名）—— 此前「新建项目」直接建一个固定叫「未命名项目」的工程，用户无从命名；现在菜单 / 快捷键 ⌘N / 工具栏 / 命令面板都先弹出对话框：项目名（决定 `<文档目录>/kairos/<项目名>/`）必填，文件名（决定 `<文件名>.kairos`）默认跟随项目名、手动改过之后不再跟随，为空时落回项目名；创建成功才收起，失败（如路径非法）保持打开并给全局错误。`newProject` 改为返回是否创建成功供对话框判定。新增对话框组件与 `useNewProjectDialog`（模块级单例，与「关于」同一套开合模式），并加 6 条用例（清空字段、跟随 / 不跟随、必填校验、文件名兜底、失败保持打开、取消与遮罩关闭）；消融（去掉必填校验）即红。前端四维覆盖 100%，verify 全绿
- **02:38** docs(status): 工程工作流条目补充新建项目弹窗 —— `architecture-status.md` 的「工程 / 方案工作流（T03/T68）」补记：新建工程先弹对话框收项目名与文件名（文件名默认跟随项目名，决定 `文档/kairos/<项目名>/<文件名>.kairos`），菜单 / ⌘N / 工具栏 / 命令面板四个入口一致；真机验证：对话框建「控制器支架」→ 落盘 `~/Documents/kairos/控制器支架/控制器支架.kairos`（工程名与文件名一致、自带默认方案）。verify 全绿
- **11:25** feat(web): 新建项目改为「工作区路径 + 项目名」，工程文件名与项目名一致 —— ①对话框下方新增**工作区路径**（初值取平台默认根 `<文档目录>/kairos`，可用系统目录选择器改到任意目录，含外接盘），落点预览实时显示 `<工作区>/<项目名>/<项目名>.kairos`；②去掉独立的「文件名」字段，工程文件一律与项目名同名；③相应把工作区判定从「必须在默认根之下」改为「只看工程目录 + 其下同名工程文件这一布局」（`workspace_root(project_file)` 不再需要文档目录），于是自定义工作区的工程同样是自包含的（几何 / 网格 / case / 报告随工程走），散装 `.kairos` 仍按散装处理；④命令层随之调整：新增 `default_workspace_path` 与 `project_path(workspace, project_name)`（工作区留空回落默认根），`workspace_root_of` / `archive_workspace_geometry` / `save_report_to_workspace` / `default_case_dir` 与几何三条工作区命令改用新判定并清理不再需要的文档目录参数。核心单测重写（布局判定四类：同目录同名 / 名字不一致 / 直接放在根下 / 根目录或相对路径），前端 8 条对话框用例覆盖默认值、选择器取消、必填、创建成功与失败、预览文案；真机验证：对话框默认工作区即 `/Users/yuki/Documents/kairos`，建「支架测试」落盘 `~/Documents/kairos/支架测试/支架测试.kairos`（目录名与文件名一致）。verify 全绿
- **11:45** feat(jobs): 作业按需自动启停虚拟机（省内存）—— 提交作业时若受管实例没在运行，作业线程自动 `multipass start`（WSL 靠首次执行自启）并轮询就绪探测到 guest 能执行命令为止（最多约 2 分钟，日志回显「启动虚拟机（作业需要，跑完自动关闭）」），就绪后才把 case 复制进 VM；队列跑空（无排队 / 运行中作业）且实例是作业自动拉起、又没有交互 Shell 会话占用时，自动关闭虚拟机。core 新增两条纯策略并单测穷举：`ready_probe_args(provider)`（multipass exec true / wsl -d distro -- true / 原生 None）与 `should_stop_when_idle(auto_started, active_jobs, shell_open)`（多作业排队期间与 Stop 在用时都不关）；适配层新增 `commands::vm_lease` 记账模块（自动拉起 / Shell 占用两个进程级标记），`vm_shell_start`/`vm_shell_stop`、`vm_stop`、退出联动各自维护标记。用户手动启动的实例不关（没有自动拉起标记），Shell 打开期间作业收尾也不关。verify 全绿
- **13:20** fix(jobs): VM 求解改为脱离会话执行 + 上游并行死锁标注 —— ①`multipass exec` 跑的求解是该 ssh 会话的子进程，客户端一退出 systemd-logind 就把会话进程回收，求解跑到一半无声消失：改为 `setsid` 独立会话启动、标准输出重定向到 case 内 `kairos-solve.log`、退出码写 `.kairos-solve.exit`，客户端只做「启动 + 每 1 秒 `tail -c +offset` 增量回读（哨兵 `##KAIROS-STATUS##` 后跟退出码）」，求解不再依赖会话存活，进度与失败判定都有据可依（core 三条纯函数 `detached_launch_command` / `detached_read_command` / `parse_read_output` 各带单测）；②作业收尾统一抽成 `finish_job`（成功路径此前漏了「空闲关实例」，已补）；③`multipass exec` 管道偶发不回退（远端命令早已结束、CLI 停在自旋）加了 600 秒等待上限；④根因修正：`multipass exec` 对停止实例会**隐式拉起**虚拟机，应用启动时读 VM 内版本标记那条 exec 就会把十几 GB 的实例拉起来——只在实例已运行时才 exec，且「作业自动拉起」租约登记改到任何会启动实例的命令**之前**（真机复验：VM Stopped 下启动应用仍 Stopped，提交作业后才起）；⑤文档标注上游 046 已修 issue #7（边界 patch 循环内归约 → 各 rank 归约次数不等）但**未发版**，真实件 np=4 复测等新版 bundle
- **14:05** refactor(jobs): 变量式求解脚本收口 + 消融验证 —— 抽取 `vm_solve_script`（VM 与 WSL 两处原本各拼一遍「source 环境 → cd case → 分步求解」），回读函数改为单次扫描并直接回报「有错误标记 / 新偏移 / 退出码」，回读失败只在首次提示一行（此前是空分支吞掉错误）；补回 `finish_job` 被挤掉的文档首行。消融测试（改坏规则 → 对应单测必须变红，逐个验证后还原）：①启动命令去掉 `setsid` → `detached_launch_uses_setsid_and_case_side_markers` 红；②回读命令去掉退出码占位 `'-'` → `detached_read_streams_from_offset_and_reports_status` 红；③偏移不推进 → `read_output_splits_log_from_exit_code` 红；④哨兵缺失时按「已结束」处理 → 同上红；⑤空闲关实例忽略 Shell 占用 → `stop_when_idle_only_for_auto_started_leased_vm` 红。5/5 均按预期变红。适配层两条「只在真机可见」的规则另有前后对照证据：`multipass exec` 对停止实例会隐式拉起（去掉状态判断时应用启动即拉起 VM，加判断后启动仍 Stopped）、脱离会话后求解活过客户端退出。verify 全绿
- **15:10** refactor(core): 材料 CSV 导入按行拆分 + 全仓库消融验证 —— ①`parse_custom_csv`（109 行）拆成「表头校验 → 逐行 `parse_csv_row` → 填料列 `parse_csv_filler`」，行为与错误文案不变；②全仓库注释/代码机械扫描：悬空 `///` 文档注释 0、TODO/FIXME/XXX 0、过程叙事式注释 0，clippy `too_many_lines` 剩余 3 处（`midplane`、`moldingfoam::generate_case`、`repair`，本轮只处理了纯线性、可拆分且覆盖不受影响的一处，其余记入待办）；③跨仓库消融测试 7 项，全部按预期变红：求解失败判定忽略 solver 错误标记、求解器错误标记识别失效、结果扫描不再按 FoamFile class 过滤、路径清洗不再替换保留字符、部署状态判定恒为已部署、配置改动不再触发自动保存、保压压力回填退回「取末点」口径。verify 全绿
- **13:35** fix(jobs): case 复制与结果回传改走 `multipass transfer`（弃用 stdin/stdout 管道）—— `multipass` 的管道通道在远端命令结束后偶发不回退（CLI 进程自旋），本机实测可稳定复现：远端 tar 早已解压完、`multipass exec` 仍停在 R 状态十几分钟，作业因此一直停在「运行中」。复制改为三步落盘：宿主打包 → `multipass transfer` 进 VM → 远端解压（解压后删归档、重跑前删同名目录）；回传同理：远端打包结果时间目录 → `multipass transfer` 回宿主 → 宿主解压。core 新增`vm_case_extract_from_archive_command` / `vm_results_pack_command` / `transfer_args` / `vm_archive_path`（含单测），宿主侧命令统一走 `run_host`（超时保护 + 把 stderr 首行带进失败原因，此前静默丢弃导致作业日志只剩一句「失败」）。真机验证：宿主 case（7.0 MB）→ transfer 进 VM → 远端解压（初始场 5、时间目录 21）→ 远端打包 → transfer 回宿主 → 解压 36 个文件 → `kairos-cli results list` 列出 0/1/2；另查明 `multipass transfer` 与 `exec` 不同、实例没运行会直接报错（App 里 transfer 在 `ensure_vm_ready` 之后，顺序安全）。消融 4/4 全红：解压不删同名目录、解压目标改成 case 自身、打包丢时间目录清单、传输退回管道形态。verify 全绿
- **13:50** docs(tasks): GUI 端到端独立成 T94，端到端验证默认走 CLI 通道 —— 求解链路本身（生成 case → 送进 VM → 求解 → 结果回传 → 结果扫描）今后统一用 `kairos-cli` 跑，可脚本化可重放；只有验证目标本身是界面 / 作业编排行为时才走 GUI。新增 [T94](tasks/T94-gui-e2e-verification.md)：作业提交与执行、虚拟机「作业拉起 / 跑完关闭」真实行为、失败可见性、取消、结果面板联动五条，并记录已知障碍（求解面板在 AX 树里折叠、需人先展开一次）；T29 剩余项 a 指向 T94，任务清单表头补 CLI 默认口径。另记上游事项：046 已修但未发版，真实件 np=4 复测等新版 bundle
- **14:10** docs(tasks): PPTX 定为 Rust 侧生成 + 新增 T95 依赖取向审查 —— ①T60 的 PPTX 取向改为「core 生成、命令层落盘、前端只触发与打开」，候选 crates.io 现成库 `ppt-rs`（8.8 万下载）/ `pptx` / `pptxboss-write`，准入看许可（permissive，GPL 不进进程内）与是否覆盖模板 + 表格 + 图片 + 中文排版，不满足则回退 `zip` + 手写最小 OOXML；②新增 [T95](tasks/T95-dependency-audit.md)：全仓库审查「用现成库 vs 自造轮子」，判断标准按「规格归属（通用问题用库；OpenFOAM case 格式 / 网格体素化口径 / KairosError 契约这类产品规格必须自写）→ 许可合规 → 可验证性（覆盖率门禁下包装层仍要被测到）→ 体积与离线 → 维护度」排序，审查面覆盖 Rust（CSV、STL/STEP/IGES 解析、几何谓词、归档压缩、哈希、id/时间）与 TS（防抖节流、深拷贝、格式化、CSV/JSON 导入导出、图表与色标自绘、交互设施），产出决策表 + 能当场改的小提交
- **14:35** docs(tasks): T95 依赖取向审查出决策表（两端盘点完毕）—— Rust 侧引库：材料 CSV 导入引 `csv`（手写 split 不支持引号/转义，Excel 导出的 BOM 会让表头校验直接失败）、几何数值引 `nalgebra`（仅线性代数；`parry3d` 暂不引，相交谓词与容差口径耦合）、下载完整性引 `sha2`（现在是零校验，供应链风险）、手写二进制编解码只引 `bytemuck`（KF1 布局是跨端契约，不能换 bincode）、解压从「调系统 tar/unzip」逐步换库（先 .tar.gz/.zip，`.tar.xz` 的 liblzma 绑定单独评估）；自写保持：`paths.rs` 跨平台契约、加权 LRU、STL 解析（已有充分单测）、`now_ms/new_id`（改为收敛到 core，不引 uuid/chrono）。TS 侧引库：`@vueuse/core`（防抖 3 处 + 点击外部 2 处 + storage 网关 62 行）、`uplot`（自绘曲线只有min/max 两刻度，替换前先出视觉对照）、`gl-matrix`（低层 mat4/vec3，顺带补 invert）；自建不引：统一数值/单位 formatter（45 处散落、零测试，收益在统一而非引库）、CSV 导出加引号+BOM、色标常量模块（着色器仍要手写同一套插值）、快捷键表（要与 macOS 原生加速器对应）。许可已逐个核对：候选全为permissive（MIT / Apache-2.0 / ISC / Unlicense / Zlib），无 GPL/AGPL。执行顺序分 P1（安全 + CSV）/P2（VueUse 收编 + formatter）/P3（uPlot、nalgebra、bytemuck、解压库化），每批独立提交带对拍或消融证据
- **15:00** docs(tasks): 任务清单整体核对 + 执行顺序落表 —— 逐条核对 12 个未完成任务声明的依赖/剩余范围/外部条件，修正 6 处口径不一致：①T29 行与文件对齐（GUI 端到端与最小连通性已归 T94）；②T83 行原写「依赖 T29 闭环」与文件不符（文件依赖 T07/T61 均已满足）→ 改为可开工；③T60 行去掉未兑现的 T95 依赖（T95 决策表无 PPTX 条目）；④T95 状态同步为进行中；⑤**T91 此前漏在两张进度表之外**（只在「已实现任务」表里、文件仍有剩余项）→ 补入待开工表并注明等上游；⑥T82 文件依赖行补上「上游降维支持口径」。新增「执行顺序」小节：档 A 可开工按 T95 → T89 第一步 → T83 → T60 → T33 可做部分；档 B 等外部（T29 剩余 / T82 / T91 剩余 / T89 第二步 / T48 / T33 收尾 / T94）；档 C 被内部任务阻塞（T92 → T93）
- **15:40** feat(deps): 下载物计算并落盘 SHA-256（T95 P1-a 供应链完整性）—— 上游 release 只发归档、不发校验值（`gh release view` 确认仅两个 tar.xz），因此摘要是**记录**而非比对：下载完成后分块计算归档摘要（core 新增 `services/digest.rs::sha256_file`，64 KiB 分块，空文件/"abc" 公开向量 + 跨块边界 + 目录读取错误（EISDIR）+ 文件缺失四条单测），写入 `manifest.json` 的 `ManifestEntry.sha256`（`serde(default)`，旧清单照常可读），供跨会话核对同一文件是否被替换，也为将来上游发布 SHA256SUMS 留出比对点。IPC DTO 与前端未改（避免 fixtures 连锁），清单展示与「上游发布校验值」记入 T95 待办。core 新增依赖 `sha2`（已在锁文件的传递依赖里，无新增下载）。verify 全绿（core 覆盖率含新模块 100%）
- **16:20** refactor(core): 材料 CSV 导入改走 `csv` 库（T95 P1-b）—— 自写 `split(',')` 换成 `csv` 1.4（`has_headers` + `flexible` + `Trim::All`）：引号包裹、字段内逗号、字段内双引号转义、CRLF 与 Excel 导出的UTF-8 BOM 现在都能正确解析（BOM 由库自身剥离；旧实现会让表头首列带上不可见字符，用户看到「表头与约定列序不一致」而文件看着完全正常）。校验口径与中文错误文案不变（表头列名 / 列数 / 数值 / 属性表 / 填料列），列数校验仍由 `parse_csv_row` 给出中文原因而不是库的英文错误。消融显示两条错误映射助手在本输入形态下（&str + flexible）不可达 → 删掉，改用 `flatten()` / `unwrap_or_default()`，不留只读得到走不到的分支；同理空行跳过交给库、删掉自写的空行分支。新增 4 条用例（BOM+CRLF+引号内逗号、双引号转义、未闭合引号、损坏表头），既有 5 条用例文案口径不变。消融 3/3 红：库不认引号、表头校验放过列名不符、列数校验放过不足列。verify 全绿（core 覆盖率含 material.rs 100%）
- **16:45** refactor(web): 自动保存防抖改走 @vueuse/core（T95 P2-a 第一处）—— 前端引入 `@vueuse/core` 14.4（首个通用工具库；此前防抖写了 3 处、点击外部 2 处、storage 网关 62 行都是手写）：`stores/project.ts` 的自动保存定时器交给 `useDebounceFn`（`this` 透传存储实例），删掉自维护的 `autoSaveTimer` 与 clearTimeout 分支，对外语义不变（连续编辑只落盘一次、800 ms 后写最新工程；显式保存仍立即落盘）。既有 41 条 store 用例全绿且无需改动——防抖合并语义本来就有用例锁定；消融验证：把防抖换成立即落盘 → 该用例立刻变红。verify 全绿（前端覆盖率仍四维 100%）。剩余 P2-a 待换：错误自动消失定时器、网格估算防抖、两处点击外部关闭、storage 网关
- **17:05** docs(tasks): T95 P2-a 逐处判定（一处换库、两处自写保持）—— `@vueuse/core` 已引入：`stores/project.ts` 的自动保存防抖换 `useDebounceFn`（41 条用例无需改动全绿）；`useGeometryPanel` 的按几何防抖**回退自写**——换库后既有假定时器口径的用例立即失败（首个变更在执行窗口内就触发了一次估算），该处行为已被用例锁死、替换要改测试语义，收益不抵；`stores/app.ts` 的错误消失定时器**保持自写**（替换后行数不减，消息相等守卫仍要自己写，store 无作用域清理可白拿）。决策表按此更新为「逐处判定」并写明依据。verify 全绿
- **17:25** docs(tasks): T95 P2-a 判定完成（仅一处换库）—— 点击外部两处判定为**自写保持**并写明依据：`useMenuBar` 是「window 级 pointerdown + `[data-menu-root]` 属性选择器 + 同一对监听处理 Escape」，`useStudyTasks` 是「右键菜单状态 + 视口边缘夹取」，而 `onClickOutside` 的模型是「一个元素 ref + 一个回调」，套过来要新加 ref 与包装层、行数不减且语义更绕；`useStudyTasks` 里真正可引库的定位夹取（floating-ui）按「仅两处、逻辑小且有测试」留作备选。P2-a 结论：3 处防抖中 1 处换库（自动保存）、2 处自写保持；点击外部 2 处自写保持；storage 网关留待下一轮判定。下一步转 P2-b（统一数值/单位 formatter，45 处散落调用，纯收益、不涉及换库）
- **17:45** feat(web): 统一数值/单位格式化地基 utils/format.ts（T95 P2-b 第一步）—— 新增三条格式化：`fixed`（定点位数）、`significant`（[1e-3,1e5) 定点、超出转科学计数，用于同屏的温度与残差）、`lengthFromMm`（µm / mm / m 按量级换档）；非有限值一律给统一占位符「—」，不把 NaN/Infinity 漏到界面。7 条单测覆盖各分支（含零、负值、边界 1 mm / 1000 mm、非有限值），前端覆盖率四维仍 100%。此前同一量在不同面板的位数/单位/占位符不一致（toFixed/toExponential 共 45 处、散落 20 个文件、零测试）；调用点迁移按文件分批进行（每批随该面板既有用例一起改、单独提交），避免一次性大改把面板文案用例全部推翻
- **18:05** refactor(web): 结果面板数值改走统一 formatter（T95 P2-b 首批调用点迁移）—— `useResultsPanel` 三处提示行（标量 min/max、矢量首单元与模量范围、张量模量与主轴）从 `toFixed(3)` / `toExponential(2)` 换成`significant()`：同屏的 `0.001` 与 `3.742e-4` 现在按同一口径选表示法（常用量级定点、跨界转科学计数）。行为改善一处：空场不再把 `NaN` 渲染到界面，统一显示占位符「—」（对应用例改名为「空场按占位符渲染」）；矢量首单元分量由 `1.00e-3` 变为更可读的 `0.001`。21 条面板用例全绿（改 2 处断言，其余无需改动）。分批迁移的节奏确立：每批只动一个面板、随该面板用例一起改、单独提交。verify 全绿
- **18:25** refactor(web): 报告面板数值改走统一 formatter（T95 P2-b 第二批）—— `useReportPanel` 六处格式化：标量统计与网格体积换 `significant`、几何尺寸与网格质量（最长边比 / 纵横比）换 `fixed(…, 2)`、探针越界读值改用 `fixed(value, 4, "越界")`（占位符由 formatter 统一给，不再写 `?? "越界"`）；**时间序列的样本值保留 4 位定点**——迁移中一度改成 `significant` 导致精度从 4 位降到 3 位、既有用例立刻报错，说明那处是有意的高精度口径，回退为 `fixed(sample.value, 4)`（时间轴仍走 `significant`）。11 条报告面板用例无需改断言即全绿。verify 全绿
- **18:45** refactor(web): 几何面板数值改走统一 formatter（T95 P2-b 第三批）—— `useGeometryPanel` 六处展示格式化（体积走 `significant`；质量比 min/avg/max、纵横比 avg/max、网格尺寸行、匹配率、三条网格明细的厚度与均值走`fixed`，位数保持不变）。保留两处 `toPrecision(3)`：那是**表单初值**（几何最大边/20 的默认字符串），属输入而非展示，换 `fixed` 会改变字段默认值语义。20 条面板用例无需改断言即全绿。P2-b 累计三批：结果面板（改 2 条断言）、报告面板（0 条）、几何面板（0 条）。verify 全绿
- **19:05** refactor(web): 其余面板数值统一走 formatter（T95 P2-b 第四批，P2-b 收口）—— 一次扫完剩余展示点：日志页（质量比三项）、状态栏与作业面板 / 方案摘要 / XY 面板 / 结果时间列表（Time 值）、材料面板（填料质量分数、发泡三项百分比）、视口（相机中心、色标读数）、依赖面板（MB）、模具面板（覆盖 / 流动长 / 适配度 / 厚度）、工艺面板（浇口入口面积 / 等效直径 / 逐浇口面积与面积比）——全部换 `fixed` / `significant`，位数不变。**刻意未换**：bench 脚本（开发期工具，不进产物）与 `utils/chart.ts` 画布刻度（与自绘布局耦合，等 P3-a 换 uPlot 时一并处理）。665 条前端用例全绿、**零断言改动**（前三批共 15 处迁移也只改了 2 条）。P2-b 累计 4 批：结果 / 报告 / 几何 / 其余面板，共 30+ 处调用点统一到同一套位数与占位符口径。verify 全绿
- **19:35** refactor(core): 对称张量主方向改走 `nalgebra`（T95 P3-b 第一步）—— `results.rs::principal_axis` 自写的85 行 Jacobi 旋回（12 次扫描 + 自管正交性/收敛/符号）换成 `nalgebra::SymmetricEigen`（`default-features=false`），按 |λ| 最大取列、保留「绝对值最大的分量取正」的符号约定与退化（全零/非有限）零向量返回。34 条结果用例**零改动**全绿（说明原实现与库解在既有断言口径内一致），verify 全绿（core 覆盖率 100%）。消融 2/3 红：取 |λ| 最小、去掉非有限兜底；第三条（去掉符号规范化）未变红——补了一条覆盖 5 组混合符号输入的约定用例后**仍不可证伪**，说明该库当前始终给出正主导分量：规范化代码是未来换库/换输入形态时的守卫，约定用例锁的是契约而非某条分支，这一点已在用例注释里写明，不再假装它被消融覆盖。几何数值的后续（`[f64;3]` 工具在 6+ 文件重抄）另行推进
- **20:05** perf(bench): 换库的性能与体积实测（T95 P3-b 附）—— ①**性能**：新增基准 `benches/principal_axis.rs`，把迁移前的手写 Jacobi 实现作为对照保留在同一基准里，512 次典型张量调用 nalgebra 版 204 µs vs 手写版 259 µs（**快约 20%**），换库没有付性能代价；基准留在仓库里，后续再换实现可同台对比。②**体积**：同口径（tauri build）对比引入 csv / sha2 / nalgebra **之前**的包（app 内二进制 7,445,536 B）与之后（7,478,512 B）→ 三个库合计 **+32,976 B ≈ +0.44%**（Kairos.app 总体积 7.3 MB、dmg 4.4 MB）；前端 `@vueuse/core` 引入前后 dist 产物 532.04 kB → 533.03 kB（gzip 169.52 → 170.03 kB，**+1 kB 原始 / +0.5 kB gzip**，tree-shaking 生效）。结论：本轮的库引入在性能与包体上都不构成「顾此失彼」；后续每次大规模引库沿用同一口径（bench 对照 + 同口径包体对比）后再落库
- **20:40** refactor(core): vec3 收编第二批（T95 P3-b 续）—— `dualdomain` 与 `iges` 的本地 `distance_sq` 收进`services/vec3`（两处与共享实现逐字等价，纯替换）；**刻意不收**两处：「meshing」的 `cross3/dot3/sub` 取的是`&[f64;3]` 引用（共享模块按值传，改签名会牵动该文件十余处调用点，收益不抵本轮）、`thickness::unit_normal` 返回`Option`（含退化阈值判断）属产品口径，按「口径留各自模块」的准则保留。370 条 core 用例全绿、覆盖率 100%。至此 vec3 收编 4 个文件（gate_location / fill_preview / dualdomain / iges）
- **21:10** perf(bench): 二进制编解码基线（T95 P3-c 第一步）—— 新增 `benches/kf1_codec.rs`（10 万字段值 / 5 万节点网格），当前手写实现基线：字段编码 608 µs、字段解码 227 µs、网格编码 **2.22 ms**、网格解码 536 µs。**发现的瓶颈**：`mesh_store::encode` 按值逐个 `extend_from_slice(&value.to_le_bytes())`（每节点 3 次、每四面体 4 次小拷贝），而解码反而更快——批量转换（`bytemuck::cast_slice`，一次 memcpy）预计能显著改善这一段。换 `bytemuck` 时用同一基准对照；注意一处口径：`to_le_bytes` 是显式小端，`cast_slice` 用本机端序——三端目标（aarch64 / x86_64 / Apple Silicon）都是小端，换后需把「本机端序即小端」写进格式说明。本批只加基准不改实现，verify 全绿
- **21:35** docs(tasks): 把 vec3 剩余收编列为明确待办（不再按「收益不抵」搁置）—— 依维护性口径：同类向量运算散落多处时，口径改动成本随文件数线性上升，不能只按单次改动量判断。待办清单（每项独立提交 + 对拍）：①`meshing::{cross3,dot3,sub}` 取引用 → 改调用点按值并删本地实现；②`thickness::unit_normal` 的「退化 → None」判定上移为共享件 `vec3::unit_or_none(v, epsilon)`（阈值作参数），`thickness` 保留薄包装维持语义；③`dualdomain` / `midplane` 两份 Möller–Trumbore 与最近点搜索收编（收前逐字段对照容差）；④前端 `render/webgpu/volume.ts` 的 normalize/cross/sub 归到 `render/math.ts`。只搬实现、不改业务口径
- **22:05** refactor(core): meshing 的引用版向量函数收编（T95 统一收编第 1 项）—— 删掉 `meshing` 本地`cross3/dot3/sub`（按 `&[f64;3]` 取址），调用点改为按值调 `services::vec3`（`[f64;3]` 是 `Copy`，无需再取址），5 处调用点逐行精确替换、`[f64;3]` 语义完全不变。过程中一次「整文件正则替换 `, &` → `, `」误伤了无关借用（把 `fill_row(&crossings)` 之类的正当借用也剥掉了）——已 `git checkout` 回退改用逐行断言替换，5 处调用点每处先校验原文本再改，避免同类误伤。370 条 core 用例全绿、覆盖率 100%。至此 vec3 收编 5 个文件，T95 统一收编清单剩：thickness::unit_normal、dualdomain/midplane 的相交与最近点、前端 webgpu/volume
- **22:30** refactor(core): 退化法向判定上移为共享件（T95 统一收编第 2 项）—— `vec3` 新增`unit_or_none(vector, epsilon)`：**阈值作参数**，公共模块只给判据、不替调用方定口径（几何里「零长度」的判定阈值按网格边长 / 浇口尺度 / 模型单位各不相同）。`thickness::unit_normal` 改为一行薄包装（`unit_or_none(cross(sub(b,a), sub(c,a)), 0.0)`），保留原签名与 `Option` 语义，调用点（退化时回退 z 轴）不变。新增用例锁「同一向量在不同阈值下结论不同」，防止有人把阈值写死在共享件里。371 条 core 用例全绿、覆盖率 100%。统一收编清单剩：dualdomain/midplane 的 Möller–Trumbore 与最近点（收前对照容差）、前端 webgpu/volume
- **23:00** refactor(web): 前端向量函数收编 + 作废一条不实的重复项（T95 统一收编第 3 项，收尾）—— ①`render/webgpu/volume.ts` 本地 `normalize/cross/sub` 删除，改从 `render/math` 引入（两处 `normalize` 的零向量语义已逐行核对等价：都返回 `[0,0,0]`）；②**核实后作废一条**：清单原写「`dualdomain` / `midplane` 各一份Möller–Trumbore 与最近点搜索」，实际 `midplane` 是 `use crate::services::dualdomain::{nearest_node, normalized_normal, TriangleGrid, weld_surface}`，只有一份实现；`repair::triangles_intersect` 是三角形-三角形相交（自交检测），与射线-三角形不是同一运算——该条来自盘点阶段的推断、当时未核实，已在决策表标注作废（教训：收编清单里的每一条在动手前都要先核实，否则会为不存在的问题写代码）。至此 vec3 统一收编 6 个文件，清单执行完毕。665 条前端用例全绿。verify 全绿
- **23:25** test(abi): vec3 共享件消融 5/5 全红 —— 逐条改坏共享原语，确认消费方用例真的在盯：①`cross` 分量顺序错（右手系变左手系）红；②`dot` 少一项红；③`distance_sq` 退化成逐轴求和红；④`normalized` 零向量分支返回单位向量（凭空造出方向）红；⑤`unit_or_none` 忽略调用方阈值、写死 0.0 红——这条正是「阈值必须由调用方给」的守卫，它红了说明该约定有用例兜底。消融脚本改坏后即还原，仓库无残留。附带说明：前端 `render/webgpu/volume.ts` 收编到`render/math` 的那三处**无消融**——`render/` 属薄适配层、不在前端覆盖率门槛内，也没有直接单测（这是已知盲区，记在 T95 里而不是假装被覆盖）
- **23:55** perf(core): 网格编码改批量转换，2.22 ms → 163 µs（T95 P3-c 替换本体，x13.6）—— `mesh_store::encode` 从「逐值 `to_le_bytes` + `extend_from_slice`」（每节点 3 次、每四面体 4 次小拷贝）改为：节点用 `bytemuck::cast_slice::<[f64;3], u8>` 一次 memcpy，索引先在连续 `Vec<u32>` 里铺好再整体写入。同一基准（10 万字段值 / 5 万节点网格）对照：网格编码 **2223 µs → 163 µs**；解码未动（536 → 496 µs，属测量抖动）。371 条 core 用例全绿（含编解码往返与越界拒绝）。**端序前提写进代码**：`cast_slice` 用本机端序、格式规定小端，三端目标都是小端；将来支持大端平台要改回逐值 `to_le_bytes`——这是换 bytemuck 时唯一的口径变化，已就地注明。`field_binary` 暂未跟进（它的载荷含 JSON 头、且当前 608 µs 编码并非瓶颈），留待有实测需要时再做
- **00:20** docs(tasks): 解压库化判定为「核实后不改」，P3-d 收口 —— 核实三点：①真实资产是 `.tar.xz`（moldingFoam bundle）与 `.tgz`（gmsh），换库必须处理 **xz**——`xz2` 是 liblzma 的 C 绑定（Windows / 交叉编译复杂化）、`lzma-rs` 纯 Rust 但明显更慢，为百 MB 级 bundle 换一条更慢的解压路径没有实测收益；②原先的动机「Windows / Linux 不保证有解压工具」不成立：macOS 自带 bsdtar、Linux 基座自带 GNU tar，Windows 10+ 系统自带 bsdtar（现有代码已有该分支）；③**重审触发条件**已写明：若将来要给 Linux 发 zip-only 资产（`unzip` 在最小发行版可能缺失），再按「先量再换」评估。结论：不改代码，避免为一个不存在的问题引入 C 依赖。T95 剩余仅 P3-a（图表换 uPlot，需视觉对照）
- **01:05** feat(runners): 单元介质数据位 `RunnerElement.medium`（T89 第一步）—— 模具网络的单元可标注介质（`melt` 缺省 / `gas` 注气通道）：Rust 侧新增 `RunnerMedium` 枚举与 `Option<RunnerMedium>` 字段（`serde(default)`，旧工程文件无此字段读为 None＝熔体，不报错）；TS 镜像加 `RunnerMedium` 类型与**可选**字段（`medium?: RunnerMedium | null`）——镜像做成可选是与 Rust 的 `Option` + default 对齐，做成必填会迫使十几个既有夹具补空值、且歪曲「旧文件没有这个字段」的事实（这条中途改错过一次，已改回）。核心用例锁两件事：气体介质随工程文件往返保真、旧文件缺字段解析为 None。中途两处踩坑记录：①`cargo check` 默认不编译测试目标，按报错行号批量补字段的脚本必须先加 `--all-targets`，否则一直「无错误」却补不上；②按行号插入字段时用「函数签名行」当锚点会命中 `fn … -> RunnerElement {`，把字段插到函数体末尾——最终按字面量的闭合括号定位才正确。第二步（气体相定义 / 入口 BC / 气芯追踪）仍等上游。verify 全绿
- **02:10** feat(runners): 浇口与流道截面匹配校验（T83 范围 4 之一）—— 扩展既有 `check_mold_network`（**没有另开函数**：它已覆盖单元参数与连通性，我最初写的 `network_warnings` 因此回退），新增「浇口直径 > 所在流道直径」提示（截面先收缩再放大 → 填充会先在这里失速）；用例四条覆盖正常超限 / 从流道另一端接入 / 截面不超限 / 孤立浇口不误报。过程中两处工具链教训：①**verify 的覆盖率结论才是准的**——它的脚本会自己解析 LLVM 工具（rustup 工具链或系统 LLVM），而我手动跑的 `cargo llvm-cov` 走的是 Homebrew rustc（其 bin 目录只有 rust-objcopy、没有 llvm-profdata），失败后复用了旧 profdata，报出「100%」的假象，误导了一轮判断；②断言里写内联闭包（`issues.iter().any(|i| i.contains(..))`）在行/函数覆盖口径下会被算成未执行，导致 1 行 1 函数未覆盖——收进测试模块的命名助手 `has()` 后恢复 100%，这类「假未覆盖」不要靠加 `#[allow]` 掩盖。verify 全绿
- **02:40** docs(tasks): T83 拆解与上游依赖核实（状态改为部分实现）—— 核实发现流道单元**已有下游消费者**：双域网格把它建成一维梁（`dualdomain` 消费 `RunnerKind::Runner`），因此范围 1「流道几何进网格」与范围 2「多腔分配」、范围 3 的流道部分全都撞到**同一个上游口径问题**（与 T82 的降维问题同源）：moldingFoam 能不能消费一维流道表示、多腔流量分配由求解器自动分配还是 case 给权重。已在任务档案写成「进度与依赖」表 + 两个待上游确认的问题（与 T82 合并提问），并声明在上游答复前只做数据位与校验类工作、不做 case 契约的推测性扩展。范围 4（连通性 + 截面匹配）已完成：连通性由既有 `check_mold_network` 覆盖，截面匹配上一提交补入。另同步：范围 3 的浇口部分已完成（`gate_portals` → inlet patch，含面积/等效直径回显）
- **03:05** feat(core): 报告 PPTX 导出落地（T60 PPTX 那一半，core 侧）—— 候选库核实后选 `ppt-rs` 0.2（Apache-2.0；另两个候选 `pptx` MIT / `pptxboss-write` MIT OR Apache-2.0 也合可，按 API 简洁度选它）：新增 `services/report_pptx.rs::build_report_deck`（标题 + 逐页要点 → 演示文稿字节流，空章节报 validation 错误），命令层与前端尚未接线（下一步）。**中文保真已用产物证明**：用例把生成的 deck 当 zip 打开、读 `ppt/slides/slide1.xml`，断言中文标题与要点原样出现（不是转义或乱码）——这条是 T60 档案里「核候选库中文排版覆盖」的答案。**包体实测**（按引库准入条款）：同口径 `cargo build --release -p kairos` 6,603,392 → 6,619,936 字节，**+16,544 字节（+0.25%）**。又遇到一次「假未覆盖」：`map_err` 内联闭包只会在库报错时执行、行覆盖记成未覆盖，依旧抽成命名函数 `deck_error` 并直接测它（不加 allow 掩盖）。verify 全绿（core 覆盖率 100%）
- **03:35** feat(tauri): 报告 PPTX 导出命令（T60 接线第一步）—— 新增 `save_report_pptx_to_workspace`（`project_path` + `file_name` + 标题 + 逐页要点 → core 生成 → 落工作区 `reports/`，文件名清洗与扩展名固定走既有路径工具），与 HTML 报告同口径：散装工程明确报错、重复导出互不覆盖（扩展名不同）；命令已在 `lib.rs` 注册。**前端那一半我撤了**：先加了 `api/project.ts` 的调用函数，knip 立刻报「导出但无人使用」——这条门禁其实是对的，半接线（有 API 没入口）等于死代码；因此前端入口与它的按钮/用例合并到下一批一起做，不留悬空导出。verify 全绿
- **04:20** feat(web): 报告面板 PPTX 导出入口（T60 接线完成）—— 面板新增「导出 PPTX」按钮，composable 新增 `exportPptx()`：与 HTML 报告同源数据组装要点（第 1 页工况参数、有几何时第 2 页网格与结果），经 `saveReportPptxToWorkspace` 落工作区 `reports/`，状态栏回显落盘路径；缺项目/方案/工程路径时给提示且不发起调用。前端 `api/project.ts` 同步补上调用函数（上一批因 knip 报死代码而撤回，这回有真实入口）。测试加到 16 条：命令参数、网格页、失败进全局错误、三条件中间态、以及 process 非空与各类回退文案。**过程中被覆盖率门禁连着拦了三次**——先是行覆盖（几何页与 catch 分支没走到），后是分支覆盖（`?? / || / 三元` 的回退侧、以及 `process` 非空那一侧恰好没被测到）。每次都补真实用例而不是加 ignore：这些回退文案正是用户会看到的界面文字，覆盖它们等于把「未登记 / 未设置」这类提示钉住。verify 全绿（前端四维 100%）
- **05:05** feat(core): PPTX 幻灯片支持插图（T60 图片那一半，core 侧）—— 核实库能力（读 vendored 源码，而不是猜文档）：`SlideContent` 有 `images: Vec<Image>` 与 `with_images()`、`Image::from_bytes(bytes, w, h, fmt)`。core 侧新增 `ReportImage`（字节 + 像素 + 格式）与等比缩放放置：快照常见 1200×800 起，原始尺寸 11,430,000 EMU 会溢出 10×7.5 英寸版面，因此按可用区（7×4 英寸）等比缩放并水平居中；用例断言「媒体部件存在 + 该页引用 r:embed + 不再是原始宽度」——具体 EMU 取整由库决定，不锁死数字（否则测试会随库的小版本变动而假红）。命令层目前显式留空图片（载荷接线：前端 dataURL → 解码字节，下一批），不为半成品设计字段。verify 全绿
- **05:50** feat(core): 快照 dataURL → deck 插图（T60 图片接线，core 侧）—— 新增 `image_from_data_url`：解 base64、按 PNG 签名 + IHDR / JPEG SOFn 扫描读尺寸（**尺寸不信前端给的数字**，前端只说「哪张图」，排版参数由导出侧定），不支持 PNG/JPEG 时明确报错。8 条用例覆盖 PNG 与 JPEG 两条路径、非 dataURL 形态、不支持的格式、非 base64、非法 base64、头部不完整、JPEG 跳过非标记字节与「走完没有 SOF」。**覆盖率又拦了三次**，都是同一类口径假象：`?` 算子的错误落点被算成未执行——改为显式长度检查 + 按索引取字节（无 Result 转换）后归零；这已是本站第四种同源假象（内联闭包 → `map_err` 闭包 → `?` 落点），统一应对都是「改写成无隐藏落点的写法或抽命名函数」，而不是加 allow。verify 全绿（core 覆盖率 100%）。命令层与前端载荷接线是下一批（把快照 dataURL 放进 slides）
- **06:25** feat(report): 快照进 PPTX（T60 图片接线完成，T60 收口）—— 命令层载荷新增 `images: Vec<String>`（dataURL，`serde(default)` 旧调用方不受影响），逐张经 core 的 `image_from_data_url` 解码；前端 `exportPptx` 收集视口 / XY 曲线快照，有图就补一页「快照」并把 dataURL 一起交给命令层。顺带给快照注册表补 `unregisterSnapshot`（视口卸载时本就该注销，否则导出会拿到已销毁的画布；测试里也用它覆盖「无快照」分支）。**又两次被覆盖率拦下**：先是「有一张快照」的偏态分支——我先把内联三元换成 `labels` 逐步 push 的写法（无隐藏分支），而不是为它专门造一个偏态用例；再是一次替换因 prettier 换行没命中、改了没生效，靠核对源码发现。报告面板 17 条用例、前端四维 100%、verify 全绿。T60 至此收口：导入日志流 + HTML 报告 + PPTX（core 生成 / 命令层落盘 / 面板入口 / 插图）
- **06:50** docs(reviews): 汇总上游问题清单 `ai-docs/reviews/upstream-questions.md` —— 把 Kairos 侧被上游能力或发版节奏阻塞的六件事整理成一页，每条带**我们已经做过的验证与证据**，便于上游直接回答：①046 修复的发版时间与版本可预期性（附我们都做过 np=1/2/4 对照与 gdb 抓到的 rank 停滞证据）；②一维流道/降维网格的消费口径（T82+T83 合并提问，附双域/中面两种表达现状）；③多腔流量分配的归属（求解器自动 or case 给权重，附我们已经落好的独立 inlet patch 能力）；④release 是否提供 SHA256SUMS（附我们已把摘要落进清单的现状）；⑤求解结束后析构阶段 malloc 告警的处置（附我们以「跑到 endTime + 输出完整」判定的绕行方式）；⑥GAIM 气芯与取向张量写出的路线图（附已落地的数据位：介质标记已往返保真、对称张量 6 分量通道已通）。任务清单顶部加指向链接
- **07:40** test(e2e): v1.0.0 上复测 np=4，046 修复在我们的链路确认 —— 上游发布 v1.0.0（含 046 并行死锁修复；上游真实件 np4 → 5069 步到 endTime、1/2/4 进程一致）。Kairos 侧确认消费正常：VM 内 `.kairos-release-tag` = `v1.0.0`、`openfoam14/etc/bashrc` 布局与常量一致，下载/部署链路无需改动。复测走完整链路（CLI 生成 `--cores 4` case → transfer → `setsid` 脱离会话 → `mpirun -np 4 foamRun`）：退出码 **0**、**1389 步到 endTime 2 s**、无 `FOAM FATAL`、V/P 切换 0.9624 @ t=1.0419 s、质量预算残差 −7.2e-07 kg。上游三项工程建议（模板值按件选档、体素/dt 口径、037 退出崩溃排查边界）与契约 v1.29 变更整理进 upstream-questions.md（§7–§10 + §0 更新记录），第 1 条关闭。本地真实件目录已不完整，本地 np4 端到端待拿到后补
- **08:10** feat(deps): 下载改用官方摘要校验（T95 P1-a 完成，供应链完整性闭环）—— 上游 v1.0.0 的 release 资产带 GitHub `digest` 字段（`sha256:<hex>`，`gh api` 可读、发行页面可见）：core 新增 `parse_sha256_digest`（只认 sha256、大小写归一、位数与非十六进制一律视为「无可比对值」），下载流程解析出的资产带上该字段，**下载后逐字节比对：不一致即删除文件并报错**，不留「看起来成功」的坏包。核验：本机已安装的 v1.0.0 arm64 包 sha256 = `ecbd8b09a20879bf7b7866ee0e7c039b3d8276d9163a7769c9cb6c34c8525bc1`，与官方公布值一致；上游问题清单 §4 关闭（不必再请上游附 SHA256SUMS 文件）。verify 全绿
- **08:45** docs(reviews): 给上游的回复草稿（核对上游建议，标出 5 处问题）—— 逐条核对其 2026-09-14 建议后，列出「结论与证据不匹配」的地方：①**037 的结论链不成立**（重建库仍崩只排除打包差异、不排除源码；给出我们的反例：v1.0.0 + np4 + 5000 四面体 case 1389 步到 End、退出码 0 未复现；要求按 `MALLOC_CHECK_=3` + gdb 取栈，并明确「End 之后崩溃时输出是否完整」）；②**dt 定标指数与量纲不符**（2× 加密 → dt ÷8 意味着 dt ∝ Δx³，与对流 Δx / 扩散 Δx² 都对不上，要求说明 dt 定义——我们要用它换算门禁余量）；③**模板值两组建议互相冲突**（§7 以 6/2/0.1 为基线 vs §10 契约 v1.29 是 8/1/0.015，且 8 子循环下 §7 的 (6,2) vs (6,1) 结论不能套用，要求唯一目标值集合）；④**MULESCorr 「禁用」范围不明**（改的是求解行为不是模板值，需说明实测条件）；⑤仍有两条未答复（一维流道消费口径、多腔流量分配归属）。同时列出「已确认无误、我们照做」的部分（含 v1.0.0 含 046 的核对：tag 领先 main `9198af5` 13 个提交、落后 0）。草稿可直接贴到 issue / release 讨论
- **09:30** fix(ci): 修 Windows 覆盖率门禁（digest.rs 错误映射按平台漂移）—— Windows CI 挂在 `coverage:rust`：`services/digest.rs` 1 行 1 函数未覆盖（97.12% 区域 / 87.5% 函数）。根因是我给「读取错误」写的那条用例依赖 「目录能打开、读取才失败」（unix 的 EISDIR）：Windows 上打开目录直接拒绝访问，于是**读取阶段的错误闭包永不执行** → 该平台的覆盖率必然差一行。修法：把打开与读取两处 `map_err` 闭包合成一个命名函数 `read_failed`，两个平台都会覆盖同一份代码（macOS 走 EISDIR、Windows 走打开失败），并把「为什么必须合并」写进文档注释。这是本站第五次处理「隐藏落点」类假未覆盖，也是第一次**跨平台**触发——教训升级为：错误映射不要按调用点分写，否则覆盖率会随平台漂移。verify 全绿
- **10:20** test(e2e): T29 收尾——大规模件 np4 全链路（v1.0.0）—— 本地真实件 STL 已不在（只剩 `.kairos`），改用可复现的最大规模件：样例几何体素加密到 0.4 mm（**17,576 节点 / 78,125 四面体**，`numberOfSubdomains 4`）。全链路：CLI 生成 → `multipass transfer` → `setsid` 脱离会话 → `mpirun -np 4 foamRun` → 结果回传 → CLI 读场。结果：**退出码 0、2220 步到 endTime 2 s、无 FATAL**，质量预算残差 3.93e-07 kg，21 个时间目录回传，`results dump --time 2 --field T` 读出 384.7 / 462.9 K。真实件本体多进程证据以上游复测为准（np4 5069 步、1/2/4 进程一致）；Kairos 侧验收口径定为「同规模 + 全链路」。T29 至此闭环：三条链路 + GUI 作业生命周期（T94）+ 4 进程并行（v1.0.0）+ 收尾大件端到端
- **10:35** docs(tasks): T29 状态收口为「已闭环（v1.0.0）」—— 任务档案阶段行与任务清单同步；余项只剩视口截图 / XY 探针核验（受会话输入通道限制，属 T94）与本地真实件复测（原档已失，验收口径已改为「同规模 + 全链路」）
- **11:10** feat(core): DOE 矩阵与批次汇总表（T92 第一步，T29 闭环后解锁）—— 新增 `services/doe.rs`：①矩阵生成——正交表 **L9(3^4)**（9 行、≤4 因子、每因子恰好 3 水平，形态不符明确报错而不是用前 3 个水平凑合）与**全因子**（≤3 维、上限 64 次）；②批次记录 `DoeRun`（参数 + 状态 + 指标 + 耗时，状态含 `Failed(原因)`）；③汇总表 `summary_csv` / `summary_json`——列顺序在整批内固定、缺值留空但不丢列（错位比空值更危险）、失败行的原因如实落表、JSON 对用户输入做转义、未执行写 `null` 而非 0。6 条用例覆盖正交表平衡性、形态拒绝（因子过多 / 水平数不对 / 空水平）、全因子组合完备与双向上限、CSV 列对齐与失败标记、JSON 转义。求解执行编排（串行提交 case + 指标提取 + 落盘）是下一批。verify 全绿
- **11:50** feat(core): 求解日志指标提取（T92 范围 3）—— `moldingfoam::parse_metrics` 从求解日志读汇总表要的指标列：填充时间 / V/P 切换填充率 / V/P 切换压力（同一条 `V/P switch:` 行）/ 质量预算残差。只认求解器打印的结构化行，**缺行就缺指标**（不猜、不填零）；同一指标多行时取最后一行（重跑 / 分阶段输出以最终结论为准）；结构对但数字坏时该指标缺失而不是 panic。指标名导出为常量（`METRIC_*`），与 DOE 汇总表列名同源。用例用真实运行日志片段（v1.0.0 / np4 / 78,125 四面体件）断言四个指标值。verify 全绿
- **12:20** docs: ai-docs 整理（链接体检 + 索引补全 + 陈旧结论刷新）—— ①机械体检 159 篇文档的内部链接，修掉唯一一处真断链（`tasks/T21` 引用评审时漏了 `../`）；②**26 篇评审 / 预研 / 决策文档此前没有任何索引引用**（孤儿文档），新增 `reviews/README.md`（按「里程碑评审 / 整体审计 / 真实求解链路 / 上游往来 / 专项 / 任务评审」分类）、`research/README.md`、`decisions/README.md`，并在 AGENTS.md 路由表补三行指向；③刷新陈旧结论：`e2e-solve-report.md` 里 5 处「上游已修**未发版**」与「v0.2.5 轮」更新为 v1.0.0 已发版 + 我们复测通过（退出码 0、2220 步到 endTime、结果回传 + 读场）。体检脚本可复用：扫 `[..](..)` 校验目标存在 + 统计未被引用文档
- **12:50** docs: 文档体检脚本固化 + 查重结论 —— ①新增 `scripts/docs-check.mts`（相对链接必须存在；「文件名未出现在任何文档正文」判为孤儿）并挂到 `bun run docs:check`，当前 162 篇：**断链 0、孤儿 0**；②机械查重（跨文档连续 3 行块）只命中一处：T86/T87/T88 三份任务评审共享 T21 的八项清单表头与相同结论措辞——这是评审证据的结构（逐项回填），**有意保留**，已在 reviews/README.md 写明，避免下次整理误删；③三处状态面（能力视图 / 任务视图 / 时间线）在 tasks/README.md 顶部注明分工与不一致时的处置口径
- **13:20** feat(core): DOE 批量编排要素（T92 范围 2 的一半）—— 新增运行目录约定与状态回填：`run_dir_name`（`run-001` 三位补零，可读可排序）、`run_case_dir`（`<工作区>/cases/<方案 id>/run-XXX`——**每次运行独立目录是硬要求**：共用目录会让上一次的时间目录被当成这一次的结果）、`batch_dir`（`<工作区>/doe/<批次名>`）、`mark_done`（指标 + 耗时一起写）、`mark_failed`（原因必填，不静默丢弃失败点）。用例断言布局与状态迁移，并锁一条容易忽略的语义：**失败行不继承上一次的指标**——否则汇总表会把别人的指标算在失败点上。CLI 串行执行入口是下一批。verify 全绿
- **13:55** feat(cli): `doe matrix` 子命令（T92 范围 2 的入口）—— `kairos-cli doe matrix --factor "熔体温度=200,210,220" --factor "保压压力=50,60,70" [--plan orthogonal|full] [--out-dir <工作区>] [--batch <名>]`：生成参数矩阵、默认把 `summary.csv` + `summary.json` 落到 `<工作区>/doe/<批次名>/`（批次名缺省由因子名连接），stdout 仍输出表（`--json` 供脚本消费）。真机演练：两因子 L9 → 9 次运行、批次目录双格式落盘；`--plan full` → 4 次运行；错误路径给出可读中文原因（L9 要求每因子 3 水平、因子缺 `=`、水平非数字、空水平）。求解执行循环（逐次生成 case → 提交 → 提取指标 → 回填状态）是 T92 最后一块
- **14:25** feat(core): DOE 因子 → 工艺参数映射（T92 执行循环的关键一环）—— `doe::apply_factors` 把一次运行的参数写进 `ProcessSettings`（其余字段沿用基准，不能因为一次运行清空其他参数）：熔体/模具/顶出温度、注射时间、V/P 切换、保压压力（**曲线**——单值因子落成「从 0 起恒定」的曲线）、保压/冷却时间、介质温度，共 9 个白名单因子（`FACTOR_NAMES` 导出）。**未知因子名必须报错**：静默忽略会让整批运行参数完全一样，而汇总表看起来「跑过了」——这类错误不会报错、只会让人拿错结论；非有限水平（NaN）同样拒绝，避免写进 case 字典。用例锁这三条语义。剩余：CLI 端把这些接起来跑循环（逐 run 生成 case → 提交 → parse_metrics → 回填 → 重写汇总表）
- **15:30** feat(cli): `doe run` 串行执行整批（T92 收口）—— 网格与几何只准备一次（矩阵只换工艺参数），逐次把该次运行的参数经 `doe::apply_factors` 落进独立 case（`cases/doe/run-XXX/`），可选 `--solve` 真跑；每次运行后**立刻重写** `summary.csv` / `summary.json`（中断也留下已完成的行）。真机演练：两因子 L9 → 9 次运行、9 个独立 case 目录、汇总表逐次刷新；`--solve` 在本机（无 OpenFOAM）逐次记为 failed 且**批次继续**，原因是 `求解失败：bash: decomposePar: command not found`。**顺带修掉两个真缺陷**：①重定向优先级——`cd … && A && B; C > log 2>&1` 里的 `>` 只绑定最后一条命令（C），A 的报错根本没进日志，因此把原因报成了 reconstructPar 的错；改为分组 `( … ) > log 2>&1`；同一处坑在既有 `solve submit` 里也存在，已改为共用 `run_solver` 助手；②失败判定改为**日志标记优先于退出码**（`; reconstructPar` 会掩盖求解失败，与桌面端 `job_failure` 同口径）。core 新增 `moldingfoam::failure_reason`（两遍扫描：强特征 FOAM FATAL / not found 优先，再弱特征，最后才用最后一行兜底——用户看到的失败原因必须是可诊断的那条）。verify 全绿
- **16:20** feat(core): 工艺参数寻优（T93 第一步）—— 新增 `services/optimize.rs`：**目标函数**（填充时间最短 / V/P 切换压力最低，指标名与 `parse_metrics` 同源）；**约束承载**（`Evaluation.violations`，违反约束的解不算可行、不参与最优评选）；**搜索**（粗搜等分网格 → 围绕当前最优做局部细化，每轮步长减半、贴边不越界、最多 4 轮）；**失败策略**（无指标 = 不可行但进入历史用于计数，同一候选重试上限 2 次，连续 3 次无指标即中止并**报出失败参数点**）。边界划分：优化器只做决策不跑求解，调用方把候选交给 DOE 批次执行、把指标回灌——因此搜索策略可用合成目标函数完整覆盖（含一条二次目标的寻优用例：从 5 点粗搜收缩到最优点 ±0.5 内）。**覆盖率拦下一处真 bug**：原实现把「无指标的失败评估」也视作改进去更新细化中心（下一轮会围着一次失败去细化），改为**中心只跟随全局最优**。9 条用例、core 覆盖率 100%。CLI 接线（寻优循环调用 doe run）待做
- **17:05** docs: 经验沉淀（「等我方排期」第 3 项）—— 把本轮反复踩到的东西写成规范而不是留在记忆里：①**覆盖率「假未覆盖」五种形态与应对**（断言内联闭包 / `map_err` 闭包 / `?` 落点 / 单侧偏态分支 / **跨平台语义差异**）归入 ARCHITECTURE §6，并明确两条纪律：不用 `allow` 掩盖、同一语义的错误映射只写一处；同时写明覆盖率结论以 `verify` 的脚本为准（手动 cargo llvm-cov 可能复用旧 profdata 报假绿灯）；②**引入依赖准入**（许可 + 性能 + 包体，附本轮实测：nalgebra 快 20%、bytemuck 让网格编码 2.22 ms→163 µs、三库 +0.44% 包体、vueuse 只 +1 kB，且要求旧实现留在基准里同台对照）；③**批次性改动的工作方式**（清单先核实存在性——两次教训；不用重复片段当锚点；迁移分批提交）。AGENTS.md 速查区同步三条纪律
- **18:40** refactor(core): 长函数拆分（「等我方排期」第 4 项）—— ①`repair::count_self_intersections` 125 → 40 行：把函数内嵌套的 `Box3` / `overlap`(→`overlaps`) / `triangles_intersect` 提到模块级（纯搬运，逻辑不变）；②`midplane::generate` 拆出 `pair_mid_nodes`（顶点沿法向配对对面，返回中面节点与配对表）与 `couple_beams`（梁端点捕捉最近中面节点、失败端成自由节点并复用重合点），主函数只留编排；③`moldingfoam::generate_case` 拆出 `collect_faces`（面表收集）与 `describe_faces`（外法向定向 / 中心 / 面积 / z 分量 / lexicographic 行）——后者是本文件最易出错的一段（定向 + 下标轮转 + 面积符号三处必须同时对），单独成函数并配文档注释。三处均为纯搬运：core 用例全绿、覆盖率 100%。**仍超长但不属本项范围**（记入待办）：`write_poly_mesh`（122）、`kairos-cli` 两处（452 / 709）、`src-tauri/lib.rs::run`（214，应用装配）
- **23:31** fix(jobs): 宿主命令改按退出码判成败（tar 警告不再误报「case 解压失败」）—— 报错原文 `case 解压进虚拟机失败：tar: Ignoring unknown extended header keyword 'LIBARCHIVE.xattr.com.apple.provenance'` 是**误报**：宿主是 macOS 的 bsdtar（libarchive），系统给每个文件挂的 `com.apple.provenance` 被写成 pax 扩展头，guest 的 GNU tar 1.35 不认识 `LIBARCHIVE.*` 前缀、逐成员打一行警告，但**照常解压、退出码 0**；而 `run_host` 在 `wait_exec` 已确认成功之后仍拿 stderr 首行当失败原因，作业随即被判失败、求解根本没启动。实测（真实 case，26 个成员带该属性）：宿主打包 → transfer → VM 解压 = exit 0、case 树完整、stderr 26 行警告。改为**只看退出码**（失败时才拿 stderr 首行当原因），并把 stderr 与等待**并行抽干**——先等后读在输出超过管道缓冲时互锁。顺带修归档卫生：打包加 `COPYFILE_DISABLE=1` + `--no-xattrs`（两个开关各管一半：前者去 `._*` 影子文件、后者去 xattr pax 头），改后同一 case 归档里 `._*` 从 26 个降到 0、guest 侧 stderr 0 字节。新增用例直接锁语义：exit 0 + stderr 有警告 = 成功，exit 3 = 失败且原因是 stderr 首行。verify 全绿
- **23:34** fix(vm): 虚拟机停机时「已部署版本」不再退回「未部署」—— 症状：每次部署完仍被提醒「求解环境有更新未部署：v1.0.0 → VM 内 未部署，请部署到虚拟机后提交作业」。根因是已部署版本只在 **VM 处于 Running** 时才读得到——读标记要走 `multipass exec`，而 exec 对停止实例会**隐式拉起**十几 GB 的虚拟机，所以停机时直接返回 null；偏偏作业跑完与应用退出都会关掉虚拟机（省内存）→ 停机即显示「未部署」→ 用户重新部署 120MB → 再停机 → 再提醒。改为①**本机记录**（应用数据目录 `vm-deployed-tag`）：部署成功即落盘，VM 可达时以 VM 内标记为**唯一事实来源**并同步刷新记录（标记没了 → 记录一并清掉，不拿旧标签充数），不可达（停机 / 启动中 / 状态未知）时回落记录；②标记回读加**哨兵** `KAIROS-TAG:`——multipass 隐式拉起实例时会把转圈动画写在 **stdout**（`\x08` 重绘），原先直接 trim 会把噪声当版本标签；③读写命令收口到 core（`vm_env_tag_path` 的波浪号保持不加引号，写入对标记值做单引号转义）。前端：作业面板挂载时只在「还没有读数」的情况下补读一次，不覆盖已有读数。真机验证：停机的虚拟机里读标记 = exit 0 / stderr 0 字节 / 末尾 `KAIROS-TAG:v1.0.0`，标记缺失 → 哨兵后为空 → 未部署；该 case 在 VM 内 decomposePar 与 foamRun 正常推进（75 s 跑到 t=0.49 s / endTime 3 s）。verify 全绿
- **23:47** refactor(vm): VM 命令构造与执行收口（实例名 / 环境路径 / 执行形状只留一处）—— 动因是同一条 `multipass exec <实例> -- bash -lc <脚本>` 的形状在命令层与作业层被手抄了 7 处（其中 5 处直接写死字面量 `"kairos"`），`~/moldingfoam-env/...` 也在多处各自 `format!` 拼，实例名 / bundle 布局 / 发行版名要改就得全仓库找。**core 新增**：`bash_script_args(provider, script)`（实例名 / 发行版名 / `bash -lc` 的唯一出处）；环境路径一族 `env_path` + `vm_bundle_archive_path` + `vm_transfer_endpoint` + `vm_bundle_transfer_target` + `env_deploy_command`（建目录 → 解压 → 校验 bashrc，从命令层搬进 core）；`vm_results_list_command`（回传清单，与打包命令共用单引号转义）；`apt_mirror_command` / `openmpi_install_command`（从命令层搬进 core，镜像主机与云镜像下载共用 `CN_MIRROR_HOST`）；`parse_env_tag`（VM 标记 / 本机记录 / 原生标记三处读回统一归一）；`transfer_args` 补上 `args[0]`（与其它 `*args` 同构，调用点不再自己拼 `multipass`）。**命令层**：`host_command`（参数表 + GUI 进程的 PATH 修补，作业层复用）、`run_quiet`（6 处 `status().map_err(..)` + 判成功的样板收敛成一行）、`stream_step`（`vm_start` 三处）、`forward_output` 两平台共用清洗与空行过滤、`read_tag_file`（原生标记与本机记录共用）；`vm_deploy_bundle` 与 `vm_start` 的分支因此各减一半。**作业层**：复制进 VM / 结果回传 / 求解启动 / 日志回读 / 原生执行共 6 处改为「core 参数表 + `host_command`」，手写的单引号转义删掉（统一走 core 的 `bash_single_quote`）。**验证**：core 用例锁生成命令的原文（含含引号路径的转义）；真机跑重构后的部署命令 = exit 0（11.6 s，bashrc 与 foamRun 就位、标记仍 v1.0.0）、case 打包 → 传输 → 解压 stderr 0 字节、结果清单 → 打包 → 回传 → 宿主解压文件齐全。core 覆盖率 100%、verify 全绿
- **23:55** refactor(vm): `install_args` 改用 `Option` 表达「原生环境没有安装步骤」—— 原先对 `VmProviderKind::Native` 返回**空表**，而 `start_args` / `ready_probe_args` 都用 `None` 表达同一语义；空表是个雷：任何调用点只要没先判 provider，取 `args[0]` 就越界 panic（GUI 进程里直接崩）。改为 `Option<Vec<String>>` 后命令层用 `let Some(args) = ... else { 报「无需安装虚拟机」 }` 收口，判空职责从调用点的自觉变成类型约束；用例同步断言 `None`

## 2026-09-15

- **00:26** test(e2e): 全流程集成测试（T96 第一步）+ 把 VM 通道编排收进 core —— 动因是这一轮踩的三处缺陷**全住在「只有界面走过、没有任何自动化覆盖」的接缝上**：①`run_host` 把 tar 的 pax 扩展头警告当失败（解压其实成功、作业却被判失败）；②已部署版本只在 VM Running 时读得到（停机就退回「未部署」，逼用户反复重部署 120MB）；③`detached_launch_command` 少写外层引号（`bash -lc <脚本>` 引号失衡，**求解永远无法在 VM 内启动**，而它的单测只断言「包含 `setsid bash -lc`」，把缺陷锁成了期望）。**结构性修法**：新增 `kairos-core::services::vm_run`——`HostCommand` / `HostRunner` 注入式编排（`stage_case` 打包→传输→解压、`copy_results` 清单→打包→回传→解压、`poll_log_once` 日志与退出码回读）+ `judge`/`failure`（**成败只看退出码**的唯一出处），桌面端 `commands/jobs.rs` 改为调用它、只留 runner（PATH 修补 + 超时），`solve_script` 也从适配层收进 core；编排函数取 `&dyn HostRunner`（泛型单态化会让同一源行登记两次、提前返回的那份计数 0，覆盖率报假未覆盖——已记入 ARCHITECTURE §覆盖率）。**测试分层**：`tests/rust/e2e/main.rs`（新 `[[test]]` 目标）L1 无条件（几何→网格→case→结果扫描，CI 三端跑）+ L2 真机（case 进 VM → 脱离会话求解 → 回传 → 宿主扫描读场，`KAIROS_E2E_VM=1` 才跑）；另加 shell 语法契约用例 `generated_shell_commands_parse_with_bash`（把本模块产出的每条命令交给 `bash -n`，含带单引号的路径）——引号失衡这类问题只有交给 shell 才算验证过。**首跑即抓出上述第 ③ 条与「原生求解脚本 PATH 前缀未转义」**。真机证据：L2 全流程通过（216 节点 / 625 四面体，退出码 0、日志有时间步推进、回传后宿主侧扫描出求解时间目录并读出 U 场）。任务档 [T96](tasks/T96-full-flow-e2e-done.md)；verify 全绿、core 覆盖率 100%
- **01:05** refactor(vm_run)+test(e2e): 执行策略入 core、L2 补失败可见性（T96 第 1、2 项）—— ①**执行策略进 CI 可见范围**：`ProcessRunner`（起进程 → 并行抽干 stdout/stderr → 超时杀掉 → 按退出码判定）与 `prefixed_path` 收进 core，适配层的 `host_runner()` 只注入宿主差异（macOS 的 Homebrew PATH 前缀，`platform_command` 与它共用同一前缀常量）；此前这段是 `#[cfg(target_os = "macos")]` 的适配层代码，CI 的 Linux / Windows 两个平台**根本跑不到**，现在三端都有用例（退出码 0 + stderr 有警告 = 成功、非零退出取 stderr 首行、超时判失败、启动失败、capture 取回 stdout、环境变量注入、PATH 前缀不重复叠加）。桌面端与集成测试因此共用同一份执行策略，`jobs.rs` 删掉自带的 runner / 超时 / 等待轮询（净减 ~50 行）。②**L2 补失败可见性**：把 case 改坏（删 `constant/polyMesh/boundary`）再跑一遍，断言**求解器错误标记**与可归因的失败原因——判据用日志标记而不是退出码（脚本尾部的 `; reconstructPar` 会掩盖前面的失败，这与产品侧 `moldingfoam::failure_reason` 同口径）。实测「核数与分解不一致」并不会失败（decomposePar 会重新分解、foamRun 容忍多余 rank），故改用改坏 case 这一确定性触发。③ 用 `coverage-rust.mts` 新增的落点诊断修掉三处**假未覆盖**：断言消息里的内联 `message()`（惰性求值 → 改为先绑定）、`try_wait` 报错这条永远走不到的错误臂（并入「仍在跑」，由超时兜底）、PATH 前缀分支（补真跑用例）。④ 顺带修 `deployed_record_*` 三个用例共用父目录的竞态（并行跑时互相删现场）。真机：L2 全流程 + 失败路径通过（19 秒）；verify 全绿、core 覆盖率 100%
- **09:20** fix(jobs)+test(e2e): 取消终于真的能停住 VM 里的求解（T96 收口）—— **发现一个真缺陷**：VM 通道的求解脱离会话跑在虚拟机里，宿主侧的启动命令早已返回，而 `cancel_job` 只杀宿主侧子进程（`children` 里根本没有 VM 作业的句柄）——**点「取消」实际上什么都没停**，求解继续烧 CPU 并跑完。修法：`solve_script` 开头记录会话 id（`echo $$ > .kairos-solve.sid`，setsid 让这个 pid 同时是会话 / 进程组 id），新增 `solver_stop_command` 按 id 整组终止（`kill -TERM -<sid>`，求解已结束 / id 缺失时空操作），`cancel_job` 对 VM 通道 detached 派发这条命令（远端 kill 阻塞，取消要立刻回界面）。L2 加第四段：启动 2 秒后取消 → 退出码非 0、日志不再增长、且**不出现求解器错误标记**（取消不能被误报成失败）。**测试工程上踩了两条**（已写进用例注释与 T96）：①一次 `multipass exec` 往返 1~2 秒而小算例墙钟只有几秒，**取消必须与启动落在同一次往返**（`launch` 后台化 + `sleep 2; stop` 拼一条）——分两次调用会把取消挤到求解结束之后，症状是「退出码 0 + 进程已消失」，看起来像取消命令失效；②取消窗口**不能靠拉长注射时间**撑开（Co 数受限时步数 ≈ endTime/dt 近似恒定），要更长只能放大网格，而压爆虚拟机内存的是**多个求解并发**（我前几轮的失败运行留下多个算例同时跑，OOM killer 杀掉求解，又被误读成「进程凭空消失」）。L2 三段全绿且连跑两次稳定（各约 20 秒）；verify 全绿、core 覆盖率 100%
- **09:35** test(e2e): 样例测试用例统一到 2.0mm 网格（0.5mm 太慢）—— 实测样例盒（10mm）在虚拟机里的单次求解：**2.0mm 网格（625 四面体）12.6 秒**、1.0mm（5 千）32 秒、0.5mm（4 万，面板「最大边/20」的默认量级）**跑到 16 分钟才走完约 1/3 步数（20 分钟以上）**。取消用例原先用 0.5mm 想撑开取消窗口，但取消是在启动后固定 2 秒发出的，窗口与算例时长无关——纯属白等，改为与 L1 共用 2.0mm 网格（顺带省掉一次网格生成与更大的归档传输）。L2 全量（L1 + 正常 + 失败 + 取消）连跑两次分别 24 / 28 秒。verify 全绿
- **09:42** feat(web): 几何面板建议网格尺寸加 2.0mm 下限（样例首轮求解从 20 分钟降到 13 秒）—— 面板建议值原为「几何最大边 ÷ 20」（两处各写一遍），在内置样例（10mm 立方体）上给出 0.5mm ≈ 4 万四面体；实测该网格单次求解 **20 分钟以上**（跑到 16 分钟才走完约 1/3 步数），首轮试用体验等于「点完就去泡茶」。改为 `max(最大边 ÷ 20, 2.0mm)` 并抽成 `suggestedSize` 单一出处：样例落到 **2.0mm ≈ 625 四面体 ≈ 12.6 秒**（实测），而 40mm 以上的真实件建议值完全不变（÷20 本就 ≥ 2mm）。实测数据（2.0mm→12.6s / 1.0mm→32s / 0.5mm→20min+，随细化超线性增长：单元数↑ 且 Co 限步长↓ 步数↑）记入 [perf-budget](perf-budget.md)，作为下限这一决策的依据；几何面板 20 条用例同步（含「30mm 受下限约束 → 2.00」「100mm 不受影响 → 5.00」两条规则断言）。verify 全绿
- **09:45** docs(perf): 修正上一行里两处不严谨的结论（复查「2.0mm 下限」决策时发现）—— ①「时间随细化**超线性**增长」与干净数据不符：625 → 5,000 四面体（单元 ×8）墙钟只涨 2.54 倍，是**次线性**；按「单元数 × 步数 × 每单元每步开销」拆开看，实测每单元每步开销 9.7 µs（625）→ 2.3 µs（5000），小网格上 MPI / 每步固定开销占比大，这才是那一段的真相。②0.5mm（4 万四面体）那条 ≥20 分钟是在**虚拟机内另有并发求解**时测的（负载 2.7–5.6 / 5 vCPU），已标注条件并给出空闲机器上的外推区间（约 4–6 分钟，待复测替换）。决策本身不变（样例默认 12.6 秒 vs 分钟级、大件不动），但依据的数字与机理已改准
- **09:54** docs(rust): Rust 规范成文 + 架构文档漂移修正 —— 动因是盘点发现 Rust 侧没有与 [web-conventions](web-conventions.md) 对应的规范文档，且 ARCHITECTURE 多处描述与代码现状不符：**规范缺位在先，重构就无从判定「该放哪」**。新增 [rust-conventions.md](rust-conventions.md)（职责边界表 / 四步归属判断 / 层间依赖矩阵 / 模块组织与可见性 / utils 六模块逐一约定 / 错误处理与 `unwrap`-`expect` 边界 / 命名与坏味道清单 / 测试 / 注释），并接进 AGENTS 路由与 web-conventions 交叉引用。**过程中查实的漂移**（形态都是「文档指向一个不存在或已改名的地方」）：①§2 目录树漏掉 T32 起就存在的 `src-crates/kairos-cli` crate，测试位置仍写成 crate 内的 `tests/contract.rs`——实际在根 `kairos-tests` 包的 `tests/rust/{contract,e2e}/main.rs`，§3/§4/§6 三处同错；②前端类型出口写成 `src-web/types.ts`（实际 `types/index.ts`）；③**§1.2 的 `paths::sanitize_file_name` 已不存在**——该函数后来拆成 `path_segment` / `file_name_of`，权威文档一直在引用死名字（`services/workspace.rs` 的 rustdoc 链接同样失效，待重构批次一并修）；④「两条铁律」下实列三条；⑤§6「适配层保持薄、不专门测」与现状相反（4 个命令模块含 40%–60% 业务逻辑），改为「只测装配本身」并明确标注这是目标而非现状；⑥§7 补明 core 的 CPU 实现只能描述为「正确性基准与测试参考」，与把它写成「GPU 回退路径」的注释冲突。新增 §1.3 utils 层：定位、依赖只向下（`error ← utils ← models / services`）、「utils 不认识领域」的判定标尺（签名出现网格 / 场 / 工况等词汇即出局，防 utils 黑洞）。两处与覆盖率门槛直接相关的设计约束写进规范而非留给实现时踩：**utils 接口一律 `pub`**（lib crate 里未被调用的 `pub` 项不触发 dead_code，`pub(crate)` 会，直接决定能否过 `clippy -D warnings`）、**遍历类接口取 `&mut dyn FnMut` 而非泛型**（泛型按调用方闭包类型各单态化一份，同一源行登记多次、未走到的实例被报假未覆盖——即 §6「多份单态实例」那条的应对）。另查实 `services/derive.rs` 全仓零调用者（core / CLI / src-tauri / tests / 前端逐处确认），靠自身单测维持着 100% 覆盖率——**死代码被覆盖率门槛养着**，且其「长度一致」错误文案已与 `results.rs` / `gpu_ops.rs` 的同义约束漂移出三份版本；生产路径实为 GPU（`derive_field` 命令 → gpu_ops），`results.rs` 的 f64 版本是 GPU 一致性基线，故处理方向定为删 `derive.rs` + 收口非数值的信封 / 命名 / 文案，记入后续批次。依赖准入已核验：`regex v1.13.1` 已在 core 依赖图内（`ppt-rs → pdfrs → regex`），提升为直接依赖不新增 crate。docs:check 0 断链 0 孤儿、prettier 通过
- **10:01** docs(reviews): 037 退出崩溃归因到「求解器库被加载两次」并受控复现（上游 2026-09-15 追问的核对报告）—— 上游追问该崩溃是否由库被映射两次引起，要我们在运行日志里 `grep -c 'Duplicate entry'` 并核对加载路径上 `libmoldingFoam.so` 只有一份实体。核对结论**双载成立**：崩溃日志（`study-1a09bc8e6c8-1`，09-14 11:12）152 条 `Duplicate entry` + 4 条 `malloc_consolidate(): unaligned fastbin chunk detected`，栈与上游给的完全同构（`End` → `Finalising parallel run` → `UPstream::exit` ← `argList::~argList`，帧在 `libPstream.so` / `libOpenFOAM.so`）。但**判据要收窄**——我们在自己的 VM 里做了受控 A/B/C（同一 case、`mpirun -np 4`、运行期采样 `/proc/<pid>/maps`）：①现状 1 次映射、②把归档内容恢复到 `FOAM_LIBBIN`（即上游要求避免的「两处各放一份」）仍是 **1 次映射**（`FOAM_USER_LIBBIN` 在 `LD_LIBRARY_PATH` 第 5 位、`FOAM_LIBBIN` 第 7 位，靠前目录把两个 dlopen 名字都遮住）、③把名字拆到两个目录（USER 只留 `libmoldingFoamSolver.so` 实体、基名落到 LIBBIN）出现**每 rank 2 条映射**（两个 sha 前缀 `7d4c7965` / `76e5e5e3`）+ **`Duplicate entry` 152 条**，与崩溃现场同签名 → 真正的不变式是「每个 dlopen 名字在加载路径上只能有一个实体可达，且 `<名字>Solver.so` 是同目录下的相对符号链接」。机制读源码钉死：`solver::load` 用裸 soname（`solverNew.C:32`）、controlDict 的 `libs ("libmoldingFoam.so")` 是第二个名字、glibc 按文件实体去重（符号链接因此能让两个名字塌缩成一次映射，这正是符号链接修复有效的机理）；兜底 `dlLibraryTable::openPattern`（`dlLibraryTable.c:170-192`）同样按裸名 dlopen，遮挡关系不变。**顺带查实我们自己的审计缺口**：核对时 VM 里生效的是 `FOAM_USER_LIBBIN` 下一份**源码重建**的库（sha256 `7d4c7965…`），上游归档那份被**手工改名**成 `.disabled-bundle` 且符号链接被删（sha256 `76e5e5e3…`）——即此前 `dup=0` 的绿色运行验证的是重建库、**不是 v1.0.0 归档里的库**；缺口补进 T29 剩余，并新开 [T97](tasks/T97-solver-lib-duplication-guard.md)（就绪探测按该不变式把关 + 部署改成「先清后解压」）。文档：新增 [solver-lib-duplication-report.md](reviews/solver-lib-duplication-report.md)（日志证据表 / maps 实测 / mermaid 判定图，已按仓库约定渲染校验）、[upstream-questions](reviews/upstream-questions.md) §0/§5/§9 与 [upstream-reply-draft](reviews/upstream-reply-draft.md) 追加回复按新结论改写（§9 原「bundle 无重复载入（单一 inode）」的记录已标注与日志不符）、[e2e-solve-report](reviews/e2e-solve-report.md) 补执行条件说明与 bundle 行更新到 v1.0.0、tasks/README 与 architecture-status 登记 T97；VM 临时 case 与脚本已清、部署环境按核对前状态还原（两份库 sha256 与核对前一致）。docs:check 通过
- **10:05** refactor(core): utils 层落地（utf8 / regex / fs / time / float / shell）—— 规范成文后第一批实装：把盘点出的六类「域无关基础设施重复」各自收口成一个模块，共 62 条用例。**utf8**：`floor` / `ceil_char_boundary`（标准库这两个函数仍是 unstable，故自实现，用 `is_char_boundary` 退让而非 `char_indices` 收集）、按字节预算截断（含省略号版本，约定结果不超预算、预算装不下省略号时返回空串）、字符↔字节下标互转、按字符补宽。**regex**：`LazyLock<Mutex<HashMap<String, Arc<Regex>>>>` 全局缓存（返回 `Arc` 而非 `&'static`——后者要么无限 `Box::leak`、要么把缓存做成自引用），加 `patterns` 静态预定义模式；锁中毒走 `.unwrap_or_else(PoisonError::into_inner)`，用**函数引用**而非闭包，避开「兜底闭包体是独立覆盖落点」这条坑（core 里假 gmsh 锁已是同一写法）。**fs**：`write_atomic(path, content, what)` 的临时文件改为**追加** `.tmp` 而非替换扩展名（`a.stl` → `a.stl.tmp`），避免同名不同扩展名的两个文件争用同一临时文件，同时保持既有 `project.kairos.tmp` 命名不变；遍历接口取 `&mut dyn FnMut` 而非泛型；错误映射抽成命名函数（`tmp_write_failed` / `replace_failed` / `read_failed`）并各自直接测。**float**：`Aabb` 以 `extend_point` 逐点累积（非泛型迭代器，零分配），初值取正负无穷而非 `f64::MAX` / `f64::MIN`；排序统一 `f64::total_cmp`（禁 `partial_cmp(..).unwrap()` 的 panic 与 `unwrap_or(Equal)` 的输入顺序依赖）；**epsilon 明确不收口**——现存 `1e-9` / `1e-12` 各有量纲与来历，机械合并会改数值行为，故 `safe_ratio` 由调用方显式传入。**shell**（`bash_single_quote` 与 `bash_quote` 分开：转义与加引号是两件事）、**time**（`now_ms`）。**依赖准入**：`regex` 本就在 core 依赖图内（`ppt-rs → pdfrs → regex v1.13.1`），提升为直接依赖后 `Cargo.lock` **只多一条依赖边、零新增 crate**（diff 逐行核对）。**覆盖率门槛当场抓出两处落点，正好是规范里预先写下的那两条**：①三个用例里内容完全相同的闭包被优化器合并，其中一份计数为 0、整行被报未覆盖（「多份单态实例」）→ 抽成共用的命名助手 `path_collector` / `extension_is`；②另一处是**真**未覆盖而非假象——「root 不是目录」用例在读目录失败时即返回，谓词根本不会被调用 → 改用已被别处覆盖的谓词，不再引入新的死体。verify 全绿、core 行覆盖 100%（13367/13367）。另记一处**既有**偶发（与本批无关）：llvm-cov 目标下 `gmsh::tetrahedralize_reads_parsed_msh_from_successful_run` 偶发失败（14 次干净运行中 1 次，信息为「无 stderr 输出」即 exec 未跑起来）；该用例的假 gmsh 脚本写**固定路径** `/tmp/kairos-fake-gmsh-ok` 而非按进程隔离，与本机并发的另一次 cargo test 撞车即触发 ETXTBSY，属测试夹具健壮性问题，待单独修
- **10:10** refactor: 三类基础设施重复收口到 utils（now_ms 4 份 / bash 引号 3 份 / 原子写入 3 份）—— 批次 2 的第一组，共同特征是「每个调用点都觉得自己写三行更省事」，于是分头演进出差异。**`now_ms`**：core `services/project` 的定义改为 `pub use crate::utils::time::now_ms`（保留入口，属公开 API 不动），删掉适配层 `commands/jobs.rs` / `commands/downloads.rs` 的两份私有副本与 CLI 里的一段内联 `SystemTime` 取值，统一从 `kairos_core::utils::time` 导入。**bash 单引号转义**：`services/vm::bash_single_quote` 同样改为 `pub use crate::utils::shell::bash_single_quote`，适配层 `jobs.rs::script_case_dir` 与 CLI `run_solver` 的两处内联 `replace('\'', "'\\''")` 改走同一份——此前 `jobs.rs` 里一处用 core 的、另一处手写，同一个文件两种写法。**原子写入**：适配层 `downloads.rs` 的清单落盘（原 `with_extension("json.tmp")` + `fs::write` + `fs::rename`）与 `vm.rs` 的部署记录落盘改调 `utils::fs::write_atomic`；core `services/project::write_atomic` 也从自带实现改为委托，`what` 固定「工程文件」。**行为等价性逐条核对**（这是本批最需要小心的地方）：①临时文件名——`utils` 用「文件名追加 `.tmp`」，对 `project.kairos` → `project.kairos.tmp`、`manifest.json` → `manifest.json.tmp`，与原 `with_extension("kairos.tmp")` / `with_extension("json.tmp")` **逐个同形**；②错误文案——`what = "工程文件"` 拼出的正是原文案「工程文件替换失败」；证据是既有用例 `write_atomic_reports_tmp_write_failure`（core）与 `write_atomic_reports_rename_failure`（gap_tests）**未做任何修改即通过**，它们同时断言了临时文件路径与消息文本。③唯一有意的差异：`downloads.rs` / `vm.rs` 原本直接 `fs::write(..)?` 走 `From<io::Error>` 的裸 io 消息，现在变成带动作描述的中文（如「下载清单替换失败」）——错误 `code` 仍是 `io`，而前端按 `code` 分支、禁止匹配 message（架构契约），故不构成对外行为变更。过程中因 `use std::fs` 在移除实现后仅剩测试在用而触发 clippy `unused_imports`，把该导入下移到测试模块。verify 全绿、core 行覆盖 100%
- **10:14** refactor: 浮点排序与包围盒 fold 收口（8 处排序 / 6 处包围盒）—— 批次 2 第二组。**排序**：全仓 8 处 `partial_cmp(..).unwrap_or(Ordering::Equal)` 清零（`thickness` / `gate_location` ×3 / `results` / `optimize` / `moldingfoam`，含 `fill_preview` 用例里那个会 panic 的 `.unwrap()` 变体），`&mut [f64]` 的场合改走 `float::sort_asc`，按键排序的场合直接改 `f64::total_cmp`。**过程中撞到一个不能机械替换的场合**：`optimize::best` 的 `score` 是 `Option<f64>` 而非 `f64`，没有 `total_cmp`；而它上面刚 `filter(feasible)` 过，`feasible() == score.is_some() && violations.is_empty()`——直接写 `match (left, right)` 的两个 `None` 分支将**永远不可达**，正是规范要禁的「单侧偏态分支」。改法按 §6 的应对：抽成**命名全序函数** `score_order(Option<f64>, Option<f64>) -> Ordering`（`None` 排在 `Some` 前，与 `Option` 的 `PartialOrd` 同序；两个 `Some` 用 `total_cmp`），并在用例里**直接测它的全域**（6 组输入覆盖 4 个分支）——它是定义完备的全序函数，测全定义域是测真实契约，不是为不可达代码造用例。另核实 `score_of` 已 `filter(is_finite)`，故 `Some` 内不会出现 NaN，`total_cmp` 与 `partial_cmp` 在可达域内**结果完全一致**。**包围盒**：新增 `float::bounds_of_points(&[[f64;3]]) -> Option<([f64;3],[f64;3])>`（薄封装 `Aabb`，取 `&[[f64;3]]` 切片故只单态化一份），迁移 6 处逐轴 fold：`volume_field::{mesh_bounds, tet_bounds}`、`thickness::bounds`、`fill_preview::cell_scale`、`gate_location` 的对角线计算、`models::geometry::bounding_box`（后者按三角形结构体逐点 `extend_point`，不分配）。**等价性是逐处论证过的，而不是看着像就换**：`Some` 返回值的比较逻辑与原来逐轴 `min` / `max` 完全相同；空输入与「非空但坐标全为 NaN」两种退化输入在原实现里都停在初值（`INFINITY` / `NEG_INFINITY` 或 `MAX` / `MIN`——`f64::min(INF, NaN)` 返回非 NaN 那侧），与 `Aabb::is_empty()` 的判据（`min[0] > max[0]`）**恰好同界**，故每处 `unwrap_or(<各处原有哨兵>)` 后逐位等价，包括 NaN 这种退化情形。**明确不统一的三处**（避免机械替换）：`dualdomain::report` 与 `midplane` 在 min/max 之外**还累加 sum**，拆两遍扫不划算；`repair` 的两处是**标量**区间（沿方向的投影）与无哨兵的 `Box3{min: t.a, max: t.a}` 初始化，形态本就不同；`models::geometry::bounding_box` 单独保留「空网格 → 零点」的对外契约。verify 全绿、488 条 core 用例、行覆盖 100%
- **10:17** refactor: 适配层的三处目录树递归收口到 `utils::fs`—— 批次 2 第三组。**动的理由**：`commands/downloads.rs` 里 `collect_env_roots` 与 `collect_bin_dirs` 是同一段骨架（深度守卫 → 读目录失败即返回 → 跳过非目录 → 递归）抄了两遍，`commands/dependencies.rs::find_executable` 是第三遍，三处都住适配层——而「有界深度遍历」是域无关的基础设施，按 §1.3 该在 `utils`。**顺带补上一个能力缺口**：原来的 `walk_dirs_bounded` 回调不返回值，无法表达 `collect_env_roots` 的「命中即停」（找到含 `openfoam14/etc/bashrc` 的目录后**不再往里找**）。改为回调返回 `bool`：`false` 表示把该目录当叶子，这正是 walkdir / ignore 那类库的 `filter_entry` 形态，也让「命中即停」不必另写一份递归。同步更新 `rust-conventions.md` 的接口表与该条约定。**语义等价性**：①`collect_bin_dirs` → `walk_dirs_bounded(.., 4, ..)` 恒返回 `true`（对应原来无条件下探）；②`collect_env_roots` 的 `continue`（命中不再下探 + 不覆盖已有的更新记录）对应回调返回 `false`，`found` 的「取最后修改」判定逻辑原样保留在闭包内；③`find_executable(dir, binary, 0)` 的 `if depth > 4 { return None }` 与目录优先下探顺序，与 `find_file_bounded(dir, 4, ..)` 的骨架逐条对齐（同一层按 `read_dir` 顺序取首个命中，先在当前层找文件再下探子目录）。**一处必须交代的实现差异**：原来用 `DirEntry::metadata()` 判断 Unix 可执行位，`find_file_bounded` 只给 `&Path`，故改用 `Path::symlink_metadata()`——二者语义都是**不跟随符号链接**（若误用 `metadata()` 会跟随），对 OpenFOAM 那种靠符号链接组织 bin 的目录树是实质差异，故特意对齐并写进注释。删掉三个递归函数后本批净减 ~70 行。**明确没做的一项**：`gmsh::failure_message` 与 `downloads.rs` 解压失败处的「stderr 末行 + `无 stderr 输出`兜底」两处重复**先不动**——共同部分只有一句表达式，且真正的同类第三处（`vm_run::failure` 取**首个非空行**的变体）属进程执行族，等批次 4 把适配层那 4 条进程执行路径收口时一并定家更合适，现在塞进 `utf8` 或另建模块都名不副实。verify 全绿、core 行覆盖 100%
- **10:20** refactor(cli): DOE 编排块去重（`matrix` 与 `run` 两条子命令共用解析与落盘）—— 批次 2 第四组。原来「编排方式名 → `DoePlan`」的 `match`（含那句 `未知的编排方式「{other}」，可用：orthogonal / full。`）、批次名推导（显式名优先，否则因子名 `join("-")`）、以及「建批次目录 + 写 `summary.csv` + 写 `summary.json`」（含 `创建批次目录失败` / `写入汇总表失败` 两句文案）在 `doe matrix` 与 `run_doe_batch` 里各有一份，汇总表落盘更是出现三处。抽成三个命名函数：`parse_doe_plan`（可选取值与错误文案单点）、`doe_batch_name`（批次名口径单点）、`write_doe_summary`（落盘 + 建目录，目录已存在时 `create_dir_all` 幂等，故逐次运行后的重写也复用它）。**求值顺序刻意保持**：原先 `let plan = match …?` 先于 `build_matrix(plan, &parsed)?`，现在写成 `build_matrix(parse_doe_plan(plan)?, &parsed)?`——实参先求值，故「计划名非法」仍先于「因子不合法」报出，错误优先级不变。**CLI 没有单测，故用真实调用两条路径做冒烟**：`doe matrix --json` 输出矩阵不变；`doe matrix --out-dir … --batch my-batch` 落盘 `summary.csv` + `summary.json` 且控制台提示一致；`doe run`（不带 `--solve`，走完网格准备 → 生成 case → 逐次重写汇总表）产出与单次运行状态；两条子命令的非法 `--plan` 都返回**逐字相同**的 `{"code":"validation","message":"未知的编排方式「bogus」，可用：orthogonal / full。"}`。verify 全绿、core 行覆盖 100%
- **10:24** refactor(core): 派生场家族收口——删零调用者的 f32 核，命名 / 信封 / 文案归一到 `services::derive`—— 批次 3，处理上一轮盘点里最意外的一条。**删**：原 `services/derive.rs` 的四个 f32 切片核（`normalize` / `linear_map` / `threshold_mask` / `field_difference`）**全仓零调用者**（core / CLI / src-tauri / tests / 前端逐处确认），却靠自身单测维持着 100% 覆盖率——死代码被覆盖率门槛养着；更关键的是它的 `field_difference` 错误文案「两场长度不一致：{} vs {}」已与 `results` / `gpu_ops` 里的同义约束**漂移成三份版本**，正好印证「双实现并存必然漂移」。**为什么不是「接上调用点」而是删**：CPU 参考在 f64 域、GPU 必须 f32，让 GPU 路径改调它等于把现有的 f64 基准降级为 f32，丢掉「f64 基准 vs f32 GPU」这个有意义的一致性对照；反向 f64→f32→f64 往返又会在 CPU 路径上丢精度。两条都改行为，故该实现没有立足点。**该模块改为承载真正会漂的那部分**（与精度无关、两条路径都依赖）：`NAME_NORMALIZE` / `NAME_THRESHOLD` / `linear_name`（含符号的 `×{scale} {offset:+}`）、`difference_request_error`（单场入口收到差值请求的文案）、`validate_difference_lengths`（长度校验，含带场名的错误文案）、`derived_field`（单场结果信封：场名追加后缀、`is_magnitude` 置 false、完整性沿用基场）、`difference_field`（差值信封：命名「主场 - 对比场」、完整性取两者之与）。`results::derive_*`（f64 CPU）与 `gpu_ops::derive_*_gpu`（f32 GPU）各自只留数值核与精度转换，命名与信封改调这一份——**同一个请求在两条路径上不再可能得出不同的场名**。**刻意没做的一件事**：没有把「请求是否单场可受理」抽成共享判定函数——两个调用点都是 `match` 直接落到各自的 `Difference` 分支（该分支**可达且有测试**：前端确实能请求差值，两条路径各有一条拒绝用例），抽出来只会多一个零调用者的预留抽象，正是规范里「未落地的预留抽象」那条要避免的（初稿写了 `is_single_field_request`，自查时发现无调用者即删）。**顺带修两处注释漂移**：`services/operators.rs` 自称「GPU 不可用时的回退路径」与 ARCHITECTURE §7「不提供 CPU 运行时回退」直接冲突，改为「正确性基准（仅测试与对拍）」并点明不要接成回退分支；`services/workspace.rs` 的 rustdoc 链接指向**已改名的** `paths::sanitize_file_name`（现为 `path_segment`），是会失效的坏链接。verify 全绿（GPU/CPU 一致性用例全过，说明共享命名未改行为）、core 行覆盖 100%
- **10:27** refactor: 错误契约两处失守修正（DTO 上的领域规则 / 命令不返回 Result）—— 批次 4 第一组，都是「架构文档写了、代码没做到」的形态。①`models::project::Project::add_study / remove_study` 原本**把领域规则和 `Result<_, String>` 一起挂在 DTO 上**：字符串化错误丢掉了 `code`，前端只能靠匹配文案区分「名称重复」和「方案不存在」——而架构契约明令「前端按 code 分支、禁止匹配 message」。改为 `services::project` 的自由函数 `add_study(&mut Project, …)` / `remove_study(&mut Project, …)`（规则住 services，`models` 只留持久化形状），错误类别落到位：名称空 / 重名 → `validation`，方案不存在 → **`not_found`**（前端可据此区分「已被删掉」与其它失败）。`create()` 里原本对 String 错误做 `.map_err(KairosError::internal)?`，现在直接 `?`——顺带把类别从 `internal` 修正成 `validation`（消息文本逐字未变，调用点仅两处：`create` 与一个用例）。②`commands::mold::check_mold_network` 返回裸 `Vec<String>`，是**唯一**不返回 `Result` 的命令，违反「命令一律返回 `Result<T>`」（该约定的用意正是将来加校验时不必改签名、不破坏前端）；补上 `Result<Vec<String>>` 后 IPC 载荷与前端 `invokeCommand` 的 TS 类型都不变。为确认没有第二处漏网，写了个按 `#[tauri::command]` 解析签名的临时脚本扫全部命令模块：**0 处**缺 `Result`。verify 全绿、core 行覆盖 100%
- **10:30** refactor: 原生菜单从 `lib.rs` 拆到 `commands/menu.rs`（257 → 120 行）—— 批次 4 第二组。`lib.rs` 的 `setup` 里内联着 138 行 macOS 菜单树（13 个动作项、7 个子菜单、应用菜单与编辑菜单的系统预定义项、about 元数据），把 crate 入口淹没了；装配函数应当只剩「插件 + State + setup + 菜单事件 + generate_handler」的骨架。抽出 `commands/menu.rs::install(app) -> tauri::Result<()>`，模块整体按平台裁剪（`#[cfg(target_os = "macos")] pub mod menu;` + 同条件调用点），不造「非 macOS 上的空实现」那种占位函数。**顺带清掉一段真正的死代码**：原块外层已是 `#[cfg(target_os = "macos")]`，里面却还有 `#[cfg(not(target_os = "macos"))] let help = …` 与同条件的 `builder.item(&help)`——**永远不参与编译**（Windows / Linux 上整个块都不存在），连注释「Windows / Linux 没有应用菜单，关于入口放帮助菜单」也是失效描述；一并删除，平台事实改由模块头注释说明。**等价性核对**：用脚本把改动前后源码里的菜单动作 id 全量提取并 `diff`——**逐个相同**（file.new / file.open / file.save / file.saveAs / view.theme / analysis.checkNetwork / tools.refreshDeps / tools.vmPanel / tools.vmStart / tools.vmShell / tools.vmStop / results.exportCsv / report.open），且这些 id 全部存在于前端动作表。**契约测试当场抓到这次搬迁**：`tests/web/contract/native-menu.test.ts` 是解析 Rust 源码提取 id 再与 `src-web/menu-actions.ts` 比对的无编译依赖契约测试，它硬编码读的是 `src-tauri/src/lib.rs`——菜单搬走后解析为空、13 个必需 id 全部报缺失（前端 675 条用例里唯一挂掉的一条）。这正是该契约存在的意义：跨语言 id 无类型约束，只能靠它拦漂移；把读取路径改指 `commands/menu.rs` 后 3 条断言全过。verify 全绿（前端 675 条、core 行覆盖 100%）
- **10:34** refactor: 适配层再交三件事给 core（核数策略 / 就绪文案 / 宿主探测的解析部分）—— 批次 4 第三组，都属「命令层算了不该算的东西」。①`generate_moldingfoam_case` 里的 `cores.clamp(1, 64)` 是领域资源规则：该值既写进 `decomposeParDict` 的 `numberOfSubdomains`、又传给 `mpirun -np`，而面板只给了 `min="1"`、**没有上界**，所以这里是唯一的把关点。收进 core：`moldingfoam::MAX_CORES` + `moldingfoam::clamp_cores(cores) -> usize`（并配上下界用例：0 → 1、`u32::MAX` → 64）。②`probe_moldingfoam` 里「环境齐 + 求解器齐 → 就绪 / 缺环境 → 下载指引 / 只有环境 → 版本过旧」的三段判定与三句用户文案是领域策略，收进 `services::dependencies::environment_hint(moldingfoam, solver)`，写成 `match (bool, bool)` 三个分支——`(false, true)` 归入 `(false, _)`，与原来 `else if !moldingfoam` 的判定顺序**逐字一致**，三条分支都有用例覆盖。③**宿主探测拆成「取数」与「解析」**：新增 `services/host.rs` 只放纯解析（`zoneinfo_name` 从软链目标取 IANA 名、`memory_gib_from_bytes` 解析裸字节数、`memory_gib_from_meminfo` 解析 `/proc/meminfo` 的 `MemTotal:`），适配层每个平台只留「起哪条命令 / 读哪个文件」。收益是可验证的：原先 macOS 与 Windows 的取数与解析**逐行相同**却是两份拷贝，而 Linux 的 `/proc/meminfo` 解析在 macOS 开发机上根本没有覆盖；现在三份解析都在三端被同一批用例覆盖（含 16 GiB 少一字节 → 15 的向下取整、退化软链目标 → 空串）。按规则「路径一律走库」核对过：`zoneinfo_name` 的 `zoneinfo/` 是 POSIX 软链目标的字面标记、返回的是**名字**而非路径，故按文本 `split_once` 处理（并写进文档注释），不涉及 Windows 分隔符语义。**明确没做的一处**：`command -v` 探测的三个副本（`solver.rs` 用 `sh -c`、`dependencies.rs` 用带 Windows 分支的 `sh -c`、`vm.rs` 探 mpirun 用 `bash -lc` 以吃到已部署环境的登录 PATH）——它们**不是同一份实现**（两种 shell 语义），且共享家只能落在适配层的某个命令模块上，正是本批要减少的命令间横向耦合；留给后续把宿主探测整体收口时一并处理。verify 全绿、core 行覆盖 100%
- **10:38** refactor: 下载的源白名单与文件名推导收进 core（`services::downloads`）—— 批次 4 第四组。适配层的 `downloads.rs` 里有两段「纯规则、且写错了不会有人立刻发现」的逻辑：①`ALLOWED_PREFIXES` 官方源白名单与它的前缀匹配（**安全边界**：Kairos 只转发官方直链，白名单是唯一防线）；②`derive_file_name` 从 URL 推导落盘文件名——它编码了 codeload / release 直链的形态知识，注释里就记着两个坑（`.tar.gz` / `.tgz` 双段扩展必须优先于「最后一个点」，否则 `master.tar.gz` 会被截成 `.gz`；以及 query / hash 剥离与相对路径段拒绝）。两者都搬进 `services::downloads`（`ensure_allowed_source` 报 `validation`、`derive_file_name` 保持逐字相同的判定顺序），适配层只留「取数 / 落盘 / 解压起进程」。**搬迁带来的直接收益是覆盖率门槛开始管这段代码**：`downloads.rs` 在适配层、没有覆盖率约束，搬进 core 后门槛立刻报出 `derive_file_name` 的 `.tgz` 与「按最后一个点取扩展名」两条分支**从未被执行过**（原用例里 `.tgz` 那条走的是「官方原始文件名直接返回」的早退路径，根本没进扩展名判定）。补上两条真实形态的用例：GitHub 分支归档的 `.../archive/refs/heads/main.zip`（走「最后一个点」分支）与 `.../archive/master.tgz`（走 `.tgz` 分支）。白名单侧补了正反用例，含三条**必须拒绝**的绕过尝试：`http://` 明文、`https://gmsh.info.evil.example.com/...` 这类前缀蒙混、以及其它仓库的 release 路径。verify 全绿、core 行覆盖 100%
- **10:42** refactor: 求解脚本构造收进 core（`native_solve_script` / `script_case_dir`）—— 批次 4 第五组，与上一组同属「命令层算了不该算的东西」，但这两段更关键——**它们决定求解能不能启动**。搬进 `services::vm`，紧挨已有的 `solve_script`（VM 通道的对口实现，其文档早已写明「与原生分支共用同一口径」，现在这一口径真的同源了）。两段都是纯字符串构造、都有既有用例，搬迁后进入覆盖率门槛管理。**净化掉一处「被测试养着的死代码」**：`script_case_dir` 搬走后，适配层的 `to_wsl_path`（core `paths::wsl_path` 的薄包装）只剩它自己的用例在调用——生产调用点为零，而 core 的 `paths::wsl_path` 早已有 11 条断言的用例（含 `C:` 盘符、`C:/`、大小写、含空格与中文、已是 POSIX、相对路径、空串）把适配层那 3 条断言完全覆盖，故连同用例一并删除（与批次 3 删零调用者的 `derive.rs` 同一形态）。**覆盖率门槛又抓到一处从未执行的分支**：`wsl_style_path` 里 `wsl_path(..).unwrap_or_else(|| path.to_string())` 的**兜底闭包**——原用例只给了能被解析的 `C:\...`，回退分支从未跑过（这正是 §6「闭包体是独立覆盖落点」那条）。补上一例真实场景：Windows 通道拿到**已是 POSIX 形态**的路径（`/already/unix/study-1`）时解析不出盘符、原样使用，不硬套 `/mnt` 前缀。另为 `native_solve_script` 补一条用例：受管 PATH 前缀含单引号（应用数据目录可由用户自定）时必须转义，否则引号失衡——这条此前也没有覆盖。适配层 `jobs.rs` 净减约 100 行（882 → 808）。verify 全绿、core 行覆盖 100%
- **10:44** test(e2e): 重构系列在真机 VM 上回归通过（L1 + L2），并记下一条约束后续改动的结构性事实—— 本轮重构改了不少「求解链路上游」的东西：脚本构造（`bash_single_quote` / `script_case_dir` / `native_solve_script`）、原子写入（部署记录与工程文件）、时间戳、结果与网格路径上的浮点排序与包围盒、派生场的命名与信封。这些都只是单元测试与 verify 绿，**而单测绿不等于求解链路没断**——所以真机跑了一遍：启动 `kairos` 实例（Ubuntu 24.04）后 `KAIROS_E2E_VM=1 cargo test -p kairos-tests --test e2e` **通过（17.9 秒）**，走过 case 进 VM → 脱离会话求解 → 结果回传 → 宿主侧扫描读场；同日早些时候的 L2 记录是 24–28 秒（含失败与取消两段），本次时间更短是因为改的这几处都不在求解热路径上。跑完把实例 stop 回原状（另一个 `of14` 实例是并行会话在用，未触碰）。**这条记录更重要的目的是钉住一个后续改动必须知道的事实**：端到端用例走的是 core + CLI 通道，**完全不覆盖 `src-tauri/commands/`**——适配层的回归只靠它自己的单测（当前只有 31 条）与人工验证。这正是本轮**没有**动剩下两项的原因，理由记在下面，避免下次有人以为「verify 绿就等于 VM 链路可安全改」。**未做的两项与各自的验证需求**：①`commands/vm.rs::run_bytes` 与 core `vm_run::ProcessRunner` 目前是两套进程执行实现（前者 spawn + `try_wait` 轮询 + 管道抽干 + 死线，后者并行抽干 + 超时杀掉 + 按退出码判定），统一需要一个能同时覆盖「安装 / 启动 / 部署 / Shell」这些**e2e 不经过**的路径的验证手段，否则等于拿 VM 生命周期去赌；②`download_to_file` 与 `downloads::download_file` 两套流式下载（含各自的 ureq agent）同理——统一要走真实的镜像下载（120MB 级）与校验和语义对比。两项都属适配层内部行为，改动后除了人工过一遍虚拟机面板没有别的兜底。verify 全绿、core 行覆盖 100%
- **10:49** test(core): 补 `apply_factors` 全部 9 个因子名的用例——**区域口径抓出的真缺口**（行口径看不见）—— 起因是核查「region 覆盖能否也做到 100%」时导出 llvm-cov 逐区域数据，发现 108 处缺失区域里有一类不是 `?` 错误边、而是**真的从未执行**的 match 臂：`services/doe.rs::apply_factors` 把 DOE 因子名映射到 `ProcessSettings` 字段，一共 9 个臂，而用例只 set 了「熔体温度」与「保压压力」，**其余 7 个（模具温度 / 顶出温度 / 注射时间 / V/P 切换 / 保压时间 / 冷却时间 / 介质温度）一次都没跑过**。行覆盖看不出这件事：每个臂所在的行同时含「模式比较」区域（遍历每个候选名都会求值、计数非 0），整行因此被判为已执行，只有臂体的区域计数为 0。**为什么这个缺口值得补**：`apply_factors` 是 DOE 从因子名到工艺设置的唯一桥，漏一个臂的后果是整批运行的该参数悄悄保持基准值，而汇总表看起来「跑过了」——与 `FACTOR_NAMES` 那条注释警告的是同一类事故（静默忽略会让整批参数一样）。**做法**：表驱动逐名断言（8 个标量因子各带一个 `fn(&ProcessSettings) -> f64` 读字段的闭包，注入值一律取「基准 + 1」量级，臂没执行时字段保持基准值即失败），保压压力单列（它是曲线不是标量，落成 `vec![(0.0, value)]`），外加一道**双向对齐**：断言表里的名字集合与 `FACTOR_NAMES` 相同——只往清单加名字不补臂，新名字会直接报「未知因子」，这条断言会拦住。**消融验证（两次都按要求变红）**：①把「模具温度」的臂故意指到 `melt_temp_c` → 用例失败并指名「因子「模具温度」没落到对应字段」；②往 `FACTOR_NAMES` 加一个没有对应臂的名字 → 用例失败报「FACTOR_NAMES 的「新因子」没被本用例覆盖」。两处消融均已还原。**实测效果**：`services/doe.rs` 缺失区域 12 → 5、区域覆盖 98.25% → 99.34%，全仓缺失区域 108 → 101——差值恰好是这 7 个臂，说明补的用例精准落在缺口上、没有顺带刷别的数字；行覆盖仍为 100%。**关于「区域也做到 100%」的结论**：不做，也不该做——剩余 101 处里约 78 处是 `?` 的**提前返回边**（`params.validate()?`、`write(&poly.join("points"), …)?` 这类，要让某一条边执行就得在那个具体调用点注入一次失败），约 21 处是测试里 `assert!(a || b)` 的短路分支，其余是块收尾区域与 `map_err` 闭包体；只要代码里有错误处理与短路求值，区域口径就不可能 100%，而错误的**映射逻辑**已在它的唯一定义点被测过（§6「同一语义的错误映射只写一处」）。门禁维持 `--fail-under-lines 100` 是正确选择。verify 全绿
- **10:53** docs(perf): 新增 moldingFoam 逐版本性能记录（[moldingfoam-perf.md](moldingfoam-perf.md)）—— 上游迭代到下一版时，「这次变慢是上游还是我们」只能靠同口径的逐版本数据回答，而此前求解器数字只有 perf-budget 里夹着的一行，且那是 Kairos 自身预算的表格、口径不同。新文档与 perf-budget 明确分工（后者是 Kairos 自身的渲染 / 网格 / 后处理预算，前者是**外部求解器**逐版本实测），并把协议摆在表格之前——**文档的核心不是那张表，而是「测量协议」**：必录上游版本标记、**求解器库 sha256 与 `FOAM_USER_LIBBIN` 指向哪棵树**、bundle 包摘要、宿主/虚拟机规格、case 口径（网格规模 / 目标尺寸 / `numberOfSubdomains` 与 `np`）、工艺口径（注射时间 / `endTime` / 材料）、**负载条件（虚拟机是否空闲）**、墙钟与步数、收敛与守恒证据（V/P 切换、质量预算残差、FATAL / NaN 计数），并附上一条可直接跑的读身份命令（含「`find -type f` 会漏掉符号链接，而加载路径正是靠链接成型」这个坑）。**为什么把库身份列成必录**：今天上午的审计已经踩过——**标称版本号 ≠ 实际加载的二进制**。本轮只读核对进一步确认了当前状态：部署树里归档库被改名为 `libmoldingFoam.so.disabled-bundle`（sha256 `76e5e5e3…`，不参与加载），而 `source …/openfoam14/etc/bashrc` 后 `FOAM_USER_LIBBIN` 指向源码重建树 `~/OpenFOAM/ubuntu-14/…`（`7d4c7965…`，与 `Solver` 名同摘要）——**即现有 v1.0.0 数字实际跑的是源码重建库**。因此 v1.0.0 一节显式标注为「**v1.0.0 环境（库为源码重建）**」而非「v1.0.0 制品」，并给出两份库的对照表与核对来源。**本轮未做新测量**，原因写明：当前 VM 无法复现「制品口径」（归档库被禁用、无符号链接对），而恢复部署正是另一会话在 [T97](tasks/T97-solver-lib-duplication-guard.md) 里进行的核对范围，不擅自改动共享虚拟机状态；「v1.0.0 制品口径墙钟」已列入「待补测」并注明前置条件。收录的实测：墙钟 vs 规模（625 四面体 12.6 s / 5,000 32.0 s / 40,000 ≥20 min 且叠加并发）、每单元每步开销 9.7 → 2.3 µs 的次线性解释、78,125 四面体 2,220 步到 `endTime`、部署 11.6 s、L2 端到端 17.9 s、bundle 资产摘要；v0.2.1 / v0.2.5 只留行为对比（填充 0.751 → 0.990、门控压力 0.15 → 57.6 MPa、真实件从 t≈0.0134 s 发散到跑满），并明说那两版**没有可比墙钟**是因为当时没记环境身份与负载——正是本协议要固定的东西。接入 AGENTS 路由表与 perf-budget 交叉引用；docs:check 0 断链 0 孤儿。**成文后按可读性重排了一遍**：关键警告（这批数字不是制品口径）原先埋在第 55 行、表格单元格里塞满解释性长句导致横向溢出、四道分隔线纯噪声——改为「**速览先行**（3 条：先读那条警告 / 对比看哪里 / 新增看哪里）+ 数据与解释分离」（宽表拆成「短事实表 + 证据 bullet 列表」，协议表单元格只留字段与一句话，读库身份的命令单独出代码块），并把「为什么为一个小版本单独建文档」收进速览末尾而非单起一节。**顺带记两个机械坑**：①GitHub 的锚点生成会**去掉版本号里的点**（`v1.0.0` → `v100`），所以带版本的标题必须自带版本号才能与 `#v100-…` 对上，否则站内跳转静默失效；②`docs:check` 只校验相对文件链接、**不校验锚点**，锚点得自己比对标题。改动后各表列数一致、最长渲染列宽回到可读范围（脚本核对过每张表的列数）
- **11:13** refactor(web): 前端整体优化——渲染后端三处真 bug、分层违规收口、9 处重复实现单一来源、视口热路径 —— 起因是「读规范 → 核实代码」。**先做的事实核查比改代码更重要**：另一轮泛读式审计给出的清单里，`lengthFromMm` / `mat4RotateX/Y` / `unregisterSnapshot` / `recents` / `probeHits`「死代码」全部不成立——knip 绿是因为测试文件是 entry，那些导出是**只有用例在调**，删与留是另一件事；`withBusy` 两处（app 覆盖式 vs vm 独占且拒并发）、三处环形缓冲（append / per-key / prepend 三种形态）也不是同一个语义，机械合并会改行为，故本轮不动。真正落地的是下面这些。**①渲染后端的三个真 bug**：`createViewportRenderer` 把 `onView` 只传给了 WebGL **回退**分支，WebGPU 主路径拿到的是 `undefined`——于是首选后端上相机注视点读数与四分格联动**长期静默失效**（无人报错，只是不联动）。补上之后立刻撞出**第二个**：`setOrbit` 无条件 `emitViewState()` → onView → `syncOrbitFrom` → 对端 `setOrbit` → …，四分格下 WebGL 路径第一次拖动相机就是相互递归直到栈溢出；修法是把「程序化同步」与「用户交互」在渲染器里分开——`setOrbit` / `resetView` / `zoomBy` / `fitView` 一律不再回报，只有 mousemove / wheel 处理器 emit；顺带发现 WebGPU 的 `zoomBy` 从不 emit，滚轮缩放本来就不会同步给其它视口，改为在 wheel 处理器里显式发一次。**第三个**是资源不回收：三个渲染器里的 `window.addEventListener("mouseup", 匿名箭头)` 在 `dispose()` 时摘不掉（闭包被 window 长期持有），而视口渲染器**从来没人调用过 `dispose()`**——布局从四分格切回单视口时副视口画布被 `v-for` 摘掉，渲染器的 RAF 循环继续对着脱离文档的画布绘制，来回切几次就是几个空转渲染器。改法：`window` 监听留成类字段 + 按布局派发销毁（退出布局即 `releaseSlot`，销毁渲染器并复位就绪态）+ `onUnmounted` 全量销毁 + 在途创建回来发现画布已摘掉则立即销毁。**②分层违规**：`render/renderer.ts` 曾 `import { THEME_CHANGED_EVENT } from "../composables/useTheme"`——render 属图底纯 TS 层，反向依赖 composable 等于把 Vue 拖进渲染底座。把主题核心（CSS 发现 / 注入 / 切换 / 持久化 + `themeVar` / `themeRgb`）整体下沉 `utils/theme`，composable 退化为「ref + 监听事件」的适配层；顺带把两处「读主题 CSS 变量」的重复实现（renderer 里的 `hexToRgb`、chart 里的 `cssVar`）收敛成一处，并补上一个此前被忽略的事实：**WebGPU 主路径根本没接主题**（清屏色是文件里的硬编码常量），现在两个后端同源。另把 `utils/bench/*` 移到顶层 `src-web/bench/`——render 与 utils 是平级的纯 TS 底座，utils 不许反向引用 render，bench 是压测渲染器的开发期脚本、本就该另立门户（同步改 vitest exclude / knip entry / package.json 脚本）。**③composable 越层**：`useReportPanel` 与 `useNewProjectDialog` 直接 import api 并自建 try/catch，收成 project store 的 `saveReport` / `saveReportPptx` / `defaultWorkspace` / `chooseWorkspaceDir`（失败入全局错误、返回 `T | null`），composable 只保留「保存成功显示路径 / 否则回退浏览器下载」的编排——报告「写不进去也能拿到」这条既有语义逐字保持（用例未改断言即通过）。**④9 处重复实现收口**（每处都在文档里登记了唯一落点）：`app.working`（原 7 处各写 `busy !== null`）、`utils/error::errorMessage`（3）、`stores/jobs::jobStatusLabel`（2 份逐字相同的状态中文表）、`utils/materials::findMaterial`（7 处各自展开 builtin+custom）、`utils/study-tasks::geometryHealthy`（3 处同样的四条件判据）、`utils/ipc::forwardingChannel`（5 处 `new Channel` + 赋 onmessage 样板）、`utils/download::downloadTextFile`（报告面板又抄了一份 Blob+锚点）、`render/math::boundsOf`（两个后端各一份扫描）、结果面板的时间步取法（张量 / 矢量各一份）。**⑤视口热路径**：`applyField` 原先在**槽循环内**做「逐面值展开 + `minMax` 全量扫描 + 新建 `Float32Array`」，而四个实例共用同一份网格——改成按共享网格只展开一次、值域只扫一次；值域 / 中值改为按**场对象身份**缓存（图例与云图各读同一个场，10⁶ 量级不该扫两遍）；包围盒改为**上传时算一次**缓存（剖切滑块每拖一格都读 `getMeshBounds`，原先每次全量扫顶点）；`applyDeformation` 原先在槽循环里调 `applyField` 与 `applyClip`，而这两个函数自身又各遍历全部槽——四分格下是 O(槽数²)，移到循环外各做一次。**⑥`.vue` 瘦身**（规范要求 script 只剩导入 / 实例化 / 解构 / 视图组装）：`App.vue` 的 `gridClass` 与 `stageVisible` → `useLayout`；`UiCard` 的折叠与持久化 → `useUiCard`；`VmDock` 的 computed → `storeToRefs`；`NewProjectDialog` 的落点预览 → composable；`ViewportPanel` 的 `VIEW_TOOLS`（含 4 个内联回调）→ composable——最后这项顺带让工具条四个动作**第一次有了测试**。**⑦文档**：`web-conventions.md` 修掉一处渲染破损（「类型定义」那行被上面的 blockquote 吞掉，表格实际少一行），新增「命名与落点」表（views / components / 根 composables / bench 各自何时用）、补「依赖方向」两条细则（utils 与 render 平级、bench 是唯一例外）、「禁止重复实现」单一来源清单、「大数组与热路径」、资源生命周期约定，并把「`.vue` 的 script 超过导入+解构即算超标」放宽为「导入 / 实例化 store / 解构 / 视图组装常量，不得出现判断与计算」——原文过严，`App.vue` 的面板清单、`UiButton` 的样式映射这类合理配置会被判违规，规则一旦被普遍违反就失去约束力。**验证**：`bun run verify` 全绿（前端 694 条用例、core 行覆盖 100%、四维前端覆盖 100%）。新增 / 补强的用例：退出四分格销毁副视口渲染器（含切回后重建、卸载后全销毁）、工具条四动作、**有场无网格时下发为空转**、`forwardingChannel`、project store 的报告落盘与目录选择失败分支、主题纯核心用例从 composables 拆到 `tests/web/utils/theme.test.ts`。**覆盖率门槛当场抓到一处「改完后不可达」的防御分支**：`attachPointerHandlers` 里的 `el === null` 判空在加了「在途创建回来发现画布已摘掉即销毁」之后再也没有调用路径，按规范**删掉**而不是为它造用例（改为把已收窄的 `el` 作为参数传入）。**顺带发现的文档缺陷**：`timeline.md` 的日期标题自 `## 2026-09-12` 起就断了，09-12 到 09-15 共 192 条记在同一节下（与文首「消除日期断裂」的自述自相矛盾）——核实与修补见下一条记录。
- **11:16** docs(timeline): 补 `## 2026-09-13` / `09-14` / `09-15` 三个日期标题——被上一轮记下的「日期断裂」缺陷 —— 现状是：文首自述「2026-09-12 整体重建自 git log，此前批次补记导致的日期断裂就此消除」，但重建只做到 09-12 那一天，此后 09-12→09-15 的 192 条记录全挂在 `## 2026-09-12` 一节下（**重建当天的意图与实际维护行为脱节**——每条新记录都是往文件末尾追加，没人回头加标题）。**难点是边界不能按行数或时间推**：时间线条目 445 条、commit 458 条，相差 13 条，且**时间戳根本对不上**（时间线记 `08:18` 而 commit 是 `08:19`，多处偏差更大——重建时的时间是手写近似值，不可作锚点）。改用**主题文本的序列对齐**（Needleman-Wunsch：匹配得分取归一化主题的相似度，跳过一个 commit / 一个条目各罚 -0.34，故相似度需 > 0.35 才值得匹配），归一化时**截掉标题尾部的括号补充**（`（T95 P3-b 续）` 这类在重建中被改写过），结果 **444/445 匹配、相似度均值 0.994**，唯一未命中的一条（`feat(web): 新建项目改为「工作区路径 + 项目名」`）前后夹逼都是 09-14，取值唯一。**两重独立验证**：①推算出的日期边界与**已有的 6 个标题逐一对齐**（09-08/09-09/09-10/09-11/09-12 的插入点行号只差标题后那一个空行，说明上下两半用的是同一套判据）；②尾段日期单调不倒流、旧段落标题**零冲突**。顺带定位出 **14 个未记入时间线的 commit**（如 `fix(render): WebGPU 渲染循环不再被首帧异常终止`、`fix(viewport): 渲染失败原因显示到界面`、`refactor(core): [f64;3] 向量原语收成 services/vec3`），这批是后续核对「时间线是否漏记」的线索，本轮不动。最终结构：9 个日期节、445 条记录、每节「空行 + 标题 + 空行 + 条目」格式与重建时一致（脚本核对过每节前后空行与首行形态）。同时修正了上一条记录里「留待单独核对后再补标题」的措辞——缺陷已在本条闭环。docs:check 0 断链 0 孤儿、format:check 通过

- **13:03** fix(core): 隔离每次求解的运行目录，统一 ID/核数校验，保留求解退出码并修复日志帧偏移；新增只读探针采样、缓存清理、网格二进制编码与 DTO 契约。core 522 项测试、行覆盖 100%，三处消融能捕获防御逻辑移除；全量 verify 通过。
- **13:04** fix(app): 接入工程会话重置、保存纪元、独立作业目录、取消令牌和双流日志；渲染/变形使用 Arc 快照及二进制 IPC，探针只读采样与场修订缓存贯通。复查补并发忙碌身份、非 UTF-8 日志和统一场加载；前端 703 项、桌面 32 项测试通过，双端覆盖率达标，提交门禁全绿。
- **13:05** docs(review): 同步 T75/T84/T94/T96、架构生命周期与状态、README 和测试地图；归档评审及闭环报告，新增 T98 实现记录与 T99 后续优化任务，保留真实平台验收边界。文档检查 171 篇、零断链/孤儿，完整提交门禁通过。
- **13:13** refactor(web): 完成 T95 P2-c：CSV 导出统一 RFC 4180 引号、CRLF 与 UTF-8 BOM；工程、流道、水路和自定义材料 ID 改用平台 CSPRNG，旧 WebView 使用单调回退；补充边界回归，前端 706 项测试与四维覆盖率 100%。
- **13:18** refactor(web): 完成 T95 P2-a 可复用收编：方案任务菜单改用 VueUse `onClickOutside` / `onKeyStroke`，工程自动保存继续使用 `useDebounceFn`；核实标题栏菜单和 storage 索引协议保留自定义实现，避免破坏产品语义。
- **13:22** fix(vm): 开始 T97：部署 moldingFoam bundle 前清理受管 `openfoam14` 环境树，避免新旧库叠加；保留解压后 bashrc 复核，core 用例锁定清理 → 建目录 → 解压 → 复核顺序。库布局守卫和真实 VM 坏状态验收列入下一批。
- **13:26** fix(vm): 完成 T97 库布局守卫基础接入：core 生成 source bashrc 后的结构化检查命令，校验三个 OpenFOAM 库目录的重复名字与 solver 相对软链接；原生环境状态新增 `libraryReady` 与独立修复提示。真实 VM 坏状态 / 恢复验收、inode 去重与路径摘要列入下一批。
- **13:31** fix(vm): 完成 T97 库守卫 inode 去重：按设备号 + inode 归并硬链接与同目标软链接，新增 bash 语法回归；本机 Multipass socket 权限不足，真实 VM 坏状态 / 恢复验收保留待可访问环境执行。
- **13:34** docs(tasks): 校正 T95 实际进度：P2-a、P3-b（nalgebra/vec3）、P3-c（bytemuck）与 P3-d（解压库评估）均已有提交并完成，剩余 P3-a 图表仅待视觉对照；同步任务索引与执行清单，避免重复施工。
- **13:36** perf(web): T95 P3-a 先优化现有 Canvas 图表：抽出共享 `chartRange`，绘制与探针标注复用同一值域，避免大场景重复扫描；补齐空值/平坦序列测试，uPlot 替换保留到可渲染桌面环境做视觉对照。
- **13:39** feat(core): 完成 T89 第一步 GAIM 数据位闭环：`RunnerMedium` / `RunnerElement.medium` 双端镜像贯通工程与求解数据路径，补 IPC 契约测试锁定 `gas` 序列化；三相 / 气芯求解仍等待上游能力。
- **13:42** docs(tasks): 核对 T92 已完成实现与任务档案不一致的问题：DOE 矩阵、串行执行、指标汇总和失败行保留均已落地，现从待办表移入已实现，并将 T93 单独标为等待其汇总口径。
- **13:45** docs(tasks): 解除 T93 的过时阻塞标记：T92 汇总表依赖已满足，core 优化器（目标函数、约束、粗搜/细化、失败策略）已有实现；任务索引改为 CLI 寻优循环与真实求解回填待推进。
- **13:48** feat(cli): T93 增加 `optimize plan`：复用 core 优化器按 `名称=min:max:levels` 生成粗搜候选，支持 `fill-time` / `injection-pressure` 与 `--json`；真实求解回填循环留下一批。
- **13:51** test(cli): 补 T93 优化因子解析边界测试，锁定名称、范围、层数格式及缺字段/多字段拒绝；任务执行顺序将 CLI 寻优循环提前到当前可开工批次。
- **13:54** refactor(cli): T93 将 DOE 单次 case 生成、可选求解与日志指标解析收成 `run_doe_case` 唯一函数，保持现有 `doe run` 行为不变，为后续 `Optimizer::record` 回填复用铺路。
- **13:57** docs(tasks): 核对 T60 文档与实现：导入日志、Rust PPTX 生成、报告面板接线和视口/曲线快照均已落地，现从待办表移入已实现，避免重复评估候选库。
- **13:57** docs(tasks): 逐项核对 T95 与当前源码、锁文件和基准：修正 CSV/摘要/VueUse/原子写现状与已完成收编清单，作废 dualdomain/midplane 重复项，补回 gl-matrix 评估、色标不一致、repair 局部向量与摘要展示待办；限定官方摘要校验范围，区分历史性能记录和可复现证据，同步任务索引，未改运行时行为。
- **14:06** refactor(core): T95 P3-b 补漏完成：`repair::triangles_intersect` 移除局部 sub/cross/dot，统一复用 `services::vec3`；保留 Möller 区间相交算法与容差语义，repair 10 项回归全部通过。
- **14:10** refactor(web): T95 色标基础收编：新增 `render/palette.ts`，WebGL/WebGPU 云图端点与视口图例共用蓝橙色标；不引入 d3，保留桌面截图回归作为剩余验收。
- **14:20** refactor(web): T95 收编体积云图色标 —— 发现前一批只统一了表面 WebGPU、WebGL 与图例，`shaders_volume.ts` 仍保留独立蓝橙常量；改为复用 `render/palette.ts`，通过 WGSL 模板注入同一组端点。任务表同步为「基础统一完成」，剩余仅桌面截图视觉回归。
- **14:40** docs/bench(web): T95 图表取舍收口 —— 盘点 XY Canvas 的产品语义（min-max 保形抽样、探针叠加、主题重绘、报告快照、CSV 导出），新增 `src-web/bench/chart.ts` 记录 100 万点值域扫描与抽样基线。uPlot 需要额外适配上述语义，当前没有实测交互瓶颈，且候选未引入不能伪造 gzip 对照；结论为继续自绘，未来出现瓶颈时再按同口径做候选包体与性能对照。
- **14:55** docs(review): T95 前端数学取舍收口 —— 核对 `render/math.ts` 的 10 个矩阵 / 向量原语及 WebGL、WebGPU、拾取、剖切调用点，确认统一列主序 `Float32Array` 且已有矩阵与向量对拍测试。调用面小、未发现数学热点；gl-matrix 会增加数组类型与矩阵约定适配，当前无性能或维护收益证据，结论为继续自写。
- **15:10** refactor(web): T95 图表方向调整为引入 uPlot —— 根据后续功能需要，加入 MIT 许可 `uplot@1.6.32`，先建立 `uplot-adapter.ts` 配置边界和契约测试；XY 面板现有 Canvas / 探针 / 主题 / 报告快照暂保留，下一批分步迁移，避免一次性破坏桌面截图与现有交互。
- **15:25** feat(web): uPlot 探针叠加 —— 空间模式将每个探针编码为独立点 series（非探针节点为 NaN），时间模式保留每探针曲线；探针列表或模式变化时重建 series 配置，数据更新仍走 `setData`。缩放 / 拖拽与探针标记现在共用同一 uPlot 交互层。
- **15:40** feat(web): uPlot 主题联动 —— 坐标轴与网格颜色改从 `--c-text-muted` / `--c-grid` 读取；主题事件销毁并重建 plot，使深浅主题切换不残留旧颜色。图表容器背景继续沿用面板主题。
- **16:00** feat(web): uPlot 缩放复位 —— XY 面板新增「重置缩放」，同时清除 x / y 两轴的 uPlot scale，回到全场或全时间范围；不触碰结果数据与探针状态。
- **16:20** docs(bench): T95 uPlot 准入证据补齐 —— `uplot@1.6.32`（MIT）已进入直接依赖；`bun run build` 实测 JS 595.24 kB / gzip 197.07 kB、CSS 64.27 kB / gzip 15.15 kB。Vite 提示 JS chunk 超过 500 kB，记录为后续代码分割优化点。
- **17:05** cleanup(web): T95 移除旧 Canvas 自绘 —— uPlot 已覆盖 XY 绘制、探针、主题、缩放、快照与自适应尺寸，删除 `utils/chart.ts::drawLineChart` 及其专用 fake Canvas 测试；`utils/chart.ts` 只保留值域 / 降采样数据工具与 CSV 导出，文档同步改为渲染层走 `render/uplot-runtime`。
- **17:30** fix(ci)+build: 修复 Windows / Ubuntu CI 并消除 Vite 大 chunk 警告 —— Windows `ProcessRunner` 测试改用 `cmd /C` 可识别的 `exit /b` 语法；Ubuntu CI 设置 `RUST_TEST_THREADS=1`，规避 wgpu GLES 软件适配器并行测试的 adapter context deadlock。Vite 按 KaTeX / uPlot / Vue vendor 拆包，主入口从 595 kB 降至 205 kB，构建不再提示超过 500 kB。

- 2026-09-15 16:22 | 视口回归 | 修复 WebGPU 着色器重复声明、导入自动加载与刷新失效、四分格只读绑定；按模型取景和动态远裁剪，增加真实 GPU 浏览器夹具与自动同步测试。
- 2026-09-15 16:30 | T99 第一批 | 新增可注入作业生命周期状态机，锁定准备取消、启动失败、远端状态未知/恢复和非法事件回滚契约。
- 2026-09-15 16:41 | T99 第二批 | 将真实 Tauri runner 的准备/启动/成功/失败/取消确认/远端未知事件接入生命周期状态机。
- 2026-09-15 16:52 | T99 第三批 | 标量场 CSV 编码下沉 core，Tauri 通过阻塞线程池导出，前端保留本地派生场回退并补充异步导出回归。
- 2026-09-15 17:05 | T99 第四批 | 结果会话显式绑定算例目录；切换运行时原子清空标量、矢量槽位和 LRU 缓存，目录重扫仍按文件修订失效。
- 2026-09-15 17:18 | T99 第五批 | EnvironmentCheck / CaseOutcome（含 GateInlet）迁入 core models，Tauri 仅做命令适配；补充 Rust DTO camelCase 契约测试。
- 2026-09-15 17:31 | T99 第六批 | 结果加载加入算例代际与槽位请求序号；切换算例或同槽位发起新请求后，旧异步返回不会回写当前会话。
- 2026-09-15 17:45 | T99 第七批 | 新增 core `FieldStats` 与 `summarize_result_field` IPC/API，结果场统计可在 Rust 单次扫描完成，保留前端面板接线待办。
- 2026-09-15 18:02 | T99 第八批 | 矢量模量统计下沉 core，新增 `summarize_vector_field`，store 与结果面板改用 Rust 统计并保留首单元分量展示。
- 2026-09-15 18:16 | T99 第九批 | 对称张量模量统计下沉 core，新增 `summarize_tensor_field`，张量结果面板改用 Rust 统计。
- 2026-09-15 18:29 | T99 第十批 | 补充不完整结果不入缓存、文件更新后重新读取的故障回归，锁定求解中途写文件不会复用旧场。
- 2026-09-15 18:43 | T99 第十一批 | 新增双字段并发读取回归，验证磁盘读取在锁外执行、主/对比槽位与缓存不会交叉污染。
- 2026-09-15 19:02 | T99 第十二批 | 结果会话主/对比槽位改用 `Arc<ScalarField>`，派生与差值复用共享场对象，避免槽位间重复持有大值数组。
- 2026-09-15 19:20 | T99 第十三批 | 新增百万值大结果可复现基准：core 统计约 5.8 ms、CSV 编码约 211 ms、载荷约 27.5 MB（本机 arm64 / rustc 1.98.1 实测）。
- 2026-09-15 19:36 | T99 第十四批 | 结果修订缺失场景补充 IPC 错误码契约回归，锁定 `cache_key` 文件失败统一映射为 `io`。
- 2026-09-15 19:52 | T99 复核 | 对照 T92/T93 逐项核验：T92 算法、串行 DOE 编排、汇总与失败行均已完成；T93 仍缺真实求解回填循环，T99 保持进行中并明确唯一阻塞项。
- 2026-09-15 17:31 | T100 真实 CLI 批次 | 以 moldingFoam v1.1.0 目标运行时启用 `doe run --solve`，9 个参数点均因环境缺少 `decomposePar` 失败；保留逐点日志、时间戳、退出码和原始输出，T93/T100 继续等待可用求解环境。
- 2026-09-15 17:45 | T100 CLI 契约修复 | DOE 批次继续保留全部失败点与汇总，但存在失败运行时返回 `solver` 错误和非零退出码，避免自动化把求解环境失败误判为成功。
- 2026-09-15 17:50 | T100 GUI/CLI 环境统一 | CLI 新增 `doe run --solve --vm`，复用 GUI 已部署的 Multipass `kairos:~/moldingfoam-env`，完成打包、传输、求解和结果回传编排；本机复验因 Multipass socket 权限阻塞并留档。
- 2026-09-15 17:42 | T93/T100 真实回填 | 在已部署的 Multipass `kairos` VM 中以 moldingFoam v1.1.0 完成 9/9 个 DOE 参数点真实求解，成功回传结果与时间戳；T93 剩余自动寻优迭代与失败点惩罚汇总。
- 2026-09-15 18:02 | T100 Mug baseline 口径 | 从 `report/mug-moldflow` 固定模型、工艺、参考指标与材料字段，补充 Generic PP 公开典型值的合理性检查；4 mm 体积网格试跑推进至 0.838 s 后因成本停止，原始日志留档，不计入对比通过。
- 2026-09-15 18:15 | T101 立项 | 新增 Generic PP 自定义材料任务：接入受控 PVT/黏度曲线，拟合并回代验证 Tait 与 Cross-WLF 参数，独立导入材料后重新生成 Mug baseline。
- 2026-09-15 18:30 | T101 输入契约收口 | 去除受限原始文件名和“用户提供”依赖，改为通用 PVT/黏度曲线输入契约；记录 Kairos 当前缺口：曲线解析、Rust 拟合、残差报告、材料版本元数据、CLI/IPC/界面选择入口与合成数据契约测试。
- 2026-09-15 18:40 | T101 曲线输入层 | 在 `kairos-core` 增加通用 PVT/黏度 CSV 解析，完成单位归一、有限值/正值校验、排序和重复点拒绝；使用合成夹具覆盖正常、空数据、非法单位和重复点。
- 2026-09-15 18:55 | T101 Cross-WLF 评估 | 增加 Cross-WLF 黏度预测与残差摘要，输出绝对误差、RMSE 和 `log10(η)` 误差；合成数据验证零残差与非法输入拒绝。
- 2026-09-15 19:10 | T101 Tait 评估 | 增加 Kairos Tait 比容预测与残差摘要，覆盖固态/熔态分支、绝对/相对误差和 RMSE；合成数据验证零残差与非法输入拒绝。
- 2026-09-15 19:20 | T101 残差留档 | 增加 Cross-WLF/Tait 残差摘要 JSON 与 CSV 序列化，供 CLI 和基线报告直接留档；逐点预测导出继续推进。
- 2026-09-15 19:25 | T101 运行时边界 | 固化 Rust 拟合/校验、moldingFoam 运行时逐单元计算的职责边界，并将跨语言 golden 点校验列为后续验收项。
- 2026-09-15 19:35 | T101 材料文件读取 | 增加 core 按 `.json`/`.csv` 扩展名读取并校验自定义材料的入口，保留旧兼容读取函数；CLI 材料选择接线待完成。
- 2026-09-15 19:45 | T101 CLI 材料选择 | `doe run` 增加 `--material <json|csv>`，单材料文件可直接进入 case 生成与求解，未指定时保持内置材料兼容路径。
- 2026-09-15 19:55 | T101 Pipeline 材料选择 | `pipeline run` 增加 `--material <json|csv>`，单次 case 编排与 DOE 使用同一材料加载路径。
- 2026-09-15 20:05 | T101 材料校验 CLI | 增加 `material validate --path <json|csv>`，输出材料数量、牌号、材料族和 ID，作为进入 case 前的显式校验步骤。
- 2026-09-15 20:20 | T101 材料拟合 CLI | 增加 `material fit --template --viscosity --pvt --output`，执行 D1 与 Tait b1 的确定性尺度回填并输出残差；完整多参数优化和真实 Mug baseline 待完成。
- 2026-09-15 20:35 | T101 逐点残差 | `material fit` 额外输出黏度/PVT 逐点残差 JSON 文件，记录输入坐标、实测值、预测值和误差，支持后续基线审计。
- 2026-09-15 20:45 | T101 Golden 点 | 固定 Cross-WLF 黏度与 Tait 比容参考点测试，作为 moldingFoam 运行时实现的跨语言数值对照基准。
- 2026-09-15 21:00 | T101 真实曲线插入 | 本地受控曲线完成 250 个 PVT 点与 200 个黏度点解析；原始模板 Tait 压力尺度不兼容被显式拒绝，合成模板仅用于验证 fit 输出链路。
- 2026-09-15 21:15 | T101 TXT 原生输入 | core 与 CLI 原生支持分段 `P=...` / `T=...` TXT 曲线，真实本地曲线无需外部转换即可完成 material fit 插入测试；原始文件仍只保留本地。
- 2026-09-15 21:30 | T101 Tait parity audit | 复核 moldingFoam v1.1.0 源码，确认运行时采用 `B(T)=b3·exp(-b4T)`、压力相关转变温度与固/熔态平滑过渡；当前简化 Tait 评估器不能作为 solver 等价结果，DTO 扩展与重拟合列为下一项。
- 2026-09-15 21:45 | T101 Tait DTO 对齐 | Rust `Tait` 增加 `b3s/b6/C/smoothBand`，评估器改用 moldingFoam 的 `B(T)`、压力转变温度和 Tait 对数项；旧材料 JSON 通过默认值兼容。
- 2026-09-15 22:00 | T101 Tait 平滑过渡 | 参考 CAE Specification 增加固/熔态 smoothstep 过渡，避免转变温度附近比容不连续；golden 与异常输入测试通过。
- 2026-09-15 22:15 | T101 solver 字典导出 | `material fit` 额外生成 moldingFoam SI 材料字典，固定 `equationOfState` 与 `CrossWlfCoeffs` 键名，供 case 生成器和跨语言 golden 对照使用。
- 2026-09-15 22:30 | T101 case 参数接线 | moldingFoam case 生成器改用材料 DTO 的 `b3s/b4m/b4s/b6/C/smoothBand`，移除原先硬编码的 `b4/b6/C/smoothBand`，并通过 moldingfoam 测试。
- 2026-09-15 22:45 | T101 VM 版本阻塞 | 新 case 在 Multipass VM 被旧版 solver 以缺少 `b4` 拒绝；不添加兼容键，要求先升级 GUI/VM 的 moldingFoam 部署后再做运行时 golden 验收。

- 2026-09-15 22:55 | T101 solver baseline | 按 moldingFoam v1.1.0 正式 Tait 字典输出 `b4/b4s`，在更新后的 Multipass VM 中完成自定义材料 case 真实求解，退出码 0、Cross-WLF/Tait 选型与 `End` 收尾证据齐全。

- 2026-09-15 23:05 | T100/T101 材料快照 | 将 PP-REF-01 的 Cross-WLF、Tait、热性能和机械字段写入 Mug 基线脱敏快照；明确 Mug DOE 的三水平约束与后续单点 baseline 入口需求。

- 2026-09-15 23:15 | 默认 PP 材料更新 | 将 PP-REF-01 的脱敏拟合结果同步为 Kairos 内置 PP 默认模板，保留授权与实测复核说明。

- 2026-09-15 23:30 | 默认 PP 测试基线 | 更新 moldingFoam 字典测试对 PP-REF-01 新 b5=400 K 的断言，默认材料切换后核心测试恢复通过。

- 2026-09-15 23:50 | T100 工艺曲线接线 | CLI 新增完整保压曲线与冷却时间参数，Mug 最终命令可直接表达 setting.txt 的工艺输入；新增曲线解析校验测试。

- 2026-09-16 00:05 | T102 建立 | 新建 Dual Domain Mug 完全一致基线任务，冻结网格、材料、工艺和指标参数，明确双面厚度积分与拓扑消费验收。

- 2026-09-16 00:20 | T102 阶段契约 | CLI 新增 `--stage fill-pack-cool`，最终 Mug 命令可执行完整填充/保压/冷却阶段；Dual Domain 网格消费仍待实现。

- 2026-09-16 00:35 | T102 双域拓扑前置 | 增加 Dual Domain solver 拓扑校验，拒绝未配对厚度、越界索引和非法网格；明确 moldingFoam 降维消费仍是后续实现。

- 2026-09-16 00:50 | T102 路线确认 | 核对 moldingFoam v1.1.0 仅消费三维体网格；冻结 DualDomainSolverInput 契约与显式降维 solver 扩展路线，禁止静默回退。

- 2026-09-16 01:00 | T102 输入契约 | 新增版本化 `DualDomainSolverInput` DTO、单位字段、拓扑转换和 Rust/IPC 契约测试，供上游 moldingFoam 降维模块消费。

- 2026-09-16 01:20 | T102 上游输入导出 | CLI 新增 `dual-domain export`，可从 STL 生成版本化 `dual-domain/v1` JSON，供 moldingFoam 上游模块直接联调。

- 2026-09-16 01:35 | T102 联调 fixture | 提交 `dual-domain/v1` sample-box JSON fixture 与 round-trip 契约测试，上游可先完成无专有资产的读取实验。

- 2026-09-16 01:50 | T102 实验 manifest | 新增 Dual Domain 网格、PP-REF-01 和 fill-pack-cool 工艺参数的机器可读实验 manifest，上游更新后可直接执行完整 smoke test。

- 2026-09-16 02:10 | T102 Mug STL 导出收口 | Dual Domain CLI 导出改为强制真实 STL，移除物理语义不成立的闭合方盒入口；用本地 Mug STL 生成 4417 节点 / 8834 三角形 `dual-domain/v1` JSON，并补充 GUI 工艺面板完整保压曲线输入与 Mug 基线预设。

- 2026-09-16 02:40 | T102/T103 路线收口 | 明确三维体网格作为当前 moldingFoam 可运行主路线，Dual Domain 保留上游降维 solver 扩展空间；新增 GUI 网格与工艺入口对齐任务，记录导出、能力提示和基线单一来源待办。

- 2026-09-16 02:55 | T95 收尾清理 | 移除快照导出和报告类型的未使用导出，knip 与 ESLint 通过；保留报告生成和快照注册实际消费的最小 API。

- 2026-09-15 20:35 | T93 smoke | 执行单点 DOE 求解回填 smoke；本机缺少 `decomposePar`，结果按契约标记 failed 并保留原因/耗时，验证失败点可见性。

- 2026-09-15 21:05 | T93 CLI 回填循环 | 新增 `optimize run`：候选执行、失败重试、指标回填、连续失败中止与 `optimize/summary.json` 汇总；sample-box smoke 已验证失败历史可见。

- 2026-09-15 21:25 | T93 约束接入 | `optimize run` 在执行 case 前复用 core `process::validate`，将非法候选记录为约束违反并跳过求解；优化器单元测试 10 项通过。

- 2026-09-15 21:45 | T82/T102 延后决策 | Mug 当前实验统一采用三维体网格，接受更长计算时间；Dual Domain solver 消费暂缓，不阻塞 v1.0.0，保留契约与精简 fixture 供后续恢复。

- 2026-09-15 22:00 | T93 收口 | 将 CLI 自适应寻优、约束过滤、失败重试/中止和汇总标记为已实现；T99 解除 T93 内部实现阻塞，真实 VM 复测归入 T100/T94。

- 2026-09-15 22:15 | T103 UI 标注 | 几何面板将“双域网格”标为实验前处理，明确当前 moldingFoam 只支持三维体网格求解，并补充 GUI 回归测试。

- 2026-09-15 22:30 | Mug 三维体网格基线 | 明确当前实验使用 5.0 mm 体积网格；几何/材料/工艺阶段已对齐，网格类型、浇口边界和离散仍只做量级对照，Dual Domain 延后。
