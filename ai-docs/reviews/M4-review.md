# M4 里程碑评审报告（T21）

- 日期：2026-09-08
- 范围：T13（结果模型与流式读取）、T14（3D 视口渲染引擎）、T15（XY 曲线与探针）、T16（设计系统与工作台布局）、T17（仿真报告导出）、T19（GPU 基础设施）、T20（GPU 加速算子）
- 结论：**通过**，M4 关闭，可开启 M5 交付。3 项立即修（已当场完成），2 项接受并记录。

## 八项清单结论

| #   | 维度           | 结论与证据                                                                                                                              |
| --- | -------------- | --------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | 架构一致性     | ✓ core 零 tauri 依赖（grep = 0）；组件零直接 invoke（grep = 0）；结果模型/报告模型在 core，进程副作用在适配层                           |
| 2   | 性能预算对照   | ⚠ 视口 FPS 埋点已接入（百万三角形正式计时待真实大网格资产）——记录为接受项；图表走 min-max 抽样（万点保形）                              |
| 3   | 代码与注释质量 | ✓ 无 TODO/FIXME；视口着色器/剖切/LOD 注释解释约束；探针校验规则与 core 一致                                                             |
| 4   | 文档同步       | **立即修（已完成）**：README 领域功能句补齐 M4 落地内容（求解集成/结果读取/视口/曲线/报告/GPU）                                         |
| 5   | 测试覆盖       | ✓ 前端 39（含图表抽样/CSV/报告转义/数学库）+ Rust 62+6（结果解析器 7 项：uniform/nonuniform/矢量模量/截断容错/扫描排序/读写）           |
| 6   | 依赖健康       | ✓ 新增 wgpu（MIT/Apache）、pollster（MIT/Apache）、bytemuck（Zlib/MIT/Apache）——均为宽松许可且为主流程必需                              |
| 7   | 安全           | ✓ **立即修（已完成）**：spawn_run_script 对 case 路径做单引号转义防注入；环境探测用固定常量命令；**接受**：CSP null 维持至 T18 分发收口 |
| 8   | 遗留追踪       | ✓ M3 接受项兑现：**cargo audit 已进 CI**（audit job）；KairosError 错误链已于 T12 兑现；网格正式计时仍缺真实资产（维持）                |

## 接受项（进入 M5 评审重评）

1. 网格生成/视口正式性能计时 —— 待真实大网格基准资产（与 T13 真实结果样例同批）。
2. CSP null → 分发前在 T18 收窄（本次收口）。
3. WebGPU 渲染后端 —— WebView 普遍支持后接入（当前 WebGL2 已满足性能预算路径）。

## M4 声明的遗留

- 报告 PDF：v1 经浏览器打印管线，原生 PDF 导出远期评估。
- 时间步动画完整时间轴：随 T13 结果目录加载深化（数据通路已就绪）。
