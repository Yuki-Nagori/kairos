# T49 评审记录（T21 清单 · 随任务执行）

评审对象：useViewportPanel 多实例重构、renderer（GL / WebGPU）轨道方法、
viewport store 布局状态、ViewportBackend 接口、视口模板（布局选择 / 四分格）、
store 单测。

| #   | 清单项         | 结论                                                                                                                                                        |
| --- | -------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | 架构一致性     | 通过。布局 / 图层意图在 store（跨面板共享），渲染器方法面在 backend 接口收敛；无 IPC / core 改动                                                            |
| 2   | 性能预算对照   | 四分格 = 4 个渲染实例各自 RAF——四分格总绘制成本 ≈ 4×单视口；中等网格（数万面）无压力，1M 三角形 × 4 实例的帧率预算随 T48 真机基准一并度量，此处不设独立预算 |
| 3   | 代码与注释质量 | 通过。联动语义（源实例 → 其余实例）有注释；`zoomBy` 以主视口为源等比传递避免叠加缩放；无 TODO/FIXME；清除了 reactive 引入后的占位语句                       |
| 4   | 文档同步       | 通过。architecture-status「多视口联动 / 剖面切片」拆分记账：多视口联动 + 相机/时间轴同步完成，剖面切片已在 T47 完成；剩余体渲染与大结果数据链               |
| 5   | 测试覆盖缺口   | 通过（按豁免口径）。viewport store 布局单测补齐（覆盖率门槛文件）；相机联动的运行时行为属命令式 WebGL 集成豁免，真机验证并记入 T48 走查清单                 |
| 6   | 依赖健康       | 无新增依赖                                                                                                                                                  |
| 7   | 安全           | 不涉及                                                                                                                                                      |
| 8   | 上轮遗留       | T39 评审回流项 T48（三端真机）保持跟踪；体渲染 POC 与大结果数据链按目标序列推进                                                                             |

## 处理记录

- **立即修**：`setupSlot` 初版存在双重守卫矛盾（先排除非空再排除空 →
  never 类型），改为局部捕获后判定，vue-tsc 闭环。
- **回流任务**：无新增（WebGPU 交互事件单向性记入 T39 POC 边界，
  随 T48 真机验收一并处理）。
- **接受并记录**：WebGPU POC 作为联动事件源不可用（仅可作目标）；
  四分格共享同一几何，不支持各视口独立几何加载（当前无使用场景）。
