# T46 评审记录（T21 清单 · 随任务执行）

评审对象：stores/results.ts（probeTimeSeries / loadProbeTimeSeries / removeProbe
联动）、useXyChartPanel（模式切换 / drawTimeMode / jumpToTime）、XY 面板模板、
types（ProbeTimeSeries）、前后端测试。

| #   | 清单项         | 结论                                                                                                                                                       |
| --- | -------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | 架构一致性     | 通过。序列数据仅经既有 `load_result_field` IPC 获取，无新命令、无新 DTO 跨界（ProbeTimeSeries 为前端状态类型，不上 IPC）；组件经 store 取数，不直接 invoke |
| 2   | 性能预算对照   | 序列加载为 N 个时间步 × 整文件读取，O(N × 文件体积)——预算对照记入「当前实现边界」，二进制分块归大结果数据链任务；无持续性能回归                            |
| 3   | 代码与注释质量 | 通过。`loadProbeTimeSeries` 的守卫与恢复语义有注释；「跳转到时间步…」空选项即守卫路径；无 TODO/FIXME、无死分支（移除了数学上不可达的配色 `??` 回退）       |
| 4   | 文档同步       | 通过。architecture-status XY 曲线行更新为「曲线/时间轴联动已接入（T46）」，多视口联动保留待办                                                              |
| 5   | 测试覆盖缺口   | 通过。新增 store 3 测试 + 面板 3 测试（序列采样与恢复、守卫静默、失败报错、移除过滤、越界回退 0、时间模式绘制、跳转加载）；四维覆盖率 100%                 |
| 6   | 依赖健康       | 无新增依赖（knip 随 verify 通过）                                                                                                                          |
| 7   | 安全           | 不涉及                                                                                                                                                     |
| 8   | 上轮遗留       | T44「接受并记录」项（派生不持久化 / GPU 归 T39）不变；T45「回流」项（时间序列联动）本轮完成闭环                                                            |

## 处理记录

- **立即修**：`jumpToTime` 与 `loadTimeSeries` 的守卫分支在初版测试中未覆盖
  （分支覆盖 94%），补守卫用例后闭环。
- **回流任务**：多视口联动单列（待 T39 渲染后端决策后规划，避免在旧后端上
  重复投资）。
- **接受并记录**：时间曲线 x 轴为时间步序号（等距），物理时间刻度需引入
  通用时间轴组件，重评条件为大结果数据链任务启动。
