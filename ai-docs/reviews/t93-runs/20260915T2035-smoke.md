# T93 smoke · DOE case 回填链路

- 时间：2026-09-15 20:35（Asia/Shanghai）
- 命令：

```bash
cargo run -p kairos-cli -- doe run --plan full \
  --factor '熔体温度=220' --factor '注射时间=5' \
  --sample-box --target-size 3.0 \
  --out-dir /private/tmp/t93-smoke-solve \
  --batch t93-smoke-solve --solve --json
```

- 结果：`status=failed`
- 失败原因：`decomposePar: command not found`
- 耗时：`0.015677 s`
- 结论：DOE 运行器正确保留失败参数点、原因和耗时，批次不会静默丢失；本机未安装 OpenFOAM，未形成真实求解指标，因此不宣称 T93 已完成。
