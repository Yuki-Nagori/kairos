# M0 里程碑评审报告（T21）

- 日期：2026-09-07
- 范围：T01（性能预算与基准框架）、T02（CI/CD 流水线）
- 结论：**通过**，可开启 M1。1 项立即修（已当场完成），0 项回流，3 项接受并记录。

## 八项清单结论

| #   | 维度           | 结论与证据                                                                                                        |
| --- | -------------- | ----------------------------------------------------------------------------------------------------------------- |
| 1   | 架构一致性     | ✓ core 无 tauri 依赖（grep 证实）；组件无直接 invoke；本里程碑未新增 DTO                                          |
| 2   | 性能预算对照   | ✓ 预算六项已定义；基线已回填（store 40–95 ns、DTO 序列化 ~86 ns）；网格/结果/视口类基准随 T06/T13/T14 落地回填    |
| 3   | 代码与注释质量 | ✓ 无 TODO/FIXME；system_info 命名链一致；基准文件带预算引用注释                                                   |
| 4   | 文档同步       | **立即修**：ARCHITECTURE.md 未引用 perf-budget.md → 已补 §6 引用；README 命令表已含 bench                         |
| 5   | 测试覆盖       | ✓ 前端 15 + Rust 4 全绿；M0 无新领域逻辑；services/app.ts 经 state 测试间接覆盖                                   |
| 6   | 依赖健康       | ✓ knip 通过；新增 criterion 0.5.1（MIT/Apache 双许可）、tinybench（MIT），均为 dev 依赖，许可证合规               |
| 7   | 安全           | ✓ capabilities 最小（core:default）；工作流无密钥注入；**接受**：CSP null 维持至分发前（T18 处理）                |
| 8   | 遗留追踪       | ✓ 零提交已解决（12 commits）；冒烟已通过；CI 已落地；**接受**：KairosError 错误链维持至 core 出现底层错误包装需求 |

## 接受并记录（进入 M1 评审重评）

1. CSP 为 null —— 触发条件：对外分发（T18 前收窄）。
2. KairosError 无 source 错误链 —— 触发条件：core 开始包装 io/解析错误。
3. `cargo audit` / 依赖漏洞通告未纳入 CI —— 触发条件：依赖树扩大后（M2 评审重评）。
