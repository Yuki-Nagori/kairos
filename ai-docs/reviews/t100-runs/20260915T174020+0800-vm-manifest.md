# T100 GUI 部署环境对接复验

- 时间：2026-09-15 17:40:20 +0800
- 入口：`kairos-cli doe run --solve --vm`
- GUI 环境目标：Multipass 实例 `kairos`，bundle 路径 `~/moldingfoam-env`
- 输出目录：`/private/tmp/t100-vm-20260915T174020+0800`
- 原始输出：[20260915T174020+0800-vm.raw.txt](20260915T174020+0800-vm.raw.txt)
- 参数点：9
- CLI 退出码：`1`

## 结果

CLI 已切换到与 GUI 相同的 VM 传输链路，但本机调用 Multipass 时返回：

```text
transfer failed: multipass socket access denied
```

因此本次未进入求解器。该结果确认了宿主 PATH 与 GUI VM 环境的分叉已修复为可显式选择的 `--vm` 通道；剩余阻塞是当前桌面会话对 Multipass socket 的权限，而不是 case 或 bundle 路径不一致。
