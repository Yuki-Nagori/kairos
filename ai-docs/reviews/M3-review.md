# M3 里程碑评审报告（T21）

- 日期：2026-09-08
- 范围：T09（OpenFOAM 集成）、T10（作业调度器）、T11（填充端到端闭环）、T12（保压与冷却分析）
- 结论：**通过**，M3 关闭。求解链路（case 生成 → 调度 → 子进程 → 进度解析）全部就位并有测试覆盖；真实求解运行依赖用户环境安装 OpenFOAM 7 + openInjMoldSim（分发策略见 T18）。

## 八项清单结论

| #   | 维度           | 结论与证据                                                                                                                          |
| --- | -------------- | ----------------------------------------------------------------------------------------------------------------------------------- |
| 1   | 架构一致性     | ✓ core 零 tauri 依赖（grep = 0）；case 生成 / 进度解析为纯函数；子进程副作用全部在适配层                                            |
| 2   | 性能预算对照   | ✓ 调度器并发预算（同时运行数 + 核数）落地；网格生成正式计时仍未做（基准资产待真实 STL，继承自 M2 接受项）                           |
| 3   | 代码与注释质量 | ✓ 无 TODO/FIXME；packingDict / coolingDict / expected_fields 均带物理含义注释；阶段字典测试锁定行为                                 |
| 4   | 文档同步       | ✓ 合规结论入库 `ai-docs/decisions/openfoam-gpl-compliance.md`；阶段化结果场目录带单调性测试                                         |
| 5   | 测试覆盖       | ✓ Rust 60（core 单测 56 + 契约 6 + 调度器 jobs 单测 5 含非法迁移穷举）；前端 24；覆盖双四面体 case 文件计数、进度解析、阶段字典渐进 |
| 6   | 依赖健康       | ✓ 无新增外部依赖（std 进程管理）；serde 回补到适配层（EnvironmentCheck 序列化必需）                                                 |
| 7   | 安全           | ✓ 子进程以独立进程组启动（unix process_group(0)），取消时整组终止无孤儿；**接受**：CSP null 维持至 T18                              |
| 8   | 遗留追踪       | ✓ **M0 接受项「KairosError 错误链」本轮兑现**（From<io::Error> 已补）；CSP 维持（T18）；cargo audit → **升级为 M4 评审加入**        |

## 接受项（进入 M4 评审重评）

1. 网格生成 100 万四面体正式计时 —— 待真实 STL 基准资产。
2. cargo audit 进 CI —— M4 评审加入。
3. 求解器专属字典的精确校准（transportProperties 键名与 openInjMoldSim v7.2 逐项对照）—— 依赖真实运行环境，T11 端到端验证时完成。

## T12 声明的遗留

- 翘曲 / 纤维取向：openInjMoldSim 未覆盖，远期评估（结构求解器选型 + openInjMoldSimF 变体）。
- 冷却模具热应力：同上。
