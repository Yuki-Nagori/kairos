# moldingFoam v0.2.4 · 保压 ramp 实验矩阵（E1–E7 可跑部分）

- 日期：2026-09-13
- 环境核对：VM 内 `libmoldingFoam.so` 的 sha256 = `01126592…21ee1`，
  与官方 `moldingFoam-v0.2.4-arm64.tar.xz`（113,864,688 B）逐字节一致——部署到位。
- 回传材料：`samples/cases/v024-ramp-matrix/`（各用例 log 全文、moldingDict、
  physicalProperties.melt、`0/U`、以及 `switch-and-pgate.txt` 的逐时刻
  `p_gate / p_target` 序列；E3/E7 另附 4.0 s 与 8 s 两帧）

## 一、环境与范围

本次矩阵跑在**已发布的 v0.2.4 bundle** 上（部署核对：VM 内 `libmoldingFoam.so` 的
sha256 与官方 arm64 资产逐字节一致）。

上游的「ramp 缺省自适应 + 回显补充」不在 v0.2.4 里——启动回显仍是
`pressureRamp = 0.05`，`libmoldingFoam.so` 里也没有对应的新回显字符串
（该线走分支对接，未并入 release 资产）。因此：

- 需要含该分支的 bundle 才能验证 E1 与「按自适应口径」的 E4；
- 本报告其余部分与实测数据在 v0.2.4 上均成立。

## 二、本次在 v0.2.4 上跑的矩阵

用例 C = 873 cm³ 真实件（材料已修：Pr=1.388889e6 → k=0.18；守卫开启除注明外），
样例盒 = 点浇口样例盒。每次只改一个变量，同一 bundle 与环境，4 rank 串行执行。

| #   | 用例   | 变量                          | 实测                                                           | 预期                   | 判定                                                      |
| --- | ------ | ----------------------------- | -------------------------------------------------------------- | ---------------------- | --------------------------------------------------------- |
| E3  | C      | `pressureRamp 0.2` 显式       | 跑到 **endTime 8 s**，填充 **0.99567**，exit 0，1 条无害警告   | 稳定                   | ✅ 基线                                                   |
| E2  | C      | `pressureRamp 0.05` 显式      | 切换 t=3.92096 s 后 **non-finite**（4 rank），最后填充 0.97228 | 复现                   | ✅ 复现（与 v0.2.3 那次**逐位一致**）                     |
| E5  | 样例盒 | 不写 ramp（v0.2.4 缺省 0.05） | 跑到 endTime 2 s，填充 **0.99466**，exit 0                     | 通过                   | ✅                                                        |
| E4  | C      | 不写 ramp + **守卫关**        | 同 E2：t=3.920958583275949 s non-finite，最后填充 0.97228      | （按自适应预期应通过） | ⚠️ 死于缺省 0.05 而非守卫；**恰好完成了守卫隔离**（见下） |
| E7  | C      | `pressureRamp 0.5`            | 跑到 endTime 8 s，填充 **0.99568**，exit 0                     | 稳定、看滞后           | ✅ 窗口上界不紧                                           |

E1（不写 ramp，验自适应）**未跑**——需含自适应分支的 bundle。E6（修 8 mm 浇口）待 T72。

## 三、三条硬证据

**1. ramp 速率是触发量（E3 vs E2 的 p_gate 序列）**

E3（0.2 s → 约 0.28 GPa/s）切换后平滑爬升：

```
packing: t = 3.921248 s, p_gate = 5034897 Pa,  p_target = 5.999293e+07 Pa
packing: t = 3.922387 s, p_gate = 5351476 Pa,  p_target = 5.999009e+07 Pa
packing: t = 3.923117 s, p_gate = 5554536 Pa,  p_target = 5.998826e+07 Pa
packing: t = 3.927319 s, p_gate = 6722574 Pa,  p_target = 5.997776e+07 Pa
```

E2（0.05 s → 约 1.1 GPa/s）同一时段把压力推得快约 4 倍，0.4 ms 内
6.27 → 6.70 MPa 后即 non-finite：

```
packing: t = 3.920761 s, p_gate = 6486240 Pa, p_target = 5.999415e+07 Pa
packing: t = 3.920950 s, p_gate = 6696427 Pa, p_target = 5.999368e+07 Pa
```

**2. 冻死守卫对正常工况零影响**：E4（守卫关）与 E2（守卫开）的
`switch-and-pgate.txt` **逐行完全相同**（同一时刻、同一压力、同一崩溃点）——
守卫只在冻结时介入，不扰动正常轨迹。

**3. 失稳是确定性的**：E2/E4 的崩溃时刻 `t = 3.920958583275949 s` 与 v0.2.3 上
同配置那次**逐位相同**，可作 039 的下界证据留档。

## 四、结论与待办

1. ramp 窗口下界在 `0.05` 与 `0.2` 之间（0.05 崩、0.2 与 0.5 均通）；
   若自适应默认确如回执所述（切换时刻的 5%，夹 [0.05, 0.5]），C 的 0.196 s
   落在安全侧（与本次 E3 的 0.2 同窗）；
2. Kairos 生成器已按该口径写 `packing.pressureRamp`（生成期按
   「注射时间 × 切换分数」预估切换时刻：873 cm³ / 4 s / 0.96 → 0.192 s）；
3. 待办：E1 复跑（等含自适应分支的 bundle）；E4 按自适应重跑（同上）；
   E6 待 T72（浇口入口面口径）修复后跑；
4. 本次全程未再出现 `freeze-off` 消息——材料修复后确实没有冻结 ✓。
