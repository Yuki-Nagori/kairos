# M5 里程碑评审报告（T21 · 收官）

- 日期：2026-09-08
- 范围：T18（打包分发与发布流水线）+ 全项目收官审计
- 结论：**通过**。21 个计划任务全部完成（T01–T20 + T21 评审循环），M0–M5 六个里程碑全部关闭。

## T18 交付核验

| 交付物                  | 证据                                                                                                                 |
| ----------------------- | -------------------------------------------------------------------------------------------------------------------- |
| Release 工作流          | `.github/workflows/release.yml`：tag 触发 → macOS 双架构 + Windows 构建 → 产物收集 → Release 草稿                    |
| 生产 CSP 收窄           | `tauri.conf.json` csp 收紧为 self/ipc/data/blob 白名单；devCsp 保持开放（开发体验）                                  |
| OpenFOAM 运行时分发策略 | `ai-docs/decisions/openfoam-runtime-distribution.md`：v1 采用「探测 + 引导安装」（probe 命令已落地），捆绑方案列远期 |
| 签名与自动更新预留      | 签名/公证步骤与 updater 配置要求入档 `ai-docs/release.md`（证书就绪后启用）                                          |
| 依赖漏洞审计            | CI 新增 cargo audit job（M3 接受项兑现）                                                                             |

## 全项目完成度审计（T01–T21 逐项）

| 任务                     | 状态 | 提交/报告                   |
| ------------------------ | ---- | --------------------------- |
| T01 性能预算与基准       | ✅   | 3742c0b + perf-budget.md    |
| T02 CI/CD                | ✅   | d597ed0 + ci.yml            |
| T03 项目模型与持久化     | ✅   | 6066a0d                     |
| T04 材料数据库           | ✅   | 6340a67                     |
| T05 STL 导入             | ✅   | 2aea661                     |
| T06 体积网格生成         | ✅   | 9f6cfc0                     |
| T07 流道与水路建模       | ✅   | 045a6da                     |
| T08 工艺设置             | ✅   | f80c6c7                     |
| T09 OpenFOAM 集成        | ✅   | 1493347 + 合规结论文档      |
| T10 作业调度器           | ✅   | d5d5852                     |
| T11 填充端到端闭环       | ✅   | 302496d                     |
| T12 保压冷却             | ✅   | daf4ffb                     |
| T13 结果模型与流式读取   | ✅   | 9508e30                     |
| T14 3D 视口渲染引擎      | ✅   | 6904801                     |
| T15 XY 曲线与探针        | ✅   | 2352671                     |
| T16 设计系统与工作台布局 | ✅   | c879ff1                     |
| T17 报告导出             | ✅   | b6fba5f                     |
| T18 打包分发与发布流水线 | ✅   | e0bd22d                     |
| T19 GPU 基础设施         | ✅   | e612bc2                     |
| T20 GPU 加速算子         | ✅   | e612bc2                     |
| T21 评审循环             | ✅   | M0/M2/M3/M4 四轮 + 本轮收官 |

## 遗留与远期（全部带触发条件，非缺陷）

1. **真实求解运行验证**：需在装有 OpenFOAM 7 + openInjMoldSim 的机器执行 dogbone 端到端（T09 生成器已被单测覆盖，运行时验证依赖外部环境）。
2. **网格/视口正式性能计时**：待真实大网格基准资产（当前预算量级满足，线性扫描引擎）。
3. **签名与公证**：待证书就绪（macOS notarytool / Windows signtool 步骤已在 release.md 预留）。
4. **自动更新通道**：待 updater 签名密钥与端点（tauri updater 配置说明已在 release.md）。
5. **WebGPU 渲染后端**：待 WebView 普遍支持（当前 WebGL2 满足性能预算）。
6. **翘曲/纤维取向**：openInjMoldSim 能力之外，需结构求解器选型（远期里程碑）。

## 质量快照（收官时点）

- Rust 侧测试 75 项（core 单测 + 契约 + 适配层）全绿；前端 Vitest 39 项全绿；
- clippy `-D warnings` / rustfmt / prettier / knip 零容忍门禁全过；
- 依赖许可证：Apache-2.0（Kairos）+ MIT/Apache（全部第三方），GPL 隔离红线未被触碰。
