# T45 评审记录（T21 清单 · 随任务执行）

评审对象：render/picking.ts、renderer.ts getCamera/FOV_Y、useViewportPanel 拾取接线、
picking 单测、视口提示文案。

| #   | 清单项         | 结论                                                                                                                                                                                    |
| --- | -------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | 架构一致性     | 通过。拾取为纯前端渲染层能力（无 IPC / core 改动）；picking.ts 不依赖 WebGL 上下文，与 renderer 的耦合仅经 CameraSnapshot 快照接口                                                      |
| 2   | 性能预算对照   | 拾取为点击触发的单遍 O(面数) 扫描，无持续开销；百万面拾取性能归 T39 渲染后端决策一并度量                                                                                                |
| 3   | 代码与注释质量 | 通过。FOV_Y 常量消除渲染循环两处重复字面量；「位移 < 4px 判定点击」的阈值常量具名（PICK_SLOP_PIXELS）；无 TODO/FIXME                                                                    |
| 4   | 文档同步       | 通过。architecture-status XY 曲线行更新（空间拾取/场关联 [x]；时间序列联动待 T46）                                                                                                      |
| 5   | 测试覆盖缺口   | 通过（按豁免口径）。picking.ts 为纯函数，按 math.ts 先例补 5 项单测（射线方向、偏转、命中映射、背向/脱空）；useViewportPanel 属命令式 WebGL 集成（vitest 配置显式豁免，行为由真机验证） |
| 6   | 依赖健康       | 无新增依赖                                                                                                                                                                              |
| 7   | 安全           | 不涉及                                                                                                                                                                                  |
| 8   | 上轮遗留       | T44 评审「接受并记录」两项（派生不持久化、GPU 归 T39）不变，无新增                                                                                                                      |

## 处理记录

- **立即修**：无。
- **回流任务**：探针时间序列曲线与时间轴双向联动 → T46（已规划）。
- **接受并记录**：视口内暂不做探针标记点渲染（叠加层扩展时一并处理）。
