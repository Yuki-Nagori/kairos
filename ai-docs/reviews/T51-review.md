# T51 评审记录（T21 清单 · 随任务执行）

评审对象：services/results.rs（field_binary 编解码 + FieldCache）、
commands/results.rs（二进制命令 + 缓存接入）、utils/field-binary.ts、
api/results.ts 二进制通道切换、前后端测试。

| #   | 清单项         | 结论                                                                                                                                                                 |
| --- | -------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | 架构一致性     | 通过。编解码在 core（服务层，无 tauri 依赖）；tauri::ipc::Response 只出现在命令层；前端解析在 utils（覆盖率门槛内），store / 视口零改动受益                          |
| 2   | 性能预算对照   | JSON 数组 → 二进制：IPC 体积从每值 ~10–20 字节文本降到 8 字节定宽，且省去前端 JSON.parse 大数组；量化的对比数字待 T48 真机基准，此处仅记方向性结论                   |
| 3   | 代码与注释质量 | 通过。字节布局在编码器与解码器两处注释一致；错误信息区分魔数 / 元数据截断 / 值区截断；移除 ResultSlots 的 derive(Default)（FieldCache 无 Default，显式构造默认容量） |
| 4   | 文档同步       | 通过。architecture-status C7 备注更新：二进制通道 + FIFO 缓存落地，分块懒加载与持久关联保留                                                                          |
| 5   | 测试覆盖缺口   | 通过。core 4 测试（往返含 Inf、魔数 / 截断 / 溢出、FIFO 淘汰、同键覆盖）+ 前端 5 测试（手写字节流逐字段 + 全部错误路径）；双端四维覆盖 100%                          |
| 6   | 依赖健康       | 无新增依赖                                                                                                                                                           |
| 7   | 安全           | 通过。解码对长度 / 魔数 / 元数据 JSON / 值区逐一校验，恶意字节流不致 panic（无 unwrap 未校验路径）                                                                   |
| 8   | 上轮遗留       | T50 遗留不变；「研究结果持久关联」不在本任务，留待工程文件模型扩展任务                                                                                               |

## 处理记录

- **立即修**：byte 字符串初版误用 unicode escape（\\u{0}）改 \\x00；meta_end
  整型歧义补 usize 标注；ResultSlots derive(Default) 与 FieldCache 无 Default
  的冲突改为显式 Default 实现。
- **回流任务**：分块懒加载（按需读子区域）与研究结果持久关联——依赖
  工程文件 schema 扩展，暂不建档（待大结果数据链整体规划时编号）。
- **接受并记录**：FIFO 而非 LRU（命中不刷新新旧序）；缓存容量固定 8；
  重评条件：真机基准显示缓存命中率 / 容量成为瓶颈时再引入 LRU 与可配置容量。
