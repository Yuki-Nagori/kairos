# T44 评审记录（T21 清单 · 随任务执行）

评审对象：T44 派生结果算子产品管线（models/results.rs、services/results.rs、
commands/results.rs、结果面板与 store / api / types、契约与前端测试）。

| #   | 清单项         | 结论                                                                                                                                                                                                                  |
| --- | -------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | 架构一致性     | 通过。DeriveRequest 在 models（serde 形状）→ services（纯逻辑）→ commands（装配），core 无 tauri；DTO 双端镜像（types/index.ts `DeriveRequest`）+ 契约测试 `derive_request_serializes_with_kind_tag` 锁定 `kind` 标签 |
| 2   | 性能预算对照   | 派生为 O(n) 单遍扫描，未引入新预算项；百万值性能验收属 GPU 路线（T39 决策门），本任务不重复设标                                                                                                                       |
| 3   | 代码与注释质量 | 通过。无 TODO/FIXME；`Difference` 经单场入口的报错信息指明正确入口（`derive_difference`）；派生不改写会话缓存的语义写入命令文档注释                                                                                   |
| 4   | 文档同步       | 通过。architecture-status T31 行更新（线性映射 / 两场差值 → CPU 管线 [x]），GPU 接入与百万值验收明确归 T39                                                                                                            |
| 5   | 测试覆盖缺口   | 通过。core 新增 5 测试（线性数值与命名 / 差值 / 长度不一致 / 完整性与 / 单场入口报错）；前端新增 7 测试（线性透传、compare 槽位、差值派生、缺场静默、失败报错）；契约 1 项                                            |
| 6   | 依赖健康       | 通过。无新增依赖（knip 随 verify 检查）                                                                                                                                                                               |
| 7   | 安全           | 不涉及（无网络 / 文件写入 / CSP 面变化）                                                                                                                                                                              |
| 8   | 上轮遗留       | 上轮（T40–T43 review）"立即修"项均已落库；无未决项带入                                                                                                                                                                |

## 处理记录

- **立即修**：无（实现期内已按清单自检）。
- **回流任务**：无新增。GPU 算子接入与百万值性能验收已有归属（T39），不重复建档。
- **接受并记录**：
  - 派生场不持久化（刷新需重新生成）——当前结果目录为只读扫描模型，持久化
    依赖研究结果关联（二进制分块 / 持久关联，属 B3 大结果数据链），重评条件：
    T39 决策后启动大结果数据链任务时一并处理。
  - 派生管线保持 CPU f64（GPU 接入需先过 T39 的后端决策门，且 GPU 为 f32，
    精度语义需要与「无 CPU 回退」政策一并裁决）。
