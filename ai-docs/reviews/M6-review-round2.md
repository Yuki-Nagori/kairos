# M6 评审记录 · 第 2 轮（2026-09-13 收口批次，T21 循环任务）

评审范围：本批次 14 个提交（T55–T88，按「先非求解器、后求解链路」排序）。
逐任务评审记录见 `ai-docs/reviews/T{xx}-review.md`，本文件是**批次级对照与八项清单**。

## 一、八项清单结论（附证据）

| #   | 清单项         | 结论与证据                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| --- | -------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1   | 架构一致性     | ✓ core 无 tauri 依赖（仅 3 处文档提及）；views/components 无直连 `invokeCommand`（全走 stores/api）；新增 DTO 均双端镜像并有契约测试——`cargo test -p kairos-tests --test contract` **19 passed**（新增 `MeshEstimate` / `GateInlet` / `CaseReport` / `VectorField` / `GeometryRef` / `CaseOutcome` 形状锁定，schema v5 断言 `geometries` 为空数组）。**平台条件编译面复核**（T21 上轮教训）：本批次新增无 `cfg(target_os)` 分支，`generate_case` 的签名变更同步了 3 个平台调用点（CLI / tauri / 测试） |
| 2   | 性能预算对照   | ✓ 三条新预算全部实测回填 [perf-budget.md](../perf-budget.md)：浇口位置分析（62.5 万四面体 = 296 ms / 预算 3 s）、填充预览（168 ms / 预算 3 s）、大结果链路（1e7 值解析 426 ms、编码 31.4 ms / 40 MB、解码 12.4 ms；矢量 1e6 单元 9.1 / 4.0 ms）。其余改动（工作区路径、术语、草稿文档）无预算项                                                                                                                                                                                                        |
| 3   | 代码与注释质量 | ✓ `bun run lint:comments` 通过；`grep TODO/FIXME` 命中 4 处全为测试里的 `b"XXXX"` 字节字面量（假阳性）。**删掉 4 处不可达分支**：`gate_inlet_faces` 的半径外 break、`ValueFormat` 的 target≤0 回退、缓存「淘汰项恰为最近项」、`Component::Prefix/RootDir`（Unix 构造不出）改为显式冒号判定                                                                                                                                                                                                             |
| 4   | 文档同步       | ✓ 每个任务同步 architecture-status 对应分节 + tasks/README 索引 + timeline 一行；本轮重排索引（已实现 80 条 / 待办 13 条 + 循环任务单列）并把「收口批次」写成一节。README/AGENTS 的路径与命令未变，无漂移                                                                                                                                                                                                                                                                                              |
| 5   | 测试覆盖缺口   | ✓ 门禁仍为 100%：core 行覆盖 100%（`bun run coverage:rust` exit 0）、前端四维 100%（625 → 631 项）。**消融抽查 8 处**（每任务 1 处，记录见各自评审）：候选限表面 / 入口温度聚合 / 值区长度校验 / 多源种子 / 候选限面 + 中心点 / 位移聚合 / 工作区根判定 / 几何 id 对齐——全部「消融 → 定向用例变红 → 文件级还原 → 复绿」                                                                                                                                                                                |
| 6   | 依赖健康       | ✓ 本批次**零新增依赖**（`Cargo.toml`/`Cargo.lock`/`package.json`/`bun.lock` 无 diff）；knip 通过（新导出的类型/函数按需移除，未留投机 API——`CaseInletRecord` / `PickResult` / `PlacementState` / `workspaceReportsDir` 均在门禁暴露后收口）                                                                                                                                                                                                                                                            |
| 7   | 安全           | ✓ CSP 与 capabilities 未改动（`default-src 'self'` + IPC/装饰插件白名单；capabilities 仅 main 窗口 + dialog/decoration + 两个窗口控制权限）；未引入网络请求；新增路径一律经受控根目录校验（工作区相对路径拒绝绝对路径 / `..` / 冒号）。`cargo audit` 本机未安装，由 CI 执行                                                                                                                                                                                                                            |
| 8   | 上轮遗留追踪   | 见下节                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |

## 二、上轮遗留项逐条复核

| 上轮项                             | 本轮状态                                                        |
| ---------------------------------- | --------------------------------------------------------------- |
| T51「值未压缩」                    | ✅ 已闭环（T84：f32 截断，f64 通道保留对拍）                    |
| T51「缓存 FIFO」                   | ✅ 已闭环（T84：LRU + 值数预算 2e7，命中/未命中可观测）         |
| T51「矢量场只取模量」              | ✅ 已闭环（T84 三分量通道 + T65 变形显示消费）                  |
| C2「双域/中面求解侧未消费」        | ⏳ 仍在（T82，等上游降维能力）                                  |
| C5「冷却水路求解侧无」             | ✅ Kairos 侧闭环（T67）；三维水路 CHT 仍不做（需模具域网格）    |
| C6「翘曲变形可视化阻塞于 M4」      | ◐ Kairos 侧闭环（T65）；真机 case 级位移场验收仍等 M4（归 T29） |
| D0「Linux 需自装 OpenFOAM」        | ⏳ 仍在（T85，需 Ubuntu 真机）                                  |
| B3「三端真机 FPS」                 | ⏳ 仍在（T48，需三端设备）                                      |
| T21 上轮「平台条件编译面审计」教训 | ✓ 本批次主动复核（见清单 1）                                    |

## 三、性能数字对照（本批次新增）

| 项                          | 预算  | 实测                               | 结论                             |
| --------------------------- | ----- | ---------------------------------- | -------------------------------- |
| 浇口位置分析（50 万四面体） | ≤ 3 s | 296 ms（62.5 万四面体 / 399 候选） | 12 倍余量                        |
| 填充预览（50 万四面体）     | ≤ 3 s | 168 ms（含邻接构建）               | 18 倍余量                        |
| ascii 场解析（1e7 值）      | —     | 426 ms                             | 加载链路大头（上游产物格式限制） |
| 二进制编码 / 解码（1e7 值） | —     | 31.4 ms / 12.4 ms（40 MB）         | 相对 f64：传输减半、CPU 持平     |

## 四、本轮处理分类

- **立即修（已在本批次提交内）**：不可达分支 4 处、`CaseInputs` 结构体收口（clippy 参数阈值）、
  术语残留 43 文件、README 索引重排、任务档案状态行补全。
- **回流任务（已建档）**：T89（GAIM）、T90（微发泡近似）、T91（纤维取向链路）、
  T92（DOE）、T93（工艺寻优）——均由 T86/T87/T88 的可行性结论拆出，含开工条件。
- **接受并记录**：① T33/T48/T60/T82/T83/T85 仍未做，理由见 tasks/README 待办表；
  ② 近似路线的精度承诺（微发泡 PVT、浇口位置 / 填充预览启发式）一律标注「非求解结果」；
  ③ 大结果压缩未引入 zstd（基准显示收益在传输而非 CPU，f32 零依赖已够）。

## 五、下轮评审要重点复查的点

1. **T29 真机复跑**：本批次改了 case 生成的两处口径（入口面选择、模壁冷却边界）与
   矢量场通道，必须真机确认 `checkMesh` + 求解不回归（尤其冷却阶段的 `coolant` 子字典关键字）；
2. **T75 工作区**：跨机器拷贝与「重启后恢复」需在真机 GUI 走一遍（本机无桌面权限）；
3. **T85 Linux 通道**：Ubuntu 上补齐 bundle 落地与 OpenMPI 探测的实测判据；
4. **T48 三端渲染**：视口新增了变形显示（顶点重上传），FPS 与内存数字要包含该路径。
