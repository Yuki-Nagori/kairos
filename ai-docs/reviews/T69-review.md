# T69 评审记录（T21 清单 · 随任务执行）

评审对象：`src-web` 全部用户可见文案与注释、`kairos-core` / `kairos-cli` 的用户可见
错误文案与注释、受影响的前端与 Rust 测试断言。

| #   | 清单项         | 结论                                                                                        |
| --- | -------------- | ------------------------------------------------------------------------------------------- |
| 1   | 架构一致性     | 通过。纯文案替换，无逻辑 / 契约 / DTO 改动（字段与标识符保持 `Study` / `study`）            |
| 2   | 性能预算对照   | 不适用（零运行时行为变化）                                                                  |
| 3   | 代码与注释质量 | 通过。注释与用户文案用同一术语，避免「代码说方案、界面说研究」的二次漂移                    |
| 4   | 文档同步       | 通过。architecture-status 记录术语口径；本任务为非功能性收口，任务索引 T69 转已实现         |
| 5   | 测试覆盖缺口   | 通过。59 处测试断言随文案同步（`bun run test:coverage` 594 项 + Rust 全量通过，覆盖率不变） |
| 6   | 依赖健康       | 无新增依赖                                                                                  |
| 7   | 安全           | 通过。错误文案变化不改变错误 `code` 分类（前端按 code 分支，不匹配文本）                    |
| 8   | 上轮遗留       | 无未决项带入                                                                                |

## 口径

- **唯一术语：方案**。它是 `Study` 在界面上的唯一中文词；「研究」在全仓源码中不再出现
  （注释也一并收口，避免下次改代码时又按旧词写文案）。
- 保留不变：DTO 字段名（`studies` / `activeStudyId`）、TS/Rust 标识符、IPC 命令名、
  任务窗格的「方案任务」复合词。
- 校验口径：`grep -rn 研究 src-web src-crates src-tauri tests` 为空。

## 处理记录

- **立即修**：43 个文件一次性替换（含 4 处 Rust 用户可见错误文案与 1 处 CLI 输出行），
  同步 59 处测试断言。
- **回流任务**：无新增。
- **接受并记录**：`ai-docs/` 下的历史文档保留「研究」原样——它们记录的是当时的措辞与
  评审过程，改动会破坏时间线台账的可追溯性；本任务只收口产品与代码里的用户可见文案。
