# T50 评审记录（T21 清单 · 随任务执行）

评审对象：services/volume_field.rs（体数据重采样）、render/webgpu/volume.ts 与
shaders_volume.ts（光线步进 POC）、utils/bench/volume-render.ts（基准入口）、
volume_field 单测。

| #   | 清单项         | 结论                                                                                                                                                       |
| --- | -------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | 架构一致性     | 通过。体数据重采样在 core（纯逻辑 + 单测）；光线步进在 render/webgpu（POC 豁免口径）；两者经 VolumeGrid 数据形状衔接，无 IPC 变化                          |
| 2   | 性能预算对照   | 重采样为包围盒栅格化（与全场体素数解耦）；光线步进基准入口落地，128³ / 96 步的数字待真机——不伪造数字，预算对照随 T48 真机验收闭环                          |
| 3   | 代码与注释质量 | 通过。uniform 布局在 WGSL 头注释逐成员标注；重心坐标容差、先到先得覆盖语义有说明；初版的 index_range / FPS 除零 / value_max 占位残留在提交前自检中全部修正 |
| 4   | 文档同步       | 通过。architecture-status「三维体渲染」更新为 POC 落地 + 边界声明；任务文件如实标注「未接入产品视口」                                                      |
| 5   | 测试覆盖缺口   | 通过。core 新增 5 测试（分辨率边界 / 空网格 / 数量不一致 / 内部采样外部置零 / 两四面体区分 / 确定性），行覆盖 100%；WebGPU 侧按 GPU 集成豁免口径真机验证   |
| 6   | 依赖健康       | 无新增依赖                                                                                                                                                 |
| 7   | 安全           | 不涉及                                                                                                                                                     |
| 8   | 上轮遗留       | T49 遗留项不变；「体渲染重评条件为 WebGPU 主路径落地」——本轮 POC 即该条件的兑现起点                                                                        |

## 处理记录

- **立即修**：初版 index_range 含占位算式、FPS 计算含除零、value_max 为
  硬编码——提交前全部修正并补单测。
- **回流任务**：体渲染接入产品视口（视口模式开关 + 迁移函数 UI + 交互）
  待 T48 真机数字后决策，随 T48 走查一并处理，不单独建档。
- **接受并记录**：覆盖空洞按 0 值处理（诊断靠 covered_ratio）；步数固定 96；
  x 主序体素排布与 WGSL 采样约定配套，暂不提供可选排布。
