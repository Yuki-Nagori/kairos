{
  "name": "kairos",
  "private": true,
  "type": "module",
  "scripts": {
    "analyze": "ANALYZE=1 vite build",
    "bench": "bun run bench:web && cargo bench",
    "bench:web": "bun src-web/utils/bench/state.ts",
    "build": "bun run typecheck && vite build",
    "coverage:rust": "cargo llvm-cov -p kairos-core --lib --summary-only --fail-under-lines 98",
    "dev": "vite",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "format:rust": "cargo fmt --all",
    "format:rust:check": "cargo fmt --all --check",
    "knip": "knip",
    "lint": "eslint . --max-warnings 0",
    "lint:fix": "eslint . --fix",
    "lint:rust": "cargo clippy --workspace --all-targets -- -D warnings",
    "prepare": "husky",
    "preview": "vite preview",
    "tauri": "tauri",
    "tauri:build": "tauri build",
    "tauri:dev": "tauri dev",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:rust": "cargo test --workspace",
    "test:watch": "vitest",
    "typecheck": "vue-tsc --noEmit",
    "typecheck:rust": "cargo check --workspace",
    "verify": "bun run typecheck && bun run typecheck:rust && bun run lint && bun run lint:rust && bun run format:check && bun run format:rust:check && bun run test && bun run test:rust && bun run knip"
  },
  "devDependencies": {
    "@eslint/js": "^9",
    "@tailwindcss/vite": "^4",
    "@tauri-apps/cli": "^2",
    "@vitejs/plugin-vue": "^6.0.8",
    "@vitest/coverage-v8": "^5.0.0",
    "@vue/test-utils": "^2.5.0",
    "eslint": "^9",
    "eslint-config-prettier": "^10",
    "eslint-plugin-vue": "^10.11.0",
    "happy-dom": "^20.14.0",
    "husky": "^9.1.7",
    "knip": "^5",
    "prettier": "^3",
    "rollup-plugin-visualizer": "^7.1.1",
    "tailwindcss": "^4",
    "tinybench": "^6.1.6",
    "typescript": "^5",
    "typescript-eslint": "^8",
    "vite": "^7",
    "vitest": "^5.0.0",
    "vue-tsc": "^3.3.11"
  },
  "dependencies": {
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-dialog": "^2.7.3",
    "katex": "^0.18.7",
    "pinia": "^4.0.3",
    "vue": "^3.5.42"
  }
}
